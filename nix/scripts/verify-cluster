#!/usr/bin/env bash
set -euo pipefail
cluster=${1:?cluster or context required}
case "$cluster" in
  backbone) context=${BACKBONE_CONTEXT:-private-backbone} ;;
  *) context=$cluster ;;
esac
kubectl --context "$context" get --raw=/readyz >/dev/null
kubectl --context "$context" get nodes -o wide
