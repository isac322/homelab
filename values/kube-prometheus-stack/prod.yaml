## CRD upgrade job to avoid ServerSideApply timeout on large CRDs
##
crds:
  upgradeJob:
    enabled: true
    forceConflicts: true

## Provide a name in place of kube-prometheus-stack for `app:` labels
##
nameOverride: shared

## Create default rules for monitoring the cluster
##
defaultRules:
  create: false

## Configuration for alertmanager
## ref: https://prometheus.io/docs/alerting/alertmanager/
##
alertmanager:
  enabled: false

## Using default values from https://github.com/grafana/helm-charts/blob/main/charts/grafana/values.yaml
##
grafana:
  enabled: false

## Component scraping the kubelet and kubelet-hosted cAdvisor
##
kubelet:
  namespace: kube-system

  serviceMonitor:
    ## In K3s, kubelet /metrics exposes apiserver metrics too (single binary).
    ## Keep kubelet-specific metrics + metrics used by the kubelet Grafana dashboard.
    metricRelabelings:
      - sourceLabels: [__name__]
        regex: kubelet_.*|rest_client_.*|storage_operation_.*|volume_manager_.*|process_.*|go_goroutines|scrape_.*|up
        action: keep

## K3s bundles all control plane components into a single binary.
## Their metrics are already collected via the apiserver job.
kubeEtcd:
  enabled: false
kubeProxy:
  enabled: false
kubeControllerManager:
  enabled: false
kubeScheduler:
  enabled: false

## Manages Prometheus and Alertmanager components
##
prometheusOperator:
  resources:
    requests:
      cpu: 10m
      memory: 32Mi
    limits:
      cpu: 200m
      memory: 128Mi

## Deploy a Prometheus instance
##
prometheus:
  ## Toggle prometheus into agent mode
  ## Note many of features described below (e.g. rules, query, alerting, remote read, thanos) will not work in agent mode.
  ## ref: https://github.com/prometheus-operator/prometheus-operator/blob/main/Documentation/platform/prometheus-agent.md
  ##
  agentMode: true

  ## Settings affecting prometheusSpec
  ## ref: https://github.com/prometheus-operator/prometheus-operator/blob/main/Documentation/api-reference/api.md#prometheusspec
  ##
  prometheusSpec:
    ## External labels to add to any time series or alerts when communicating with external systems
    ##
    externalLabels:
      cluster: prod

    ## The remote_write spec configuration for Prometheus.
    ## ref: https://github.com/prometheus-operator/prometheus-operator/blob/main/Documentation/api-reference/api.md#remotewritespec
    remoteWrite:
      - url: "https://prometheus.bhyoo.com/api/v1/write"
        name: backbone

    ## Resource limits & requests
    ##
    resources:
      requests:
        cpu: 30m
        memory: 256Mi
      limits:
        cpu: 500m
        memory: 1Gi
