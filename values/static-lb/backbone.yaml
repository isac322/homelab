resources:
  requests:
    cpu: 5m
    memory: 8Mi
  limits:
    cpu: 10m
    memory: 32Mi

# where to assign node's internal ips (enum: ingress, external)
internalIPMappings:
  - ingress

# where to assign node's internal ips (enum: ingress, external)
externalIPMappings:
  - ingress
