resources:
  requests:
    cpu: 5m
    memory: 16Mi
  limits:
    cpu: 10m
    memory: 48Mi

# where to assign node's internal ips (enum: ingress, external)
internalIPMappings: []

# where to assign node's internal ips (enum: ingress, external)
externalIPMappings:
  - ingress
