apiVersion: v1
kind: LimitRange
metadata:
  name: cloudflare-gateway-limits
  namespace: cloudflare-gateway
spec:
  limits:
    - type: Container
      defaultRequest:
        cpu: 15m
        memory: 64Mi
      default:
        cpu: 100m
        memory: 128Mi
