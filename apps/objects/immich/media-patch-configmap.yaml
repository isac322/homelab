apiVersion: v1
kind: ConfigMap
metadata:
  name: immich-media-patch
  namespace: immich
data:
  media.js: |
    "use strict";
    Object.defineProperty(exports, "__esModule", { value: true });
    exports.RkmppHwDecodeConfig = exports.RkmppSwDecodeConfig = exports.VaapiHwDecodeConfig = exports.VaapiSwDecodeConfig = exports.QsvHwDecodeConfig = exports.QsvSwDecodeConfig = exports.NvencHwDecodeConfig = exports.NvencSwDecodeConfig = exports.AV1Config = exports.VP9Config = exports.HEVCConfig = exports.H264Config = exports.ThumbnailConfig = exports.BaseHWConfig = exports.BaseConfig = exports.getCodecString = exports.getOutputSize = exports.isVideoVertical = exports.isVideoRotated = void 0;
    const constants_1 = require("../constants");
    const enum_1 = require("../enum");
    const isVideoRotated = (videoStream) => Math.abs(videoStream.rotation) === 90;
    exports.isVideoRotated = isVideoRotated;
    const isVideoVertical = (videoStream) => videoStream.height > videoStream.width || (0, exports.isVideoRotated)(videoStream);
    exports.isVideoVertical = isVideoVertical;
    const getOutputSize = (videoStream, targetRes) => {
        const factor = Math.max(videoStream.height, videoStream.width) / Math.min(videoStream.height, videoStream.width);
        let larger = Math.round(targetRes * factor);
        if (larger % 2 !== 0) {
            larger -= 1;
        }
        return (0, exports.isVideoVertical)(videoStream) ? { width: targetRes, height: larger } : { width: larger, height: targetRes };
    };
    exports.getOutputSize = getOutputSize;
    const pickLevel = (levels, frame, rate) => levels.find((level) => frame <= level.maxFrame && rate <= level.maxRate) ?? levels.at(-1);
    const getCodecString = (codec, width, height, fps) => {
        switch (codec) {
            case enum_1.VideoCodec.H264: {
                const macroblocks = Math.ceil(width / 16) * Math.ceil(height / 16);
                return `avc1.6400${pickLevel(constants_1.H264_LEVELS, macroblocks, macroblocks * fps).token}`;
            }
            case enum_1.VideoCodec.Hevc: {
                const samples = width * height;
                return `hvc1.1.6.${pickLevel(constants_1.HEVC_LEVELS, samples, samples * fps).token}.B0`;
            }
            case enum_1.VideoCodec.Av1: {
                const samples = width * height;
                return `av01.0.${pickLevel(constants_1.AV1_LEVELS, samples, samples * fps).token}.08`;
            }
            default: {
                throw new Error(`Codec '${codec}' does not support HLS codec strings`);
            }
        }
    };
    exports.getCodecString = getCodecString;
    class BaseConfig {
        config;
        tune;
        presets = ['veryslow', 'slower', 'slow', 'medium', 'fast', 'faster', 'veryfast', 'superfast', 'ultrafast'];
        constructor(config, tune = { strictGop: false, lowLatency: false }) {
            this.config = config;
            this.tune = tune;
        }
        static create(config, interfaces, tune) {
            if (config.accel === enum_1.TranscodeHardwareAcceleration.Disabled) {
                return BaseConfig.getSWCodecConfig(config, tune);
            }
            return BaseConfig.getHWCodecConfig(config, interfaces, tune);
        }
        static getSWCodecConfig(config, tune) {
            switch (config.targetVideoCodec) {
                case enum_1.VideoCodec.H264: {
                    return new H264Config(config, tune);
                }
                case enum_1.VideoCodec.Hevc: {
                    return new HEVCConfig(config, tune);
                }
                case enum_1.VideoCodec.Vp9: {
                    return new VP9Config(config, tune);
                }
                case enum_1.VideoCodec.Av1: {
                    return new AV1Config(config, tune);
                }
                default: {
                    throw new Error(`Codec '${config.targetVideoCodec}' is unsupported`);
                }
            }
        }
        static getHWCodecConfig(config, interfaces, tune) {
            if (!constants_1.SUPPORTED_HWA_CODECS[config.accel].includes(config.targetVideoCodec)) {
                throw new Error(`${config.accel.toUpperCase()} acceleration does not support codec '${config.targetVideoCodec.toUpperCase()}'. Supported codecs: ${constants_1.SUPPORTED_HWA_CODECS[config.accel]}`);
            }
            let handler;
            switch (config.accel) {
                case enum_1.TranscodeHardwareAcceleration.Nvenc: {
                    handler = config.accelDecode
                        ? new NvencHwDecodeConfig(config, interfaces, tune)
                        : new NvencSwDecodeConfig(config, interfaces, tune);
                    break;
                }
                case enum_1.TranscodeHardwareAcceleration.Qsv: {
                    handler = config.accelDecode
                        ? new QsvHwDecodeConfig(config, interfaces, tune)
                        : new QsvSwDecodeConfig(config, interfaces, tune);
                    break;
                }
                case enum_1.TranscodeHardwareAcceleration.Vaapi: {
                    handler = config.accelDecode
                        ? new VaapiHwDecodeConfig(config, interfaces, tune)
                        : new VaapiSwDecodeConfig(config, interfaces, tune);
                    break;
                }
                case enum_1.TranscodeHardwareAcceleration.Rkmpp: {
                    handler = config.accelDecode
                        ? new RkmppHwDecodeConfig(config, interfaces, tune)
                        : new RkmppSwDecodeConfig(config, interfaces, tune);
                    break;
                }
                default: {
                    throw new Error(`${config.accel.toUpperCase()} acceleration is unsupported`);
                }
            }
            return handler;
        }
        getCommand(target, video, audio, format) {
            const options = {
                inputOptions: this.getBaseInputOptions(video, format),
                outputOptions: [
                    ...this.getBaseOutputOptions(target, video, audio),
                    ...this.getPresetOptions(),
                    ...this.getBitrateOptions(),
                    ...this.getEncoderOptions(),
                    '-movflags',
                    'faststart',
                    '-fps_mode',
                    'passthrough',
                    '-v',
                    'verbose',
                ],
                twoPass: this.eligibleForTwoPass(),
                progress: { frameCount: video.frameCount, percentInterval: 5 },
            };
            if ([enum_1.TranscodeTarget.All, enum_1.TranscodeTarget.Video].includes(target)) {
                const filters = this.getFilterOptions(video);
                if (filters.length > 0) {
                    options.outputOptions.push('-vf', filters.join(','));
                }
            }
            return options;
        }
        getHlsCommand(options, video, audio) {
            const args = this.getBaseInputOptions(video);
            if (options.seekSeconds) {
                args.push('-ss', String(options.seekSeconds));
            }
            args.push('-nostdin', '-nostats', '-i', options.inputPath, ...this.getBaseOutputOptions(options.target, video, audio), ...this.getPresetOptions(), ...this.getBitrateOptions(), ...this.getEncoderOptions(), '-copyts', '-r', `${options.packetCount * options.timeBase}/${options.totalDuration}`, '-avoid_negative_ts', 'disabled', '-f', 'hls', '-hls_time', String(options.segmentDuration), '-hls_list_size', '0', '-hls_segment_type', 'fmp4', '-hls_fmp4_init_filename', options.initFilename, '-hls_segment_options', 'movflags=+frag_discont', '-hls_flags', 'temp_file', '-hls_segment_filename', options.segmentFilename, '-start_number', String(options.startSegment));
            if ([enum_1.TranscodeTarget.All, enum_1.TranscodeTarget.Video].includes(options.target)) {
                const filters = this.getFilterOptions(video);
                if (filters.length > 0) {
                    args.push('-vf', filters.join(','));
                }
            }
            args.push(options.playlistFilename);
            return args;
        }
        getBaseInputOptions(videoStream, format) {
            return this.getInputThreadOptions();
        }
        getBaseOutputOptions(target, videoStream, audioStream) {
            const videoCodec = [enum_1.TranscodeTarget.All, enum_1.TranscodeTarget.Video].includes(target) ? this.getVideoCodec() : 'copy';
            const audioCodec = [enum_1.TranscodeTarget.All, enum_1.TranscodeTarget.Audio].includes(target) ? this.getAudioEncoder() : 'copy';
            const options = ['-c:v', videoCodec, '-c:a', audioCodec, '-map', `0:${videoStream.index}`, '-map_metadata', '-1'];
            if (audioStream) {
                options.push('-map', `0:${audioStream.index}`);
                if ([enum_1.TranscodeTarget.All, enum_1.TranscodeTarget.Audio].includes(target)) {
                    options.push('-ac', '2');
                }
            }
            if (this.getBFrames() > -1) {
                options.push('-bf', String(this.getBFrames()));
            }
            if (this.getRefs() > 0) {
                options.push('-refs', String(this.getRefs()));
            }
            if (this.getGopSize() > 0) {
                options.push('-g', String(this.getGopSize()));
                if (this.tune.strictGop) {
                    options.push('-keyint_min', String(this.getGopSize()));
                }
            }
            const isHvc = this.config.targetVideoCodec === enum_1.VideoCodec.Hevc && (videoCodec !== 'copy' || videoStream.codecName === 'hevc');
            if (isHvc) {
                options.push('-tag:v', 'hvc1');
            }
            return options;
        }
        getEncoderOptions() {
            return [];
        }
        getFilterOptions(videoStream) {
            const options = [];
            if (this.shouldScale(videoStream)) {
                options.push(`scale=${this.getScaling(videoStream)}`);
            }
            const tonemapOptions = this.getToneMapping(videoStream);
            if (tonemapOptions.length > 0) {
                options.push(...tonemapOptions);
            }
            else if (!videoStream.pixelFormat.endsWith('420p')) {
                options.push('format=yuv420p');
            }
            return options;
        }
        getPresetOptions() {
            return ['-preset', this.config.preset];
        }
        getBitrateOptions() {
            const bitrates = this.getBitrateDistribution();
            if (this.eligibleForTwoPass()) {
                return [
                    '-b:v',
                    `${bitrates.target}${bitrates.unit}`,
                    '-minrate',
                    `${bitrates.min}${bitrates.unit}`,
                    '-maxrate',
                    `${bitrates.max}${bitrates.unit}`,
                ];
            }
            if (bitrates.max > 0) {
                return [
                    `-${this.useCQP() ? 'q:v' : 'crf'}`,
                    String(this.config.crf),
                    '-maxrate',
                    `${bitrates.max}${bitrates.unit}`,
                    '-bufsize',
                    `${bitrates.max * 2}${bitrates.unit}`,
                ];
            }
            return [`-${this.useCQP() ? 'q:v' : 'crf'}`, String(this.config.crf)];
        }
        getInputThreadOptions() {
            return [];
        }
        getOutputThreadOptions() {
            if (this.config.threads <= 0) {
                return [];
            }
            return ['-threads', String(this.config.threads)];
        }
        eligibleForTwoPass() {
            if (!this.config.twoPass || this.config.accel !== enum_1.TranscodeHardwareAcceleration.Disabled) {
                return false;
            }
            return this.isBitrateConstrained();
        }
        getBitrateDistribution() {
            const max = this.getMaxBitrateValue();
            const target = Math.ceil(max / 1.45);
            const min = target / 2;
            const unit = this.getBitrateUnit();
            return { max, target, min, unit };
        }
        getTargetResolution(videoStream) {
            let target;
            target =
                this.config.targetResolution === 'original'
                    ? Math.min(videoStream.height, videoStream.width)
                    : Number.parseInt(this.config.targetResolution);
            if (target % 2 !== 0) {
                target -= 1;
            }
            return target;
        }
        shouldScale(videoStream) {
            const isOddDimensions = videoStream.height % 2 !== 0 || videoStream.width % 2 !== 0;
            const isLargerThanTarget = Math.min(videoStream.height, videoStream.width) > this.getTargetResolution(videoStream);
            return isOddDimensions || isLargerThanTarget;
        }
        shouldToneMap(videoStream) {
            return (this.config.tonemap !== enum_1.ToneMapping.Disabled &&
                (videoStream.colorTransfer === enum_1.ColorTransfer.Smpte2084 || videoStream.colorTransfer === enum_1.ColorTransfer.AribStdB67));
        }
        getScaling(videoStream, mult = 2) {
            const targetResolution = this.getTargetResolution(videoStream);
            return (0, exports.isVideoVertical)(videoStream) ? `${targetResolution}:-${mult}` : `-${mult}:${targetResolution}`;
        }
        isBitrateConstrained() {
            return this.getMaxBitrateValue() > 0;
        }
        getBitrateUnit() {
            const maxBitrate = this.getMaxBitrateValue();
            return this.config.maxBitrate.trim().slice(maxBitrate.toString().length) || 'k';
        }
        getMaxBitrateValue() {
            return Number.parseInt(this.config.maxBitrate) || 0;
        }
        getPresetIndex() {
            return this.presets.indexOf(this.config.preset);
        }
        getColors() {
            return {
                primaries: 'bt709',
                transfer: 'bt709',
                matrix: 'bt709',
            };
        }
        getToneMapping(videoStream) {
            if (!this.shouldToneMap(videoStream)) {
                return [];
            }
            const { primaries, transfer, matrix } = this.getColors();
            const options = `tonemapx=tonemap=${this.config.tonemap}:desat=0:p=${primaries}:t=${transfer}:m=${matrix}:r=tv:format=yuv420p`;
            return [options];
        }
        getAudioEncoder() {
            return constants_1.AUDIO_ENCODER[this.config.targetAudioCodec];
        }
        getVideoCodec() {
            return this.config.targetVideoCodec;
        }
        getBFrames() {
            return this.config.bframes;
        }
        getRefs() {
            return this.config.refs;
        }
        getGopSize() {
            return this.config.gopSize;
        }
        useCQP() {
            return this.config.cqMode === enum_1.CQMode.Cqp;
        }
    }
    exports.BaseConfig = BaseConfig;
    class BaseHWConfig extends BaseConfig {
        config;
        interfaces;
        device;
        constructor(config, interfaces, tune) {
            super(config, tune);
            this.config = config;
            this.interfaces = interfaces;
            this.device = this.getDevice(interfaces);
        }
        validateDevices(devices) {
            if (devices.length === 0) {
                throw new Error('No /dev/dri devices found. If using Docker, make sure at least one /dev/dri device is mounted');
            }
            return devices.filter(function (device) {
                return device.startsWith('renderD') || device.startsWith('card');
            });
        }
        getDevice({ dri }) {
            if (this.config.preferredHwDevice === 'auto') {
                return `/dev/dri/${this.validateDevices(dri).reduce(function (a, b) {
                    return a.localeCompare(b) < 0 ? b : a;
                })}`;
            }
            const deviceName = this.config.preferredHwDevice.replace('/dev/dri/', '');
            if (!dri.includes(deviceName)) {
                throw new Error(`Device '${deviceName}' does not exist. If using Docker, make sure this device is mounted`);
            }
            return `/dev/dri/${deviceName}`;
        }
        getVideoCodec() {
            return `${this.config.targetVideoCodec}_${this.config.accel}`;
        }
        getGopSize() {
            if (this.config.gopSize <= 0) {
                return 256;
            }
            return this.config.gopSize;
        }
    }
    exports.BaseHWConfig = BaseHWConfig;
    class ThumbnailConfig extends BaseConfig {
        static create(config) {
            return new ThumbnailConfig(config);
        }
        getBaseInputOptions(videoStream, format) {
            const options = format?.formatName === 'mpegts'
                ? ['-sws_flags', 'accurate_rnd+full_chroma_int']
                : ['-skip_frame', 'nointra', '-sws_flags', 'accurate_rnd+full_chroma_int'];
            const metadataOverrides = [];
            if (videoStream.colorPrimaries === enum_1.ColorPrimaries.Reserved) {
                metadataOverrides.push('colour_primaries=1');
            }
            if (videoStream.colorMatrix === enum_1.ColorMatrix.Reserved) {
                metadataOverrides.push('matrix_coefficients=1');
            }
            if (videoStream.colorTransfer === enum_1.ColorTransfer.Reserved) {
                metadataOverrides.push('transfer_characteristics=1');
            }
            if (metadataOverrides.length > 0) {
                options.push(`-bsf:${videoStream.index}`, `${videoStream.codecName}_metadata=${metadataOverrides.join(':')}`);
            }
            return options;
        }
        getBaseOutputOptions() {
            return ['-fps_mode', 'vfr', '-frames:v', '1', '-update', '1'];
        }
        getFilterOptions(videoStream) {
            return [
                'fps=12:start_time=0:eof_action=pass:round=down',
                'thumbnail=12',
                String.raw `select=gt(scene\,0.1)-eq(prev_selected_n\,n)+isnan(prev_selected_n)+gt(n\,20)`,
                'trim=end_frame=2',
                'reverse',
                ...super.getFilterOptions(videoStream),
            ];
        }
        getPresetOptions() {
            return [];
        }
        getBitrateOptions() {
            return [];
        }
        eligibleForTwoPass() {
            return false;
        }
        getScaling(videoStream) {
            return super.getScaling(videoStream) + ':flags=lanczos+accurate_rnd+full_chroma_int:out_range=pc';
        }
    }
    exports.ThumbnailConfig = ThumbnailConfig;
    class H264Config extends BaseConfig {
        getEncoderOptions() {
            const out = this.getOutputThreadOptions();
            if (this.tune.strictGop) {
                out.push('-sc_threshold:v', '0');
            }
            if (this.config.threads === 1) {
                out.push('-x264-params', 'frame-threads=1:pools=none');
            }
            return out;
        }
    }
    exports.H264Config = H264Config;
    class HEVCConfig extends BaseConfig {
        getEncoderOptions() {
            const out = this.getOutputThreadOptions();
            const params = [];
            if (this.tune.strictGop) {
                params.push('no-scenecut=1', 'no-open-gop=1');
            }
            if (this.config.threads === 1) {
                params.push('frame-threads=1', 'pools=none');
            }
            if (params.length > 0) {
                out.push('-x265-params', params.join(':'));
            }
            return out;
        }
    }
    exports.HEVCConfig = HEVCConfig;
    class VP9Config extends BaseConfig {
        getPresetOptions() {
            const speed = Math.min(this.getPresetIndex(), 5);
            if (speed >= 0) {
                return ['-cpu-used', String(speed)];
            }
            return [];
        }
        getBitrateOptions() {
            const bitrates = this.getBitrateDistribution();
            if (bitrates.max > 0 && this.eligibleForTwoPass()) {
                return [
                    '-b:v',
                    `${bitrates.target}${bitrates.unit}`,
                    '-minrate',
                    `${bitrates.min}${bitrates.unit}`,
                    '-maxrate',
                    `${bitrates.max}${bitrates.unit}`,
                ];
            }
            return [`-${this.useCQP() ? 'q:v' : 'crf'}`, String(this.config.crf), '-b:v', `${bitrates.max}${bitrates.unit}`];
        }
        getEncoderOptions() {
            return ['-row-mt', '1', ...this.getOutputThreadOptions()];
        }
        eligibleForTwoPass() {
            return this.config.twoPass;
        }
    }
    exports.VP9Config = VP9Config;
    class AV1Config extends BaseConfig {
        getVideoCodec() {
            return 'libsvtav1';
        }
        getPresetOptions() {
            const speed = this.getPresetIndex() + 4;
            if (speed >= 0) {
                return ['-preset', String(speed)];
            }
            return [];
        }
        getBitrateOptions() {
            return ['-crf', String(this.config.crf)];
        }
        getEncoderOptions() {
            const params = [];
            if (this.tune.lowLatency) {
                params.push('hierarchical-levels=3', 'lookahead=0', 'enable-tf=0');
            }
            if (this.config.threads > 0) {
                params.push(`lp=${this.config.threads}`);
            }
            const bitrates = this.getBitrateDistribution();
            if (bitrates.max > 0) {
                params.push(`mbr=${bitrates.max}${bitrates.unit}`);
            }
            return params.length > 0 ? ['-svtav1-params', params.join(':')] : [];
        }
        eligibleForTwoPass() {
            return this.config.twoPass;
        }
    }
    exports.AV1Config = AV1Config;
    class NvencSwDecodeConfig extends BaseHWConfig {
        getDevice() {
            return '0';
        }
        getBaseInputOptions() {
            return ['-init_hw_device', `cuda=cuda:${this.device}`, '-filter_hw_device', 'cuda'];
        }
        getBaseOutputOptions(target, videoStream, audioStream) {
            const options = [
                '-tune',
                'hq',
                '-qmin',
                '0',
                '-rc-lookahead',
                '20',
                '-i_qfactor',
                '0.75',
                ...super.getBaseOutputOptions(target, videoStream, audioStream),
            ];
            if (this.getBFrames() > 0) {
                options.push('-b_ref_mode', 'middle', '-b_qfactor', '1.1');
            }
            if (this.config.temporalAQ) {
                options.push('-temporal-aq', '1');
            }
            return options;
        }
        getFilterOptions(videoStream) {
            const options = this.getToneMapping(videoStream);
            options.push('hwupload_cuda');
            if (this.shouldScale(videoStream)) {
                options.push(`scale_cuda=${this.getScaling(videoStream)}:format=nv12`);
            }
            return options;
        }
        getPresetOptions() {
            let presetIndex = this.getPresetIndex();
            if (presetIndex < 0) {
                return [];
            }
            presetIndex = 7 - Math.min(6, presetIndex);
            return ['-preset', `p${presetIndex}`];
        }
        getBitrateOptions() {
            const bitrates = this.getBitrateDistribution();
            if (bitrates.max > 0 && this.config.twoPass) {
                return [
                    '-b:v',
                    `${bitrates.target}${bitrates.unit}`,
                    '-maxrate',
                    `${bitrates.max}${bitrates.unit}`,
                    '-bufsize',
                    `${bitrates.target}${bitrates.unit}`,
                    '-multipass',
                    '2',
                ];
            }
            return bitrates.max > 0
                ? [
                    '-cq:v',
                    String(this.config.crf),
                    '-maxrate',
                    `${bitrates.max}${bitrates.unit}`,
                    '-bufsize',
                    `${bitrates.target}${bitrates.unit}`,
                ]
                : ['-cq:v', String(this.config.crf)];
        }
        getEncoderOptions() {
            const out = this.getOutputThreadOptions();
            if (this.tune.strictGop) {
                out.push('-forced-idr', '1');
            }
            return out;
        }
        getRefs() {
            const bframes = this.getBFrames();
            if (bframes > 0 && bframes < 3 && this.config.refs < 3) {
                return 0;
            }
            return this.config.refs;
        }
    }
    exports.NvencSwDecodeConfig = NvencSwDecodeConfig;
    class NvencHwDecodeConfig extends NvencSwDecodeConfig {
        getBaseInputOptions() {
            return ['-hwaccel', 'cuda', '-hwaccel_output_format', 'cuda', '-noautorotate', ...this.getInputThreadOptions()];
        }
        getFilterOptions(videoStream) {
            const options = [];
            const tonemapOptions = this.getToneMapping(videoStream);
            if (this.shouldScale(videoStream) || (tonemapOptions.length === 0 && !videoStream.pixelFormat.endsWith('420p'))) {
                options.push(`scale_cuda=${this.getScaling(videoStream)}`);
            }
            options.push(...tonemapOptions);
            if (options.length > 0) {
                options[options.length - 1] += ':format=nv12';
            }
            return options;
        }
        getToneMapping(videoStream) {
            if (!this.shouldToneMap(videoStream)) {
                return [];
            }
            const { matrix, primaries, transfer } = this.getColors();
            const tonemapOptions = [
                'desat=0',
                `matrix=${matrix}`,
                `primaries=${primaries}`,
                'range=pc',
                `tonemap=${this.config.tonemap}`,
                'tonemap_mode=lum',
                `transfer=${transfer}`,
                'peak=100',
            ];
            return [`tonemap_cuda=${tonemapOptions.join(':')}`];
        }
        getInputThreadOptions() {
            return ['-threads', '1'];
        }
        getEncoderOptions() {
            return this.tune.strictGop ? ['-forced-idr', '1'] : [];
        }
    }
    exports.NvencHwDecodeConfig = NvencHwDecodeConfig;
    class QsvSwDecodeConfig extends BaseHWConfig {
        getBaseInputOptions() {
            return ['-init_hw_device', `qsv=hw,child_device=${this.device}`, '-filter_hw_device', 'hw'];
        }
        getBaseOutputOptions(target, videoStream, audioStream) {
            const options = super.getBaseOutputOptions(target, videoStream, audioStream);
            if (this.config.targetVideoCodec === enum_1.VideoCodec.Vp9) {
                options.push('-low_power', '1');
            }
            return options;
        }
        getFilterOptions(videoStream) {
            const options = this.getToneMapping(videoStream);
            options.push('hwupload=extra_hw_frames=64');
            if (this.shouldScale(videoStream)) {
                options.push(`scale_qsv=${this.getScaling(videoStream)}:mode=hq:format=nv12`);
            }
            return options;
        }
        getPresetOptions() {
            let presetIndex = this.getPresetIndex();
            if (presetIndex < 0) {
                return [];
            }
            presetIndex = Math.min(6, presetIndex) + 1;
            return ['-preset', String(presetIndex)];
        }
        getBitrateOptions() {
            const options = [`-${this.useCQP() ? 'q:v' : 'global_quality:v'}`, String(this.config.crf)];
            const bitrates = this.getBitrateDistribution();
            if (bitrates.max > 0) {
                if (!this.useCQP()) {
                    options.push('-b:v', `${bitrates.target}${bitrates.unit}`);
                }
                options.push('-maxrate', `${bitrates.max}${bitrates.unit}`, '-bufsize', `${bitrates.max * 2}${bitrates.unit}`);
            }
            return options;
        }
        getBFrames() {
            if (this.config.bframes < 0) {
                return 7;
            }
            return this.config.bframes;
        }
        getRefs() {
            if (this.config.refs <= 0) {
                return 5;
            }
            return this.config.refs;
        }
        useCQP() {
            return this.config.cqMode === enum_1.CQMode.Cqp || this.config.targetVideoCodec === enum_1.VideoCodec.Vp9;
        }
        getScaling(videoStream) {
            return super.getScaling(videoStream, 1);
        }
        getEncoderOptions() {
            const out = this.getOutputThreadOptions();
            if (this.tune.strictGop) {
                out.push('-idr_interval', '0');
            }
            return out;
        }
    }
    exports.QsvSwDecodeConfig = QsvSwDecodeConfig;
    class QsvHwDecodeConfig extends QsvSwDecodeConfig {
        getBaseInputOptions() {
            return [
                '-hwaccel',
                'qsv',
                '-hwaccel_output_format',
                'qsv',
                '-async_depth',
                '4',
                '-noautorotate',
                '-qsv_device',
                this.device,
                ...this.getInputThreadOptions(),
            ];
        }
        getFilterOptions(videoStream) {
            const options = [];
            const tonemapOptions = this.getToneMapping(videoStream);
            if (tonemapOptions.length === 0 && !videoStream.pixelFormat.endsWith('420p')) {
                options.push(`scale_qsv=${this.getScaling(videoStream)}:async_depth=4:mode=hq:format=nv12`);
            }
            else if (this.shouldScale(videoStream)) {
                options.push(`scale_qsv=${this.getScaling(videoStream)}:async_depth=4:mode=hq`);
            }
            options.push(...tonemapOptions);
            return options;
        }
        getToneMapping(videoStream) {
            if (!this.shouldToneMap(videoStream)) {
                return [];
            }
            const { matrix, primaries, transfer } = this.getColors();
            const tonemapOptions = [
                'desat=0',
                'format=nv12',
                `matrix=${matrix}`,
                `primaries=${primaries}`,
                `transfer=${transfer}`,
                'range=pc',
                `tonemap=${this.config.tonemap}`,
                'tonemap_mode=lum',
                'peak=100',
            ];
            return [
                'hwmap=derive_device=opencl',
                `tonemap_opencl=${tonemapOptions.join(':')}`,
                'hwmap=derive_device=qsv:reverse=1,format=qsv',
            ];
        }
        getInputThreadOptions() {
            return ['-threads', '1'];
        }
    }
    exports.QsvHwDecodeConfig = QsvHwDecodeConfig;
    class VaapiSwDecodeConfig extends BaseHWConfig {
        getBaseInputOptions() {
            return ['-init_hw_device', `vaapi=accel:${this.device}`, '-filter_hw_device', 'accel'];
        }
        getFilterOptions(videoStream) {
            const options = this.getToneMapping(videoStream);
            options.push('hwupload=extra_hw_frames=64');
            if (this.shouldScale(videoStream)) {
                options.push(`scale_vaapi=${this.getScaling(videoStream)}:mode=hq:out_range=pc:format=nv12`);
            }
            return options;
        }
        getPresetOptions() {
            let presetIndex = this.getPresetIndex();
            if (presetIndex < 0) {
                return [];
            }
            presetIndex = Math.min(6, presetIndex) + 1;
            return ['-compression_level', String(presetIndex)];
        }
        getBitrateOptions() {
            const bitrates = this.getBitrateDistribution();
            const options = [];
            if (this.config.targetVideoCodec === enum_1.VideoCodec.Vp9) {
                options.push('-bsf:v', 'vp9_raw_reorder,vp9_superframe');
            }
            if (bitrates.max > 0) {
                options.push('-b:v', `${bitrates.target}${bitrates.unit}`, '-maxrate', `${bitrates.max}${bitrates.unit}`, '-minrate', `${bitrates.min}${bitrates.unit}`, '-rc_mode', '3');
            }
            else if (this.useCQP()) {
                options.push('-qp:v', String(this.config.crf), '-global_quality:v', String(this.config.crf), '-rc_mode', '1');
            }
            else {
                options.push('-global_quality:v', String(this.config.crf), '-rc_mode', '4');
            }
            return options;
        }
        useCQP() {
            return this.config.cqMode !== enum_1.CQMode.Icq || this.config.targetVideoCodec === enum_1.VideoCodec.Vp9;
        }
        getEncoderOptions() {
            const out = this.getOutputThreadOptions();
            if (this.tune.strictGop) {
                out.push('-idr_interval', '0');
            }
            return out;
        }
    }
    exports.VaapiSwDecodeConfig = VaapiSwDecodeConfig;
    class VaapiHwDecodeConfig extends VaapiSwDecodeConfig {
        getBaseInputOptions() {
            return [
                '-hwaccel',
                'vaapi',
                '-hwaccel_output_format',
                'vaapi',
                '-noautorotate',
                '-hwaccel_device',
                this.device,
                ...this.getInputThreadOptions(),
            ];
        }
        getFilterOptions(videoStream) {
            const options = [];
            const tonemapOptions = this.getToneMapping(videoStream);
            if (tonemapOptions.length === 0 && !videoStream.pixelFormat.endsWith('420p')) {
                options.push(`scale_vaapi=${this.getScaling(videoStream)}:mode=hq:out_range=pc:format=nv12`);
            }
            else if (this.shouldScale(videoStream)) {
                options.push(`scale_vaapi=${this.getScaling(videoStream)}:mode=hq:out_range=pc`);
            }
            options.push(...tonemapOptions);
            return options;
        }
        getToneMapping(videoStream) {
            if (!this.shouldToneMap(videoStream)) {
                return [];
            }
            const { matrix, primaries, transfer } = this.getColors();
            const tonemapOptions = [
                'desat=0',
                'format=nv12',
                `matrix=${matrix}`,
                `primaries=${primaries}`,
                `transfer=${transfer}`,
                'range=pc',
                `tonemap=${this.config.tonemap}`,
                'tonemap_mode=lum',
                'peak=100',
            ];
            return [
                'hwmap=derive_device=opencl',
                `tonemap_opencl=${tonemapOptions.join(':')}`,
                'hwmap=derive_device=vaapi:reverse=1,format=vaapi',
            ];
        }
        getInputThreadOptions() {
            return ['-threads', '1'];
        }
    }
    exports.VaapiHwDecodeConfig = VaapiHwDecodeConfig;
    class RkmppSwDecodeConfig extends BaseHWConfig {
        eligibleForTwoPass() {
            return false;
        }
        getBaseInputOptions() {
            return [];
        }
        getPresetOptions() {
            switch (this.config.targetVideoCodec) {
                case enum_1.VideoCodec.H264: {
                    return ['-level', '51'];
                }
                case enum_1.VideoCodec.Hevc: {
                    return ['-level', '153'];
                }
                default: {
                    throw new Error(`Incompatible video codec for RKMPP: ${this.config.targetVideoCodec}`);
                }
            }
        }
        getBitrateOptions() {
            const bitrate = this.getMaxBitrateValue();
            if (bitrate > 0) {
                return ['-rc_mode', 'AVBR', '-b:v', `${bitrate}${this.getBitrateUnit()}`];
            }
            return ['-rc_mode', 'CQP', '-qp_init', String(this.config.crf)];
        }
        getVideoCodec() {
            return `${this.config.targetVideoCodec}_rkmpp`;
        }
    }
    exports.RkmppSwDecodeConfig = RkmppSwDecodeConfig;
    const hasFrameCrop = (videoStream) => [videoStream?.cropTop, videoStream?.cropBottom, videoStream?.cropLeft, videoStream?.cropRight].some((value) => value !== null && value !== undefined && value > 0);
    class RkmppHwDecodeConfig extends RkmppSwDecodeConfig {
        getBaseInputOptions(videoStream) {
            const options = ['-hwaccel', 'rkmpp', '-hwaccel_output_format', 'drm_prime', '-afbc', 'rga', '-noautorotate'];
            return videoStream !== undefined && hasFrameCrop(videoStream) ? ['-apply_cropping', 'codec', ...options] : options;
        }
        getFilterOptions(videoStream) {
            if (this.shouldToneMap(videoStream)) {
                const { primaries, transfer, matrix } = this.getColors();
                if (this.interfaces.mali) {
                    return [
                        `scale_rkrga=${this.getScaling(videoStream)}:format=p010:afbc=1:async_depth=4`,
                        'hwmap=derive_device=opencl:mode=read',
                        `tonemap_opencl=format=nv12:r=tv:p=${primaries}:t=${transfer}:m=${matrix}:tonemap=${this.config.tonemap}:desat=0:apply_dovi=0`,
                        'hwmap=derive_device=rkmpp:mode=write:reverse=1',
                        'format=drm_prime',
                    ];
                }
                return [
                    `scale_rkrga=${this.getScaling(videoStream)}:format=p010:afbc=1:async_depth=4`,
                    'hwdownload',
                    'format=p010',
                    `tonemapx=tonemap=${this.config.tonemap}:desat=0:p=${primaries}:t=${transfer}:m=${matrix}:r=tv:format=yuv420p`,
                    'hwupload',
                ];
            }
            if (this.shouldScale(videoStream)) {
                if (hasFrameCrop(videoStream)) {
                    const top = videoStream.cropTop ?? 0;
                    const bottom = videoStream.cropBottom ?? 0;
                    const left = videoStream.cropLeft ?? 0;
                    const right = videoStream.cropRight ?? 0;
                    const width = videoStream.width - left - right;
                    const height = videoStream.height - top - bottom;
                    const scaling = this.getScaling({ ...videoStream, width, height }).replace(':', ':h=');
                    return [
                        `vpp_rkrga=cw=${width}:ch=${height}:cx=${left}:cy=${top}:w=${scaling}:format=nv12:afbc=1:async_depth=4`,
                    ];
                }
                return [`scale_rkrga=${this.getScaling(videoStream)}:format=nv12:afbc=1:async_depth=4`];
            }
            if (hasFrameCrop(videoStream)) {
                const top = videoStream.cropTop ?? 0;
                const bottom = videoStream.cropBottom ?? 0;
                const left = videoStream.cropLeft ?? 0;
                const right = videoStream.cropRight ?? 0;
                const width = videoStream.width - left - right;
                const height = videoStream.height - top - bottom;
                return [
                    `vpp_rkrga=cw=${width}:ch=${height}:cx=${left}:cy=${top}:w=${width}:h=${height}:format=nv12:afbc=1:async_depth=4`,
                ];
            }
            return [];
        }
    }
    exports.RkmppHwDecodeConfig = RkmppHwDecodeConfig;
    //# sourceMappingURL=media.js.map
  database.js: |
    "use strict";
    Object.defineProperty(exports, "__esModule", { value: true });
    exports.updateLockedColumns = exports.searchStatisticsV3Examples = exports.searchSmartV3Examples = exports.searchRandomV3Examples = exports.searchMetadataV3Examples = exports.tokenizeForSearch = exports.dummy = exports.isVideoStreamSessionPkConstraint = exports.isAssetChecksumConstraint = exports.VIDEO_STREAM_SESSION_PK_CONSTRAINT = exports.ASSET_CHECKSUM_CONSTRAINT = exports.removeUndefinedKeys = exports.unnest = exports.anyUuid = exports.asUuid = exports.getKyselyConfig = void 0;
    exports.withDefaultVisibility = withDefaultVisibility;
    exports.withExif = withExif;
    exports.withExifInner = withExifInner;
    exports.withAudioStream = withAudioStream;
    exports.withVideoStream = withVideoStream;
    exports.withVideoFormat = withVideoFormat;
    exports.withVideoPackets = withVideoPackets;
    exports.withSmartSearch = withSmartSearch;
    exports.withFaces = withFaces;
    exports.withFiles = withFiles;
    exports.withFilePath = withFilePath;
    exports.withFacesAndPeople = withFacesAndPeople;
    exports.hasPeople = hasPeople;
    exports.inSharedAlbum = inSharedAlbum;
    exports.inAlbums = inAlbums;
    exports.hasTags = hasTags;
    exports.withOwner = withOwner;
    exports.withLibrary = withLibrary;
    exports.withTags = withTags;
    exports.truncatedDate = truncatedDate;
    exports.withTagId = withTagId;
    exports.withEdits = withEdits;
    exports.searchAssetBuilderLegacy = searchAssetBuilderLegacy;
    exports.searchAssetBuilder = searchAssetBuilder;
    exports.withSearchOrder = withSearchOrder;
    exports.vectorIndexQuery = vectorIndexQuery;
    const sql_tools_1 = require("@immich/sql-tools");
    const kysely_1 = require("kysely");
    const kysely_postgres_js_1 = require("kysely-postgres-js");
    const postgres_1 = require("kysely/helpers/postgres");
    const database_1 = require("../database");
    const decorators_1 = require("../decorators");
    const search_dto_1 = require("../dtos/search.dto");
    const enum_1 = require("../enum");
    const request_1 = require("./request");
    const getKyselyConfig = (connection) => {
        return {
            dialect: new kysely_postgres_js_1.PostgresJSDialect({
                postgres: (0, sql_tools_1.createPostgres)({
                    connection,
                    onNotice: (notice) => {
                        if (notice['severity'] !== 'NOTICE') {
                            console.warn('Postgres notice:', notice);
                        }
                    },
                }),
            }),
            log(event) {
                if (event.level !== 'error') {
                    return;
                }
                if ((0, exports.isAssetChecksumConstraint)(event.error)) {
                    return;
                }
                console.error('Query failed :', {
                    durationMs: event.queryDurationMillis,
                    error: event.error,
                    sql: event.query.sql,
                    params: event.query.parameters,
                });
            },
        };
    };
    exports.getKyselyConfig = getKyselyConfig;
    const uniqueIds = (ids) => [...new Set(ids)];
    const asUuid = (id) => (0, kysely_1.sql) `${id}::uuid`;
    exports.asUuid = asUuid;
    const anyUuid = (ids) => (0, kysely_1.sql) `any(${`{${ids}}`}::uuid[])`;
    exports.anyUuid = anyUuid;
    const unnest = (array) => (0, kysely_1.sql) `unnest(array[${kysely_1.sql.join(array)}]::text[])`;
    exports.unnest = unnest;
    const removeUndefinedKeys = (update, template) => {
        for (const key in update) {
            if (template[key] === undefined) {
                delete update[key];
            }
        }
        return update;
    };
    exports.removeUndefinedKeys = removeUndefinedKeys;
    exports.ASSET_CHECKSUM_CONSTRAINT = 'UQ_assets_owner_checksum';
    exports.VIDEO_STREAM_SESSION_PK_CONSTRAINT = 'video_stream_session_pkey';
    const isAssetChecksumConstraint = (error) => error?.constraint_name === exports.ASSET_CHECKSUM_CONSTRAINT;
    exports.isAssetChecksumConstraint = isAssetChecksumConstraint;
    const isVideoStreamSessionPkConstraint = (error) => error?.constraint_name === exports.VIDEO_STREAM_SESSION_PK_CONSTRAINT;
    exports.isVideoStreamSessionPkConstraint = isVideoStreamSessionPkConstraint;
    function withDefaultVisibility(qb) {
        return qb.where('asset.visibility', 'in', [kysely_1.sql.lit(enum_1.AssetVisibility.Archive), kysely_1.sql.lit(enum_1.AssetVisibility.Timeline)]);
    }
    const selectExifInfo = (eb) => eb.fn
        .toJson(eb.table('asset_exif'))
        .$castTo()
        .as('exifInfo');
    function withExif(qb) {
        return qb.leftJoin('asset_exif', 'asset.id', 'asset_exif.assetId').select(selectExifInfo);
    }
    function withExifInner(qb) {
        return qb
            .innerJoin('asset_exif', 'asset.id', 'asset_exif.assetId')
            .select((eb) => eb.fn.toJson(eb.table('asset_exif')).as('exifInfo'))
            .$narrowType();
    }
    exports.dummy = (0, kysely_1.sql) `(select 1)`.as('dummy');
    function withAudioStream(eb) {
        return (0, postgres_1.jsonObjectFrom)(eb
            .selectFrom(exports.dummy)
            .select(['asset_audio.index', 'asset_audio.codecName', 'asset_audio.profile', 'asset_audio.bitrate'])
            .where('asset_audio.assetId', 'is not', kysely_1.sql.lit(null))
            .$castTo());
    }
    function withVideoStream(eb) {
        return (0, postgres_1.jsonObjectFrom)(eb
            .selectFrom(exports.dummy)
            .select((eb) => [
            'asset_video.index',
            'asset_video.codecName',
            'asset_video.profile',
            'asset_video.level',
            'asset_video.bitrate',
            'asset_exif.exifImageWidth as width',
            'asset_exif.exifImageHeight as height',
            'asset_video.pixelFormat',
            'asset_video.frameCount',
            'asset_exif.fps as frameRate',
            'asset_video.timeBase',
            eb
                .case()
                .when('asset_exif.orientation', '=', kysely_1.sql.lit(enum_1.ExifOrientation.Rotate90CW.toString()))
                .then(kysely_1.sql.lit(-90))
                .when('asset_exif.orientation', '=', kysely_1.sql.lit(enum_1.ExifOrientation.Rotate270CW.toString()))
                .then(kysely_1.sql.lit(90))
                .when('asset_exif.orientation', '=', kysely_1.sql.lit(enum_1.ExifOrientation.Rotate180.toString()))
                .then(kysely_1.sql.lit(180))
                .else(0)
                .end()
                .as('rotation'),
            'asset_video.colorPrimaries',
            'asset_video.colorMatrix',
            'asset_video.colorTransfer',
            'asset_video.dvProfile',
            'asset_video.dvLevel',
            'asset_video.dvBlSignalCompatibilityId',
            'asset_video.cropTop',
            'asset_video.cropBottom',
            'asset_video.cropLeft',
            'asset_video.cropRight',
        ])
            .where('asset_video.assetId', 'is not', kysely_1.sql.lit(null))).$castTo();
    }
    function withVideoFormat(eb) {
        return (0, postgres_1.jsonObjectFrom)(eb
            .selectFrom(exports.dummy)
            .select(['asset_video.formatName', 'asset_video.formatLongName', 'asset.duration', 'asset_video.bitrate'])
            .where('asset_video.assetId', 'is not', kysely_1.sql.lit(null))).$castTo();
    }
    function withVideoPackets(eb) {
        return (0, postgres_1.jsonObjectFrom)(eb
            .selectFrom(exports.dummy)
            .where('asset_keyframe.assetId', 'is not', kysely_1.sql.lit(null))
            .select([
            'asset_keyframe.pts as keyframePts',
            'asset_keyframe.accDuration as keyframeAccDuration',
            'asset_keyframe.ownDuration as keyframeOwnDuration',
            'asset_keyframe.totalDuration',
            'asset_keyframe.packetCount',
            'asset_keyframe.outputFrames',
        ])).$castTo();
    }
    function withSmartSearch(qb) {
        return qb
            .leftJoin('smart_search', 'asset.id', 'smart_search.assetId')
            .select((eb) => (0, postgres_1.jsonObjectFrom)(eb.table('smart_search')).as('smartSearch'));
    }
    function withFaces(eb, withHidden, withDeletedFace) {
        return (0, postgres_1.jsonArrayFrom)(eb
            .selectFrom('asset_face')
            .selectAll('asset_face')
            .whereRef('asset_face.assetId', '=', 'asset.id')
            .$if(!withDeletedFace, (qb) => qb.where('asset_face.deletedAt', 'is', null))
            .$if(!withHidden, (qb) => qb.where('asset_face.isVisible', '=', true))).as('faces');
    }
    function withFiles(eb, type) {
        return (0, postgres_1.jsonArrayFrom)(eb
            .selectFrom('asset_file')
            .select(database_1.columns.assetFiles)
            .whereRef('asset_file.assetId', '=', 'asset.id')
            .$if(!!type, (qb) => qb.where('asset_file.type', '=', type))).as('files');
    }
    function withFilePath(eb, type, isEdited = false) {
        return eb
            .selectFrom('asset_file')
            .select('asset_file.path')
            .whereRef('asset_file.assetId', '=', 'asset.id')
            .where('asset_file.type', '=', kysely_1.sql.lit(type))
            .where('asset_file.isEdited', '=', kysely_1.sql.lit(isEdited));
    }
    function withFacesAndPeople({ viewingUserId, withHidden, withDeletedFace }) {
        return (eb) => (0, postgres_1.jsonArrayFrom)(eb
            .selectFrom('asset_face')
            .leftJoinLateral((eb) => eb
            .selectFrom('person')
            .selectAll('person')
            .whereRef('person.personGroupId', '=', 'asset_face.personGroupId')
            .$if(!viewingUserId, (qb) => qb.whereRef('person.ownerId', '=', 'asset.ownerId'))
            .$if(!!viewingUserId, (qb) => qb.where('person.ownerId', '=', viewingUserId))
            .as('person'), (join) => join.onTrue())
            .selectAll('asset_face')
            .select((eb) => eb.table('person').$castTo().as('person'))
            .whereRef('asset_face.assetId', '=', 'asset.id')
            .$if(!withDeletedFace, (qb) => qb.where('asset_face.deletedAt', 'is', null))
            .$if(!withHidden, (qb) => qb.where('asset_face.isVisible', 'is', true))).as('faces');
    }
    function hasPeople(qb, personGroupIds) {
        return qb.innerJoin((eb) => eb
            .selectFrom('asset_face')
            .select('assetId')
            .where('personGroupId', '=', (0, exports.anyUuid)(personGroupIds))
            .where('deletedAt', 'is', null)
            .where('isVisible', 'is', true)
            .groupBy('assetId')
            .having((eb) => eb.fn.count('personGroupId').distinct(), '=', personGroupIds.length)
            .as('has_people'), (join) => join.onRef('has_people.assetId', '=', 'asset.id'));
    }
    function inSharedAlbum(eb, userId) {
        return eb.exists(eb
            .selectFrom('album_asset')
            .select(kysely_1.sql.lit(1).as('exists'))
            .innerJoin('album', (join) => join.onRef('album.id', '=', 'album_asset.albumId').on('album.deletedAt', 'is', null))
            .innerJoin('album_user', (join) => join.onRef('album_user.albumId', '=', 'album.id').on('album_user.userId', '=', (0, exports.asUuid)(userId)))
            .whereRef('album_asset.assetId', '=', 'asset.id'));
    }
    function inAlbums(qb, albumIds) {
        return qb.innerJoin((eb) => eb
            .selectFrom('album_asset')
            .select('assetId')
            .where('albumId', '=', (0, exports.anyUuid)(albumIds))
            .groupBy('assetId')
            .having((eb) => eb.fn.count('albumId').distinct(), '=', albumIds.length)
            .as('has_album'), (join) => join.onRef('has_album.assetId', '=', 'asset.id'));
    }
    function hasTags(qb, tagIds) {
        return qb.innerJoin((eb) => eb
            .selectFrom('tag_asset')
            .select('assetId')
            .innerJoin('tag_closure', 'tag_asset.tagId', 'tag_closure.id_descendant')
            .where('tag_closure.id_ancestor', '=', (0, exports.anyUuid)(tagIds))
            .groupBy('assetId')
            .having((eb) => eb.fn.count('tag_closure.id_ancestor').distinct(), '>=', tagIds.length)
            .as('has_tags'), (join) => join.onRef('has_tags.assetId', '=', 'asset.id'));
    }
    function withOwner(eb) {
        return (0, postgres_1.jsonObjectFrom)(eb.selectFrom('user').select(database_1.columns.user).whereRef('user.id', '=', 'asset.ownerId')).as('owner');
    }
    function withLibrary(eb) {
        return (0, postgres_1.jsonObjectFrom)(eb.selectFrom('library').selectAll('library').whereRef('library.id', '=', 'asset.libraryId')).as('library');
    }
    function withTags(eb) {
        return (0, postgres_1.jsonArrayFrom)(eb
            .selectFrom('tag')
            .select(database_1.columns.tag)
            .innerJoin('tag_asset', 'tag.id', 'tag_asset.tagId')
            .whereRef('asset.id', '=', 'tag_asset.assetId')).as('tags');
    }
    function truncatedDate(order = enum_1.AssetOrderBy.TakenAt, size) {
        return (0, kysely_1.sql) `date_trunc(${kysely_1.sql.lit(size ?? 'MONTH')}, ${kysely_1.sql.ref(order === enum_1.AssetOrderBy.CreatedAt ? 'asset.createdAt' : 'localDateTime')} AT TIME ZONE 'UTC') AT TIME ZONE 'UTC'`;
    }
    function withTagId(qb, tagId) {
        return qb.where((eb) => eb.exists(eb
            .selectFrom('tag_closure')
            .innerJoin('tag_asset', 'tag_asset.tagId', 'tag_closure.id_descendant')
            .whereRef('tag_asset.assetId', '=', 'asset.id')
            .where('tag_closure.id_ancestor', '=', tagId)));
    }
    const isCJK = (c) => (c >= 0x4e_00 && c <= 0x9f_ff) ||
        (c >= 0xac_00 && c <= 0xd7_af) ||
        (c >= 0x30_40 && c <= 0x30_9f) ||
        (c >= 0x30_a0 && c <= 0x30_ff) ||
        (c >= 0x34_00 && c <= 0x4d_bf);
    const tokenizeForSearch = (text) => {
        const tokens = [];
        let i = 0;
        while (i < text.length) {
            const c = text.charCodeAt(i);
            if (c <= 32) {
                i++;
                continue;
            }
            const start = i;
            if (isCJK(c)) {
                while (i < text.length && isCJK(text.charCodeAt(i))) {
                    i++;
                }
                if (i - start === 1) {
                    tokens.push(text[start]);
                }
                else {
                    for (let k = start; k < i - 1; k++) {
                        tokens.push(text[k] + text[k + 1]);
                    }
                }
            }
            else {
                while (i < text.length && text.charCodeAt(i) > 32 && !isCJK(text.charCodeAt(i))) {
                    i++;
                }
                tokens.push(text.slice(start, i));
            }
        }
        return tokens;
    };
    exports.tokenizeForSearch = tokenizeForSearch;
    function withEdits(eb) {
        return (0, postgres_1.jsonArrayFrom)(eb
            .selectFrom('asset_edit')
            .select(['asset_edit.action', 'asset_edit.parameters'])
            .whereRef('asset_edit.assetId', '=', 'asset.id')).as('edits');
    }
    const joinDeduplicationPlugin = new kysely_1.DeduplicateJoinsPlugin();
    function searchAssetBuilderLegacy(kysely, options) {
        options.withDeleted ||= !!(options.trashedAfter || options.trashedBefore || options.isOffline);
        return kysely
            .withPlugin(joinDeduplicationPlugin)
            .selectFrom('asset')
            .$if(!!options.visibility, (qb) => options.visibility === 'not-locked'
            ? qb.where('asset.visibility', '!=', enum_1.AssetVisibility.Locked)
            : qb.where('asset.visibility', '=', options.visibility))
            .$if(!!options.albumIds && options.albumIds.length > 0, (qb) => inAlbums(qb, options.albumIds))
            .$if(!!options.tagIds && options.tagIds.length > 0, (qb) => hasTags(qb, options.tagIds))
            .$if(options.tagIds === null, (qb) => qb.where((eb) => eb.not(eb.exists((eb) => eb.selectFrom('tag_asset').whereRef('assetId', '=', 'asset.id')))))
            .$if(!!options.personIds && options.personIds.length > 0, (qb) => hasPeople(qb, options.personIds))
            .$if(!!options.createdBefore, (qb) => qb.where('asset.createdAt', '<=', options.createdBefore))
            .$if(!!options.createdAfter, (qb) => qb.where('asset.createdAt', '>=', options.createdAfter))
            .$if(!!options.updatedBefore, (qb) => qb.where('asset.updatedAt', '<=', options.updatedBefore))
            .$if(!!options.updatedAfter, (qb) => qb.where('asset.updatedAt', '>=', options.updatedAfter))
            .$if(!!options.trashedBefore, (qb) => qb.where('asset.deletedAt', '<=', options.trashedBefore))
            .$if(!!options.trashedAfter, (qb) => qb.where('asset.deletedAt', '>=', options.trashedAfter))
            .$if(!!options.takenBefore, (qb) => qb.where('asset.fileCreatedAt', '<=', options.takenBefore))
            .$if(!!options.takenAfter, (qb) => qb.where('asset.fileCreatedAt', '>=', options.takenAfter))
            .$if(options.city !== undefined, (qb) => qb
            .innerJoin('asset_exif', 'asset.id', 'asset_exif.assetId')
            .where('asset_exif.city', options.city === null ? 'is' : '=', options.city))
            .$if(options.state !== undefined, (qb) => qb
            .innerJoin('asset_exif', 'asset.id', 'asset_exif.assetId')
            .where('asset_exif.state', options.state === null ? 'is' : '=', options.state))
            .$if(options.country !== undefined, (qb) => qb
            .innerJoin('asset_exif', 'asset.id', 'asset_exif.assetId')
            .where('asset_exif.country', options.country === null ? 'is' : '=', options.country))
            .$if(options.make !== undefined, (qb) => qb
            .innerJoin('asset_exif', 'asset.id', 'asset_exif.assetId')
            .where('asset_exif.make', options.make === null ? 'is' : '=', options.make))
            .$if(options.model !== undefined, (qb) => qb
            .innerJoin('asset_exif', 'asset.id', 'asset_exif.assetId')
            .where('asset_exif.model', options.model === null ? 'is' : '=', options.model))
            .$if(options.lensModel !== undefined, (qb) => qb
            .innerJoin('asset_exif', 'asset.id', 'asset_exif.assetId')
            .where('asset_exif.lensModel', options.lensModel === null ? 'is' : '=', options.lensModel))
            .$if(options.rating !== undefined, (qb) => qb
            .innerJoin('asset_exif', 'asset.id', 'asset_exif.assetId')
            .where('asset_exif.rating', options.rating === null ? 'is' : '=', options.rating))
            .$if(!!options.checksum, (qb) => qb.where('asset.checksum', '=', options.checksum))
            .$if(!!options.id, (qb) => qb.where('asset.id', '=', (0, exports.asUuid)(options.id)))
            .$if(!!options.libraryId, (qb) => qb.where('asset.libraryId', '=', (0, exports.asUuid)(options.libraryId)))
            .$if(!!options.userIds, (qb) => qb.where('asset.ownerId', '=', (0, exports.anyUuid)(options.userIds)))
            .$if(!!options.encodedVideoPath, (qb) => qb
            .innerJoin('asset_file', (join) => join
            .onRef('asset.id', '=', 'asset_file.assetId')
            .on('asset_file.type', '=', enum_1.AssetFileType.EncodedVideo)
            .on('asset_file.isEdited', '=', false))
            .where('asset_file.path', '=', options.encodedVideoPath))
            .$if(!!options.originalPath, (qb) => qb.where((0, kysely_1.sql) `f_unaccent(asset."originalPath")`, 'ilike', (0, kysely_1.sql) `'%' || f_unaccent(${options.originalPath}) || '%'`))
            .$if(!!options.originalFileName, (qb) => qb.where((0, kysely_1.sql) `f_unaccent(asset."originalFileName")`, 'ilike', (0, kysely_1.sql) `'%' || f_unaccent(${options.originalFileName}) || '%'`))
            .$if(!!options.description, (qb) => qb
            .innerJoin('asset_exif', 'asset.id', 'asset_exif.assetId')
            .where((0, kysely_1.sql) `f_unaccent(asset_exif.description)`, 'ilike', (0, kysely_1.sql) `'%' || f_unaccent(${options.description}) || '%'`))
            .$if(!!options.ocr, (qb) => qb
            .innerJoin('ocr_search', 'asset.id', 'ocr_search.assetId')
            .where(() => (0, kysely_1.sql) `f_unaccent(ocr_search.text) %>> f_unaccent(${(0, exports.tokenizeForSearch)(options.ocr).join(' ')})`))
            .$if(!!options.type, (qb) => qb.where('asset.type', '=', options.type))
            .$if(options.isFavorite !== undefined, (qb) => qb.where('asset.isFavorite', '=', options.isFavorite))
            .$if(options.isOffline !== undefined, (qb) => qb.where('asset.isOffline', '=', options.isOffline))
            .$if(options.isEncoded !== undefined, (qb) => qb.where((eb) => {
            const exists = eb.exists((eb) => eb
                .selectFrom('asset_file')
                .whereRef('assetId', '=', 'asset.id')
                .where('type', '=', enum_1.AssetFileType.EncodedVideo));
            return options.isEncoded ? exists : eb.not(exists);
        }))
            .$if(options.isMotion !== undefined, (qb) => qb.where('asset.livePhotoVideoId', options.isMotion ? 'is not' : 'is', null))
            .$if(!!options.isNotInAlbum && (!options.albumIds || options.albumIds.length === 0), (qb) => qb.where((eb) => eb.not(eb.exists((eb) => eb.selectFrom('album_asset').whereRef('assetId', '=', 'asset.id')))))
            .$if(options.withStacked === false, (qb) => qb.where('asset.stackId', 'is', null))
            .$if(!!options.withExif, withExifInner)
            .$if(!!(options.withFaces || options.withPeople), (qb) => qb.select(withFacesAndPeople({ viewingUserId: options.viewingUserId })))
            .$if(!options.withDeleted, (qb) => qb.where('asset.deletedAt', 'is', null));
    }
    const albumAssets = (eb) => eb.selectFrom('album_asset').whereRef('album_asset.assetId', '=', 'asset.id');
    const visibleFaces = (eb) => eb
        .selectFrom('asset_face')
        .whereRef('asset_face.assetId', '=', 'asset.id')
        .where('asset_face.deletedAt', 'is', null)
        .where('asset_face.isVisible', '=', true);
    const tagAssets = (eb) => eb.selectFrom('tag_asset').whereRef('tag_asset.assetId', '=', 'asset.id');
    function idsPredicates(eb, { any, all, none } = {}, ops) {
        const predicates = [];
        if (any) {
            predicates.push(ops.matchesAny(any));
        }
        if (all) {
            const ids = uniqueIds(all);
            predicates.push(ids.length === 1 ? ops.matchesAny(ids) : ops.matchesAll(ids));
        }
        if (none) {
            predicates.push(eb.not(ops.matchesAny(none)));
        }
        return predicates;
    }
    function albumIdsPredicates(eb, filter) {
        const matching = (ids) => albumAssets(eb).where('album_asset.albumId', '=', (0, exports.anyUuid)(ids));
        return idsPredicates(eb, filter, {
            matchesAny: (ids) => eb.exists(matching(ids)),
            matchesAll: (ids) => eb.exists(matching(ids)
                .select('album_asset.assetId')
                .groupBy('album_asset.assetId')
                .having((eb) => eb.fn.count('album_asset.albumId').distinct(), '=', ids.length)),
        });
    }
    function personIdsPredicates(eb, filter) {
        const matching = (ids) => visibleFaces(eb).where('asset_face.personGroupId', '=', (0, exports.anyUuid)(ids));
        return idsPredicates(eb, filter, {
            matchesAny: (ids) => eb.exists(matching(ids)),
            matchesAll: (ids) => eb.exists(matching(ids)
                .select('asset_face.assetId')
                .groupBy('asset_face.assetId')
                .having((eb) => eb.fn.count('asset_face.personGroupId').distinct(), '=', ids.length)),
        });
    }
    function tagIdsPredicates(eb, filter) {
        const matching = (ids) => tagAssets(eb)
            .innerJoin('tag_closure', 'tag_asset.tagId', 'tag_closure.id_descendant')
            .where('tag_closure.id_ancestor', '=', (0, exports.anyUuid)(ids));
        return idsPredicates(eb, filter, {
            matchesAny: (ids) => eb.exists(matching(ids)),
            matchesAll: (ids) => eb.exists(matching(ids)
                .select('tag_asset.assetId')
                .groupBy('tag_asset.assetId')
                .having((eb) => eb.fn.count('tag_closure.id_ancestor').distinct(), '=', ids.length)),
        });
    }
    function comparisonPredicates(eb, column, filter = {}) {
        const predicates = [];
        if (filter.eq !== undefined) {
            predicates.push(filter.eq === null ? eb(column, 'is', null) : eb(column, '=', filter.eq));
        }
        if (filter.ne !== undefined) {
            predicates.push(filter.ne === null ? eb(column, 'is not', null) : eb(column, '!=', filter.ne));
        }
        if (filter.lt !== undefined) {
            predicates.push(eb(column, '<', filter.lt));
        }
        if (filter.lte !== undefined) {
            predicates.push(eb(column, '<=', filter.lte));
        }
        if (filter.gt !== undefined) {
            predicates.push(eb(column, '>', filter.gt));
        }
        if (filter.gte !== undefined) {
            predicates.push(eb(column, '>=', filter.gte));
        }
        if (filter.in !== undefined) {
            predicates.push(eb(column, 'in', filter.in));
        }
        if (filter.notIn !== undefined) {
            predicates.push(eb(column, 'not in', filter.notIn));
        }
        return predicates;
    }
    function stringPatternPredicates(eb, column, filter = {}) {
        const ref = kysely_1.sql.ref(column);
        const predicates = comparisonPredicates(eb, column, filter);
        if (filter.like !== undefined) {
            predicates.push((0, kysely_1.sql) `f_unaccent(${ref}) ilike ('%' || f_unaccent(${filter.like}) || '%')`);
        }
        if (filter.notLike !== undefined) {
            predicates.push((0, kysely_1.sql) `f_unaccent(${ref}) not ilike ('%' || f_unaccent(${filter.notLike}) || '%')`);
        }
        if (filter.startsWith !== undefined) {
            predicates.push((0, kysely_1.sql) `f_unaccent(${ref}) ilike (f_unaccent(${filter.startsWith}) || '%')`);
        }
        if (filter.endsWith !== undefined) {
            predicates.push((0, kysely_1.sql) `f_unaccent(${ref}) ilike ('%' || f_unaccent(${filter.endsWith}))`);
        }
        return predicates;
    }
    function checksumPredicates(eb, filter = {}) {
        return comparisonPredicates(eb, 'asset.checksum', {
            eq: filter.eq === undefined ? undefined : (0, request_1.fromChecksum)(filter.eq),
            ne: filter.ne === undefined ? undefined : (0, request_1.fromChecksum)(filter.ne),
            in: filter.in?.map((checksum) => (0, request_1.fromChecksum)(checksum)),
            notIn: filter.notIn?.map((checksum) => (0, request_1.fromChecksum)(checksum)),
        });
    }
    const encodedVideoFiles = (eb) => eb
        .selectFrom('asset_file')
        .whereRef('asset_file.assetId', '=', 'asset.id')
        .where('asset_file.type', '=', enum_1.AssetFileType.EncodedVideo);
    function existsPredicates(eb, filter, subquery) {
        if (!filter) {
            return [];
        }
        const exists = eb.exists(subquery());
        return [filter.eq ? exists : eb.not(exists)];
    }
    function branchPredicates(eb, branch) {
        const { encodedVideoPath } = branch;
        return [
            ...comparisonPredicates(eb, 'asset.id', branch.id),
            ...comparisonPredicates(eb, 'asset.libraryId', branch.libraryId),
            ...comparisonPredicates(eb, 'asset.type', branch.type),
            ...comparisonPredicates(eb, 'asset.visibility', branch.visibility),
            ...(branch.isFavorite ? [eb('asset.isFavorite', '=', branch.isFavorite.eq)] : []),
            ...(branch.isOffline ? [eb('asset.isOffline', '=', branch.isOffline.eq)] : []),
            ...(branch.isMotion ? [eb('asset.livePhotoVideoId', branch.isMotion.eq ? 'is not' : 'is', null)] : []),
            ...existsPredicates(eb, branch.isEncoded, () => encodedVideoFiles(eb)),
            ...existsPredicates(eb, branch.hasAlbums, () => albumAssets(eb)),
            ...existsPredicates(eb, branch.hasPeople, () => visibleFaces(eb)),
            ...existsPredicates(eb, branch.hasTags, () => tagAssets(eb)),
            ...comparisonPredicates(eb, 'asset_exif.city', branch.city),
            ...comparisonPredicates(eb, 'asset_exif.state', branch.state),
            ...comparisonPredicates(eb, 'asset_exif.country', branch.country),
            ...comparisonPredicates(eb, 'asset_exif.make', branch.make),
            ...comparisonPredicates(eb, 'asset_exif.model', branch.model),
            ...comparisonPredicates(eb, 'asset_exif.lensModel', branch.lensModel),
            ...stringPatternPredicates(eb, 'asset_exif.description', branch.description),
            ...stringPatternPredicates(eb, 'asset.originalFileName', branch.originalFileName),
            ...stringPatternPredicates(eb, 'asset.originalPath', branch.originalPath),
            ...(branch.ocr
                ? [
                    eb.exists(eb
                        .selectFrom('ocr_search')
                        .whereRef('ocr_search.assetId', '=', 'asset.id')
                        .where((0, kysely_1.sql) `f_unaccent(ocr_search.text) %>> f_unaccent(${(0, exports.tokenizeForSearch)(branch.ocr.matches).join(' ')})`)),
                ]
                : []),
            ...comparisonPredicates(eb, 'asset_exif.rating', branch.rating),
            ...comparisonPredicates(eb, 'asset_exif.fileSizeInByte', branch.fileSizeInBytes),
            ...comparisonPredicates(eb, 'asset.fileCreatedAt', branch.takenAt),
            ...comparisonPredicates(eb, 'asset.createdAt', branch.createdAt),
            ...comparisonPredicates(eb, 'asset.updatedAt', branch.updatedAt),
            ...comparisonPredicates(eb, 'asset.deletedAt', branch.trashedAt),
            ...albumIdsPredicates(eb, branch.albumIds),
            ...personIdsPredicates(eb, branch.personIds),
            ...tagIdsPredicates(eb, branch.tagIds),
            ...checksumPredicates(eb, branch.checksum),
            ...(encodedVideoPath
                ? [
                    eb.exists(encodedVideoFiles(eb)
                        .where('asset_file.isEdited', '=', false)
                        .where((eb) => eb.and(comparisonPredicates(eb, 'asset_file.path', encodedVideoPath)))),
                ]
                : []),
        ];
    }
    function searchAssetBuilder(kysely, options, scope) {
        const filter = options.filter ?? {};
        const branches = filter.or ?? [];
        const ownershipPredicate = (eb) => eb('asset.ownerId', '=', (0, exports.anyUuid)(scope.userIds));
        const topConfined = (0, search_dto_1.isAlbumConfined)(filter);
        const anyBranchConfined = branches.some((branch) => (0, search_dto_1.isAlbumConfined)(branch));
        const scopePerBranch = !topConfined && anyBranchConfined;
        const scopeGlobally = !topConfined && !anyBranchConfined;
        return (kysely
            .withPlugin(joinDeduplicationPlugin)
            .selectFrom('asset')
            .leftJoin('asset_exif', 'asset.id', 'asset_exif.assetId')
            .$if(!!options.withExif, (qb) => qb.select(selectExifInfo))
            .$if(scopeGlobally, (qb) => qb.where(ownershipPredicate))
            .where((eb) => eb.or([eb('asset.visibility', '!=', enum_1.AssetVisibility.Locked), eb('asset.ownerId', '=', scope.lockedOwnerId)]))
            .$if(!!(options.withFaces || options.withPeople), (qb) => qb.select(withFacesAndPeople({ viewingUserId: scope.viewingUserId })))
            .$if(options.withStacked === false, (qb) => qb.where('asset.stackId', 'is', null))
            .where((eb) => {
            const predicates = branchPredicates(eb, filter);
            if (branches.length > 0) {
                predicates.push(eb.or(branches.map((branch) => eb.and([
                    ...branchPredicates(eb, branch),
                    ...(scopePerBranch && !(0, search_dto_1.isAlbumConfined)(branch) ? [ownershipPredicate(eb)] : []),
                ]))));
            }
            return predicates.length > 0 ? eb.and(predicates) : eb.lit(true);
        }));
    }
    const searchOrderColumns = {
        [enum_1.SearchOrderField.FileCreatedAt]: { column: 'asset.fileCreatedAt', nullable: false },
        [enum_1.SearchOrderField.LocalDateTime]: { column: 'asset.localDateTime', nullable: false },
        [enum_1.SearchOrderField.FileSizeInBytes]: { column: 'asset_exif.fileSizeInByte', nullable: true },
        [enum_1.SearchOrderField.Rating]: { column: 'asset_exif.rating', nullable: true },
    };
    function withSearchOrder(qb, order) {
        const { field, direction } = order ?? search_dto_1.DEFAULT_SEARCH_ORDER;
        const { column, nullable } = searchOrderColumns[field];
        return (qb
            .orderBy(column, (ob) => {
            const ordered = direction === enum_1.AssetOrder.Asc ? ob.asc() : ob.desc();
            return nullable ? ordered.nullsLast() : ordered;
        })
            .orderBy('asset.id', direction));
    }
    const scopeExample = { userIds: [decorators_1.DummyValue.UUID], lockedOwnerId: decorators_1.DummyValue.UUID };
    exports.searchMetadataV3Examples = [
        { name: 'baseline', params: [{ take: 100 }, {}, scopeExample] },
        {
            name: 'or-mixed-scope',
            params: [
                { take: 100 },
                {
                    filter: { or: [{ albumIds: { any: [decorators_1.DummyValue.UUID] } }, { city: { eq: decorators_1.DummyValue.STRING } }] },
                },
                scopeExample,
            ],
        },
        {
            name: 'or-exif-only',
            params: [
                { take: 100 },
                {
                    filter: { or: [{ city: { eq: decorators_1.DummyValue.STRING } }] },
                },
                scopeExample,
            ],
        },
        {
            name: 'string-eq-null',
            params: [{ take: 100 }, { filter: { city: { eq: null } } }, scopeExample],
        },
        {
            name: 'string-pattern-like',
            params: [
                { take: 100 },
                {
                    filter: { description: { like: decorators_1.DummyValue.STRING } },
                },
                scopeExample,
            ],
        },
        {
            name: 'string-pattern-notLike',
            params: [
                { take: 100 },
                {
                    filter: { description: { notLike: decorators_1.DummyValue.STRING } },
                },
                scopeExample,
            ],
        },
        {
            name: 'string-pattern-startsWith',
            params: [
                { take: 100 },
                {
                    filter: { originalFileName: { startsWith: decorators_1.DummyValue.STRING } },
                },
                scopeExample,
            ],
        },
        {
            name: 'string-similarity-ocr',
            params: [{ take: 100 }, { filter: { ocr: { matches: decorators_1.DummyValue.STRING } } }, scopeExample],
        },
        {
            name: 'ids-any',
            params: [{ take: 100 }, { filter: { albumIds: { any: [decorators_1.DummyValue.UUID] } } }, scopeExample],
        },
        {
            name: 'ids-all',
            params: [
                { take: 100 },
                {
                    filter: { personIds: { all: [decorators_1.DummyValue.UUID, decorators_1.DummyValue.UUID_1] } },
                },
                scopeExample,
            ],
        },
        {
            name: 'ids-all-single',
            params: [{ take: 100 }, { filter: { albumIds: { all: [decorators_1.DummyValue.UUID] } } }, scopeExample],
        },
        {
            name: 'ids-none',
            params: [{ take: 100 }, { filter: { tagIds: { none: [decorators_1.DummyValue.UUID] } } }, scopeExample],
        },
        {
            name: 'ids-tags-all',
            params: [
                { take: 100 },
                {
                    filter: { tagIds: { all: [decorators_1.DummyValue.UUID, decorators_1.DummyValue.UUID_1] } },
                },
                scopeExample,
            ],
        },
        {
            name: 'has-albums-false',
            params: [{ take: 100 }, { filter: { hasAlbums: { eq: false } } }, scopeExample],
        },
        {
            name: 'is-encoded',
            params: [{ take: 100 }, { filter: { isEncoded: { eq: true } } }, scopeExample],
        },
        {
            name: 'number-range',
            params: [
                { take: 100 },
                {
                    filter: { fileSizeInBytes: { gte: 100, lte: 1000 } },
                },
                scopeExample,
            ],
        },
        {
            name: 'date-eq',
            params: [{ take: 100 }, { filter: { takenAt: { eq: decorators_1.DummyValue.DATE } } }, scopeExample],
        },
        {
            name: 'date-range',
            params: [
                { take: 100 },
                {
                    filter: { takenAt: { gte: decorators_1.DummyValue.DATE, lt: decorators_1.DummyValue.DATE } },
                },
                scopeExample,
            ],
        },
        {
            name: 'order-fileSize-noExif',
            params: [
                { take: 100 },
                {
                    order: { field: enum_1.SearchOrderField.FileSizeInBytes, direction: enum_1.AssetOrder.Desc },
                    withExif: false,
                },
                scopeExample,
            ],
        },
        {
            name: 'order-rating-withExif',
            params: [
                { take: 100 },
                {
                    order: { field: enum_1.SearchOrderField.Rating, direction: enum_1.AssetOrder.Asc },
                    withExif: true,
                },
                scopeExample,
            ],
        },
        {
            name: 'or-branches',
            params: [
                { take: 100 },
                {
                    filter: {
                        or: [{ isFavorite: { eq: true } }, { personIds: { any: [decorators_1.DummyValue.UUID] } }],
                    },
                },
                scopeExample,
            ],
        },
        {
            name: 'or-with-top-level',
            params: [
                { take: 100 },
                {
                    filter: {
                        takenAt: { gte: decorators_1.DummyValue.DATE, lt: decorators_1.DummyValue.DATE },
                        or: [{ isFavorite: { eq: true } }, { albumIds: { any: [decorators_1.DummyValue.UUID] } }],
                    },
                },
                scopeExample,
            ],
        },
        {
            name: 'cursor-offset',
            params: [{ take: 100, skip: 100 }, { filter: { isFavorite: { eq: true } } }, scopeExample],
        },
    ];
    exports.searchRandomV3Examples = [
        { name: 'baseline', params: [100, {}, scopeExample] },
        {
            name: 'with-filter',
            params: [100, { filter: { isFavorite: { eq: true } } }, scopeExample],
        },
    ];
    exports.searchSmartV3Examples = [
        {
            name: 'baseline',
            params: [{ take: 100 }, { embedding: decorators_1.DummyValue.VECTOR }, scopeExample],
        },
        {
            name: 'with-filter',
            params: [
                { take: 100 },
                {
                    embedding: decorators_1.DummyValue.VECTOR,
                    filter: { takenAt: { gte: decorators_1.DummyValue.DATE, lt: decorators_1.DummyValue.DATE } },
                },
                scopeExample,
            ],
        },
        {
            name: 'cursor-offset',
            params: [{ take: 100, skip: 100 }, { embedding: decorators_1.DummyValue.VECTOR }, scopeExample],
        },
    ];
    exports.searchStatisticsV3Examples = [
        { name: 'baseline', params: [{}, scopeExample] },
        {
            name: 'with-filter',
            params: [
                {
                    filter: {
                        takenAt: { gte: decorators_1.DummyValue.DATE, lt: decorators_1.DummyValue.DATE },
                        fileSizeInBytes: { gte: 100 },
                    },
                },
                scopeExample,
            ],
        },
        {
            name: 'with-or',
            params: [
                {
                    filter: {
                        or: [{ isFavorite: { eq: true } }, { hasAlbums: { eq: false } }],
                    },
                },
                scopeExample,
            ],
        },
    ];
    function vectorIndexQuery({ vectorExtension, table, indexName, lists }) {
        switch (vectorExtension) {
            case enum_1.DatabaseExtension.VectorChord: {
                return `
            CREATE INDEX IF NOT EXISTS ${indexName} ON ${table} USING vchordrq (embedding vector_cosine_ops) WITH (options = $$
            residual_quantization = false
            [build.internal]
            lists = [${lists ?? 1}]
            spherical_centroids = true
            build_threads = 4
            sampling_factor = 1024
            $$)`;
            }
            case enum_1.DatabaseExtension.Vector: {
                return `
            CREATE INDEX IF NOT EXISTS ${indexName} ON ${table}
            USING hnsw (embedding vector_cosine_ops)
            WITH (ef_construction = 300, m = 16)`;
            }
            default: {
                throw new Error(`Unsupported vector extension: '${vectorExtension}'`);
            }
        }
    }
    const updateLockedColumns = (exif) => {
        exif.lockedProperties = database_1.lockableProperties.filter((property) => Object.hasOwn(exif, property));
        return exif;
    };
    exports.updateLockedColumns = updateLockedColumns;
    //# sourceMappingURL=database.js.map
  media.repository.js: |
    "use strict";
    var __decorate = (this && this.__decorate) || function (decorators, target, key, desc) {
        var c = arguments.length, r = c < 3 ? target : desc === null ? desc = Object.getOwnPropertyDescriptor(target, key) : desc, d;
        if (typeof Reflect === "object" && typeof Reflect.decorate === "function") r = Reflect.decorate(decorators, target, key, desc);
        else for (var i = decorators.length - 1; i >= 0; i--) if (d = decorators[i]) r = (c < 3 ? d(r) : c > 3 ? d(target, key, r) : d(target, key)) || r;
        return c > 3 && r && Object.defineProperty(target, key, r), r;
    };
    var __metadata = (this && this.__metadata) || function (k, v) {
        if (typeof Reflect === "object" && typeof Reflect.metadata === "function") return Reflect.metadata(k, v);
    };
    var __importDefault = (this && this.__importDefault) || function (mod) {
        return (mod && mod.__esModule) ? mod : { "default": mod };
    };
    var MediaRepository_1;
    Object.defineProperty(exports, "__esModule", { value: true });
    exports.MediaRepository = void 0;
    const common_1 = require("@nestjs/common");
    const exiftool_vendored_1 = require("exiftool-vendored");
    const fluent_ffmpeg_1 = __importDefault(require("fluent-ffmpeg"));
    const lodash_1 = __importDefault(require("lodash"));
    const luxon_1 = require("luxon");
    const node_child_process_1 = require("node:child_process");
    const promises_1 = __importDefault(require("node:fs/promises"));
    const node_stream_1 = require("node:stream");
    const sharp_1 = __importDefault(require("sharp"));
    const constants_1 = require("../constants");
    const enum_1 = require("../enum");
    const logging_repository_1 = require("./logging.repository");
    const misc_1 = require("../utils/misc");
    const transform_1 = require("../utils/transform");
    const probe = (input, options) => new Promise((resolve, reject) => fluent_ffmpeg_1.default.ffprobe(input, options, (error, data) => (error ? reject(error) : resolve(data))));
    const pascalCase = (str) => lodash_1.default.upperFirst(lodash_1.default.camelCase(str.toLowerCase()));
    let MediaRepository = MediaRepository_1 = class MediaRepository {
        logger;
        constructor(logger) {
            this.logger = logger;
            this.logger.setContext(MediaRepository_1.name);
            sharp_1.default.concurrency(0);
            sharp_1.default.cache({ files: 0 });
        }
        async extract(input) {
            for (const { tag, format } of [
                { tag: 'JpgFromRaw2', format: enum_1.RawExtractedFormat.Jpeg },
                { tag: 'JpgFromRaw', format: enum_1.RawExtractedFormat.Jpeg },
                { tag: 'PreviewJXL', format: enum_1.RawExtractedFormat.Jxl },
                { tag: 'PreviewImage', format: enum_1.RawExtractedFormat.Jpeg },
            ]) {
                try {
                    const buffer = await exiftool_vendored_1.exiftool.extractBinaryTagToBuffer(tag, input);
                    this.logger.debug(`Successfully extracted ${tag} buffer from image`);
                    return { buffer, format };
                }
                catch (error) {
                    this.logger.debug(`Could not extract ${tag} buffer from image: ${error}`);
                }
            }
            return null;
        }
        async writeExif(tags, output) {
            try {
                const tagsToWrite = {
                    ExifImageWidth: tags.exifImageWidth,
                    ExifImageHeight: tags.exifImageHeight,
                    DateTimeOriginal: tags.dateTimeOriginal && exiftool_vendored_1.ExifDateTime.fromMillis(tags.dateTimeOriginal.getTime()),
                    ModifyDate: tags.modifyDate && exiftool_vendored_1.ExifDateTime.fromMillis(tags.modifyDate.getTime()),
                    TimeZone: tags.timeZone,
                    GPSLatitude: tags.latitude,
                    GPSLongitude: tags.longitude,
                    ProjectionType: tags.projectionType,
                    City: tags.city,
                    Country: tags.country,
                    Make: tags.make,
                    Model: tags.model,
                    LensModel: tags.lensModel,
                    Fnumber: tags.fNumber?.toFixed(1),
                    FocalLength: tags.focalLength?.toFixed(1),
                    ISO: tags.iso,
                    ExposureTime: tags.exposureTime,
                    ProfileDescription: tags.profileDescription,
                    ColorSpace: tags.colorspace,
                    Rating: tags.rating === null ? 0 : tags.rating,
                    'Orientation#': tags.orientation ? Number(tags.orientation) : undefined,
                };
                await exiftool_vendored_1.exiftool.write(output, tagsToWrite, {
                    ignoreMinorErrors: true,
                    writeArgs: ['-overwrite_original'],
                });
                return true;
            }
            catch (error) {
                this.logger.warn(`Could not write exif data to image: ${error.message}`);
                return false;
            }
        }
        async copyTagGroup(tagGroup, source, target) {
            try {
                await exiftool_vendored_1.exiftool.write(target, {}, {
                    ignoreMinorErrors: true,
                    writeArgs: ['-TagsFromFile', source, `-${tagGroup}:all>${tagGroup}:all`, '-overwrite_original'],
                });
                return true;
            }
            catch (error) {
                this.logger.warn(`Could not copy tag data to image: ${error.message}`);
                return false;
            }
        }
        decodeImage(input, options) {
            return this.getImageDecodingPipeline(input, options).raw().toBuffer({ resolveWithObject: true });
        }
        applyEdits(pipeline, edits) {
            const crop = edits.find((edit) => edit.action === 'crop');
            if (crop) {
                pipeline = pipeline.extract({
                    left: Math.round(crop.parameters.x),
                    top: Math.round(crop.parameters.y),
                    width: Math.round(crop.parameters.width),
                    height: Math.round(crop.parameters.height),
                });
            }
            const affineEditOperations = edits.filter((edit) => edit.action !== 'crop');
            if (affineEditOperations.length > 0) {
                const { a, b, c, d } = (0, transform_1.createAffineMatrix)(affineEditOperations);
                pipeline = pipeline.affine([
                    [a, b],
                    [c, d],
                ]);
            }
            return pipeline;
        }
        async generateThumbnail(input, options, output) {
            await this.getImageDecodingPipeline(input, options)
                .toFormat(options.format, {
                quality: options.quality,
                chromaSubsampling: options.quality >= 80 ? '4:4:4' : '4:2:0',
                progressive: options.progressive,
            })
                .toFile(output);
        }
        getImageDecodingPipeline(input, options) {
            let pipeline = (0, sharp_1.default)(input, {
                failOn: options.processInvalidImages ? 'none' : 'error',
                limitInputChannels: false,
                limitInputPixels: false,
                raw: options.raw,
                unlimited: true,
            })
                .pipelineColorspace(options.colorspace === enum_1.Colorspace.Srgb ? 'srgb' : 'rgb16')
                .withIccProfile(options.colorspace);
            if (!options.raw) {
                const { angle, flip, flop } = options.orientation ? constants_1.ORIENTATION_TO_SHARP_ROTATION[options.orientation] : {};
                pipeline = pipeline.rotate(angle);
                if (flip) {
                    pipeline = pipeline.flip();
                }
                if (flop) {
                    pipeline = pipeline.flop();
                }
            }
            if (options.edits && options.edits.length > 0) {
                pipeline = this.applyEdits(pipeline, options.edits);
            }
            if (options.size !== undefined) {
                pipeline = pipeline.resize(options.size, options.size, { fit: 'outside', withoutEnlargement: true });
            }
            return pipeline;
        }
        async generateThumbhash(input, options) {
            const { rgbaToThumbHash } = await import('thumbhash');
            const { data, info } = await this.getImageDecodingPipeline(input, {
                colorspace: options.colorspace,
                processInvalidImages: options.processInvalidImages,
                raw: options.raw,
                edits: options.edits,
            })
                .resize(100, 100, { fit: 'inside', withoutEnlargement: true })
                .raw()
                .ensureAlpha()
                .toBuffer({ resolveWithObject: true });
            return Buffer.from(rgbaToThumbHash(info.width, info.height, data));
        }
        async probe(input, options) {
            const results = await probe(input, options?.countFrames ? ['-count_packets'] : []);
            return {
                format: {
                    formatName: results.format.format_name,
                    formatLongName: results.format.format_long_name,
                    duration: this.parseFloat(results.format.duration),
                    bitrate: this.parseInt(results.format.bit_rate),
                },
                videoStreams: results.streams
                    .filter((stream) => stream.codec_type === 'video' && !stream.disposition?.attached_pic)
                    .sort((a, b) => this.compareStreams(a, b))
                    .map((stream) => {
                    const height = this.parseInt(stream.height);
                    const dar = this.getDar(stream.display_aspect_ratio);
                    return {
                        index: stream.index,
                        height,
                        width: dar ? Math.round(height * dar) : this.parseInt(stream.width),
                        codecName: stream.codec_name === 'h265' ? 'hevc' : (stream.codec_name ?? null),
                        profile: this.parseVideoProfile(stream.codec_name, stream.profile) ?? null,
                        level: this.parseOptionalInt(stream.level),
                        frameCount: this.parseInt(options?.countFrames ? stream.nb_read_packets : stream.nb_frames),
                        frameRate: this.parseFrameRate(stream.avg_frame_rate ?? stream.r_frame_rate),
                        timeBase: this.parseRational(stream.time_base)?.den ?? null,
                        rotation: this.parseInt(stream.rotation),
                        bitrate: this.parseInt(stream.bit_rate),
                        pixelFormat: stream.pix_fmt || 'yuv420p',
                        colorPrimaries: this.parseEnum(enum_1.ColorPrimaries, stream.color_primaries) ?? enum_1.ColorPrimaries.Unknown,
                        colorMatrix: this.parseEnum(enum_1.ColorMatrix, stream.color_space) ?? enum_1.ColorMatrix.Unknown,
                        colorTransfer: this.parseEnum(enum_1.ColorTransfer, stream.color_transfer) ?? enum_1.ColorTransfer.Unknown,
                        dvProfile: this.parseOptionalInt(stream.dv_profile),
                        dvLevel: this.parseOptionalInt(stream.dv_level),
                        dvBlSignalCompatibilityId: this.parseOptionalInt(stream.dv_bl_signal_compatibility_id),
                        cropTop: this.parseOptionalInt(stream.crop_top),
                        cropBottom: this.parseOptionalInt(stream.crop_bottom),
                        cropLeft: this.parseOptionalInt(stream.crop_left),
                        cropRight: this.parseOptionalInt(stream.crop_right),
                    };
                }),
                audioStreams: results.streams
                    .filter((stream) => stream.codec_type === 'audio')
                    .sort((a, b) => this.compareStreams(a, b))
                    .map((stream) => ({
                    index: stream.index,
                    codecName: stream.codec_name ?? null,
                    profile: stream.codec_name === 'aac' ? this.parseEnum(enum_1.AacProfile, stream.profile) : null,
                    bitrate: this.parseInt(stream.bit_rate),
                })),
            };
        }
        probePackets(input, streamIndex) {
            const ffprobe = (0, node_child_process_1.spawn)('ffprobe', [
                '-v',
                'error',
                '-select_streams',
                String(streamIndex),
                '-show_entries',
                'packet=pts,duration,flags',
                '-of',
                'csv=p=0',
                input,
            ], { stdio: ['ignore', 'pipe', 'pipe'] });
            let totalDuration = 0;
            const keyframePts = [];
            const keyframeAccDuration = [];
            const keyframeOwnDuration = [];
            const postDiscard = [];
            const parseLine = (line) => {
                if (!line) {
                    return;
                }
                const [ptsStr, durationStr, flags] = line.split(',', 3);
                const pts = Number.parseInt(ptsStr);
                const duration = Number.parseInt(durationStr);
                if (Number.isNaN(pts) || Number.isNaN(duration) || !flags) {
                    return;
                }
                totalDuration += duration;
                if (flags[1] !== 'D') {
                    postDiscard.push({ pts, duration });
                }
                if (flags[0] === 'K') {
                    keyframePts.push(pts);
                    keyframeAccDuration.push(totalDuration);
                    keyframeOwnDuration.push(duration);
                }
            };
            let stderr = '';
            let remainder = '';
            ffprobe.stderr.setEncoding('utf8');
            ffprobe.stderr.on('data', (chunk) => (stderr += chunk));
            ffprobe.stdout.setEncoding('utf8');
            ffprobe.stdout.on('data', (chunk) => {
                const lines = chunk.split('\n');
                lines[0] = remainder + lines[0];
                remainder = lines.pop();
                for (const line of lines) {
                    parseLine(line);
                }
            });
            return new Promise((resolve, reject) => {
                ffprobe.on('error', reject);
                ffprobe.on('close', (code) => {
                    if (code !== 0) {
                        return reject(new Error(`ffprobe exited with code ${code}: ${stderr.trim()}`));
                    }
                    parseLine(remainder);
                    if (postDiscard.length === 0) {
                        return resolve(null);
                    }
                    resolve({
                        totalDuration,
                        packetCount: postDiscard.length,
                        outputFrames: this.cfrOutputFrames(postDiscard, postDiscard.length / totalDuration),
                        keyframePts,
                        keyframeAccDuration,
                        keyframeOwnDuration,
                    });
                });
            });
        }
        transcode(input, output, options) {
            if (!options.twoPass) {
                return new Promise((resolve, reject) => {
                    this.configureFfmpegCall(input, output, options)
                        .on('error', reject)
                        .on('end', () => resolve())
                        .run();
                });
            }
            if (typeof output !== 'string') {
                throw new TypeError('Two-pass transcoding does not support writing to a stream');
            }
            return new Promise((resolve, reject) => {
                this.configureFfmpegCall(input, '/dev/null', options)
                    .addOptions('-pass', '1')
                    .addOptions('-passlogfile', output)
                    .addOptions('-f null')
                    .on('error', reject)
                    .on('end', () => {
                    this.configureFfmpegCall(input, output, options)
                        .addOptions('-pass', '2')
                        .addOptions('-passlogfile', output)
                        .on('error', reject)
                        .on('end', () => (0, misc_1.handlePromiseError)(promises_1.default.unlink(`${output}-0.log`), this.logger))
                        .on('end', () => (0, misc_1.handlePromiseError)(promises_1.default.rm(`${output}-0.log.mbtree`, { force: true }), this.logger))
                        .on('end', () => resolve())
                        .run();
                })
                    .run();
            });
        }
        async getImageMetadata(input) {
            const { width = 0, height = 0, hasAlpha = false } = await (0, sharp_1.default)(input, { unlimited: true }).metadata();
            return { width, height, isTransparent: hasAlpha };
        }
        configureFfmpegCall(input, output, options) {
            const ffmpegCall = (0, fluent_ffmpeg_1.default)(input, { niceness: 10 })
                .inputOptions(options.inputOptions)
                .outputOptions(options.outputOptions)
                .output(output)
                .on('start', (command) => this.logger.debug(command))
                .on('error', (error, _, stderr) => this.logger.error(stderr || error));
            const { frameCount, percentInterval } = options.progress;
            const frameInterval = Math.ceil(frameCount / (100 / percentInterval));
            if (this.logger.isLevelEnabled(enum_1.LogLevel.Debug) && frameCount && frameInterval) {
                let lastProgressFrame = 0;
                ffmpegCall.on('progress', (progress) => {
                    if (progress.frames - lastProgressFrame < frameInterval) {
                        return;
                    }
                    lastProgressFrame = progress.frames;
                    const percent = ((progress.frames / frameCount) * 100).toFixed(2);
                    const ms = progress.currentFps ? Math.floor((frameCount - progress.frames) / progress.currentFps) * 1000 : 0;
                    const duration = ms ? luxon_1.Duration.fromMillis(ms).rescale().toHuman({ unitDisplay: 'narrow' }) : '';
                    const outputText = output instanceof node_stream_1.Writable ? 'stream' : output.split('/').pop();
                    this.logger.debug(`Transcoding ${percent}% done${duration ? `, estimated ${duration} remaining` : ''} for output ${outputText}`);
                });
            }
            return ffmpegCall;
        }
        parseInt(value) {
            return Number.parseInt(value) || 0;
        }
        parseFloat(value) {
            return Number.parseFloat(value) || 0;
        }
        parseOptionalInt(value) {
            const parsed = Number.parseInt(value);
            return Number.isNaN(parsed) ? null : parsed;
        }
        parseEnum(enumObj, value) {
            return value ? (enumObj[pascalCase(value)] ?? null) : null;
        }
        parseRational(value) {
            if (value) {
                const [num, den = 1] = value.split('/').map(Number);
                if (num && den) {
                    return { num, den };
                }
            }
            return null;
        }
        parseFrameRate(value) {
            const r = this.parseRational(value);
            return r ? r.num / r.den : null;
        }
        getDar(dar) {
            if (dar) {
                const [darW, darH] = dar.split(':').map(Number);
                if (darW && darH) {
                    return darW / darH;
                }
            }
            return 0;
        }
        parseVideoProfile(codec, profile) {
            switch (codec) {
                case 'h264': {
                    return this.parseEnum(enum_1.H264Profile, profile);
                }
                case 'h265':
                case 'hevc': {
                    return this.parseEnum(enum_1.HevcProfile, profile);
                }
                case 'av1': {
                    return this.parseEnum(enum_1.Av1Profile, profile);
                }
                default: {
                    return null;
                }
            }
        }
        compareStreams(a, b) {
            const d = (b.disposition?.default ?? 0) - (a.disposition?.default ?? 0);
            if (d !== 0) {
                return d;
            }
            return this.parseInt(b.bit_rate) - this.parseInt(a.bit_rate);
        }
        cfrOutputFrames(packets, slotsPerTick) {
            packets.sort((a, b) => a.pts - b.pts);
            const firstPts = packets[0].pts;
            let outputFrames = 0;
            let nextPts = 0;
            const history = [0, 0, 0];
            for (const pkt of packets) {
                const syncIpts = (pkt.pts - firstPts) * slotsPerTick;
                const duration = pkt.duration * slotsPerTick;
                let delta0 = syncIpts - nextPts;
                const delta = delta0 + duration;
                if (delta0 < 0 && delta > 0) {
                    delta0 = 0;
                }
                let nb = 1;
                let nbPrev = 0;
                if (delta < -1.1) {
                    nb = 0;
                }
                else if (delta > 1.1) {
                    nb = Math.round(delta);
                    if (delta0 > 1.1) {
                        nbPrev = Math.round(delta0 - 0.6);
                    }
                }
                outputFrames += nb;
                nextPts += nb;
                history[2] = history[1];
                history[1] = history[0];
                history[0] = nbPrev;
            }
            const median = history.sort((a, b) => a - b)[1];
            return outputFrames + median;
        }
    };
    exports.MediaRepository = MediaRepository;
    exports.MediaRepository = MediaRepository = MediaRepository_1 = __decorate([
        (0, common_1.Injectable)(),
        __metadata("design:paramtypes", [logging_repository_1.LoggingRepository])
    ], MediaRepository);
    //# sourceMappingURL=media.repository.js.map
  asset.repository.js: |
    "use strict";
    var __decorate = (this && this.__decorate) || function (decorators, target, key, desc) {
        var c = arguments.length, r = c < 3 ? target : desc === null ? desc = Object.getOwnPropertyDescriptor(target, key) : desc, d;
        if (typeof Reflect === "object" && typeof Reflect.decorate === "function") r = Reflect.decorate(decorators, target, key, desc);
        else for (var i = decorators.length - 1; i >= 0; i--) if (d = decorators[i]) r = (c < 3 ? d(r) : c > 3 ? d(target, key, r) : d(target, key)) || r;
        return c > 3 && r && Object.defineProperty(target, key, r), r;
    };
    var __metadata = (this && this.__metadata) || function (k, v) {
        if (typeof Reflect === "object" && typeof Reflect.metadata === "function") return Reflect.metadata(k, v);
    };
    var __param = (this && this.__param) || function (paramIndex, decorator) {
        return function (target, key) { decorator(target, key, paramIndex); }
    };
    Object.defineProperty(exports, "__esModule", { value: true });
    exports.AssetRepository = void 0;
    const common_1 = require("@nestjs/common");
    const kysely_1 = require("kysely");
    const postgres_1 = require("kysely/helpers/postgres");
    const lodash_1 = require("lodash");
    const nestjs_kysely_1 = require("nestjs-kysely");
    const decorators_1 = require("../decorators");
    const enum_1 = require("../enum");
    const database_1 = require("../utils/database");
    const misc_1 = require("../utils/misc");
    const distinctLocked = (eb, columns) => (0, kysely_1.sql) `nullif(array(select distinct unnest(${eb.ref('asset_exif.lockedProperties')} || ${columns})), '{}')`;
    const getBoundingCircle = (bbox) => {
        const { west, south, east, north } = bbox;
        const eastUnwrapped = west <= east ? east : east + 360;
        const centerLongitude = (((west + eastUnwrapped) / 2 + 540) % 360) - 180;
        const centerLatitude = (south + north) / 2;
        const radius = (0, kysely_1.sql) `greatest(
        earth_distance(ll_to_earth_public(${centerLatitude}, ${centerLongitude}), ll_to_earth_public(${south}, ${west})),
        earth_distance(ll_to_earth_public(${centerLatitude}, ${centerLongitude}), ll_to_earth_public(${south}, ${east})),
        earth_distance(ll_to_earth_public(${centerLatitude}, ${centerLongitude}), ll_to_earth_public(${north}, ${west})),
        earth_distance(ll_to_earth_public(${centerLatitude}, ${centerLongitude}), ll_to_earth_public(${north}, ${east}))
      )`;
        return { centerLatitude, centerLongitude, radius };
    };
    const withBoundingBox = (qb, bbox) => {
        const { west, south, east, north } = bbox;
        const withLatitude = qb.where('asset_exif.latitude', '>=', south).where('asset_exif.latitude', '<=', north);
        if (west <= east) {
            return withLatitude.where('asset_exif.longitude', '>=', west).where('asset_exif.longitude', '<=', east);
        }
        return withLatitude.where((eb) => eb.or([eb('asset_exif.longitude', '>=', west), eb('asset_exif.longitude', '<=', east)]));
    };
    let AssetRepository = class AssetRepository {
        db;
        constructor(db) {
            this.db = db;
        }
        async upsertExif({ exif, audio, video, keyframes, lockedPropertiesBehavior }) {
            let query = this.db;
            if (audio) {
                query = this.db.with('audio', (qb) => qb
                    .insertInto('asset_audio')
                    .values(audio)
                    .onConflict((oc) => oc.column('assetId').doUpdateSet(({ ref }) => ({
                    bitrate: ref('excluded.bitrate'),
                    index: ref('excluded.index'),
                    profile: ref('excluded.profile'),
                    codecName: ref('excluded.codecName'),
                }))));
            }
            if (video) {
                query = query.with('video', (qb) => qb
                    .insertInto('asset_video')
                    .values(video)
                    .onConflict((oc) => oc.column('assetId').doUpdateSet(({ ref }) => ({
                    bitrate: ref('excluded.bitrate'),
                    frameCount: ref('excluded.frameCount'),
                    timeBase: ref('excluded.timeBase'),
                    index: ref('excluded.index'),
                    profile: ref('excluded.profile'),
                    level: ref('excluded.level'),
                    colorPrimaries: ref('excluded.colorPrimaries'),
                    colorTransfer: ref('excluded.colorTransfer'),
                    colorMatrix: ref('excluded.colorMatrix'),
                    dvProfile: ref('excluded.dvProfile'),
                    dvLevel: ref('excluded.dvLevel'),
                    dvBlSignalCompatibilityId: ref('excluded.dvBlSignalCompatibilityId'),
                    codecName: ref('excluded.codecName'),
                    formatName: ref('excluded.formatName'),
                    formatLongName: ref('excluded.formatLongName'),
                    pixelFormat: ref('excluded.pixelFormat'),
                    cropTop: ref('excluded.cropTop'),
                    cropBottom: ref('excluded.cropBottom'),
                    cropLeft: ref('excluded.cropLeft'),
                    cropRight: ref('excluded.cropRight'),
                }))));
            }
            if (keyframes) {
                query = query.with('keyframe', (qb) => qb
                    .insertInto('asset_keyframe')
                    .values(keyframes)
                    .onConflict((oc) => oc.column('assetId').doUpdateSet(({ ref }) => ({
                    pts: ref('excluded.pts'),
                    accDuration: ref('excluded.accDuration'),
                    ownDuration: ref('excluded.ownDuration'),
                    totalDuration: ref('excluded.totalDuration'),
                    packetCount: ref('excluded.packetCount'),
                    outputFrames: ref('excluded.outputFrames'),
                }))));
            }
            await query
                .insertInto('asset_exif')
                .values(exif)
                .onConflict((oc) => oc.column('assetId').doUpdateSet((eb) => {
                const updateLocked = (col) => eb.ref(`excluded.${col}`);
                const skipLocked = (col) => eb
                    .case()
                    .when((0, kysely_1.sql) `${col}`, '=', eb.fn.any('asset_exif.lockedProperties'))
                    .then(eb.ref(`asset_exif.${col}`))
                    .else(eb.ref(`excluded.${col}`))
                    .end();
                const ref = lockedPropertiesBehavior === 'skip' ? skipLocked : updateLocked;
                return {
                    ...(0, database_1.removeUndefinedKeys)({
                        description: ref('description'),
                        exifImageWidth: ref('exifImageWidth'),
                        exifImageHeight: ref('exifImageHeight'),
                        fileSizeInByte: ref('fileSizeInByte'),
                        orientation: ref('orientation'),
                        dateTimeOriginal: ref('dateTimeOriginal'),
                        modifyDate: ref('modifyDate'),
                        timeZone: ref('timeZone'),
                        latitude: ref('latitude'),
                        longitude: ref('longitude'),
                        projectionType: ref('projectionType'),
                        city: ref('city'),
                        livePhotoCID: ref('livePhotoCID'),
                        autoStackId: ref('autoStackId'),
                        state: ref('state'),
                        country: ref('country'),
                        make: ref('make'),
                        model: ref('model'),
                        lensModel: ref('lensModel'),
                        fNumber: ref('fNumber'),
                        focalLength: ref('focalLength'),
                        iso: ref('iso'),
                        exposureTime: ref('exposureTime'),
                        profileDescription: ref('profileDescription'),
                        colorspace: ref('colorspace'),
                        bitsPerSample: ref('bitsPerSample'),
                        rating: ref('rating'),
                        fps: ref('fps'),
                        tags: ref('tags'),
                        lockedProperties: lockedPropertiesBehavior === 'append'
                            ? distinctLocked(eb, exif.lockedProperties ?? null)
                            : ref('lockedProperties'),
                    }, exif),
                };
            }))
                .execute();
        }
        async updateAllExif(ids, options) {
            if (ids.length === 0) {
                return;
            }
            await this.db
                .updateTable('asset_exif')
                .set((eb) => ({
                ...options,
                lockedProperties: distinctLocked(eb, Object.keys(options)),
            }))
                .where('assetId', 'in', ids)
                .execute();
        }
        updateDateTimeOriginal(ids, delta, timeZone) {
            return this.db
                .updateTable('asset_exif')
                .set((eb) => ({
                dateTimeOriginal: (0, kysely_1.sql) `"dateTimeOriginal" + ${(delta ?? 0) + ' minute'}::interval`,
                timeZone,
                lockedProperties: distinctLocked(eb, ['dateTimeOriginal', 'timeZone']),
            }))
                .where('assetId', 'in', ids)
                .returning(['assetId', 'dateTimeOriginal', 'timeZone'])
                .execute();
        }
        unlockProperties(assetId, properties) {
            return this.db
                .updateTable('asset_exif')
                .where('assetId', '=', assetId)
                .set((eb) => ({
                lockedProperties: (0, kysely_1.sql) `nullif(array(select distinct property from unnest(${eb.ref('asset_exif.lockedProperties')}) property where not property = any(${properties})), '{}')`,
            }))
                .execute();
        }
        async upsertJobStatus(...jobStatus) {
            if (jobStatus.length === 0) {
                return;
            }
            const values = jobStatus.map((row) => ({ ...row, assetId: (0, database_1.asUuid)(row.assetId) }));
            await this.db
                .insertInto('asset_job_status')
                .values(values)
                .onConflict((oc) => oc.column('assetId').doUpdateSet((eb) => (0, database_1.removeUndefinedKeys)({
                duplicatesDetectedAt: eb.ref('excluded.duplicatesDetectedAt'),
                facesRecognizedAt: eb.ref('excluded.facesRecognizedAt'),
                metadataExtractedAt: eb.ref('excluded.metadataExtractedAt'),
                ocrAt: eb.ref('excluded.ocrAt'),
            }, values[0])))
                .execute();
        }
        getMetadata(assetId) {
            return this.db
                .selectFrom('asset_metadata')
                .select(['key', 'value', 'updatedAt'])
                .where('assetId', '=', assetId)
                .execute();
        }
        upsertMetadata(id, items) {
            if (items.length === 0) {
                return [];
            }
            return this.db
                .insertInto('asset_metadata')
                .values(items.map((item) => ({ assetId: id, ...item })))
                .onConflict((oc) => oc
                .columns(['assetId', 'key'])
                .doUpdateSet((eb) => ({ key: eb.ref('excluded.key'), value: eb.ref('excluded.value') })))
                .returning(['key', 'value', 'updatedAt'])
                .execute();
        }
        upsertBulkMetadata(items) {
            return this.db
                .insertInto('asset_metadata')
                .values(items)
                .onConflict((oc) => oc
                .columns(['assetId', 'key'])
                .doUpdateSet((eb) => ({ key: eb.ref('excluded.key'), value: eb.ref('excluded.value') })))
                .returning(['assetId', 'key', 'value', 'updatedAt'])
                .execute();
        }
        getMetadataByKey(assetId, key) {
            return this.db
                .selectFrom('asset_metadata')
                .select(['key', 'value', 'updatedAt'])
                .where('assetId', '=', assetId)
                .where('key', '=', key)
                .executeTakeFirst();
        }
        async deleteMetadataByKey(id, key) {
            await this.db.deleteFrom('asset_metadata').where('assetId', '=', id).where('key', '=', key).execute();
        }
        async deleteBulkMetadata(items) {
            if (items.length === 0) {
                return;
            }
            await this.db.transaction().execute(async (tx) => {
                for (const { assetId, key } of items) {
                    await tx.deleteFrom('asset_metadata').where('assetId', '=', assetId).where('key', '=', key).execute();
                }
            });
        }
        create(asset) {
            return this.db.insertInto('asset').values(asset).returningAll().executeTakeFirstOrThrow();
        }
        async createAll(assets) {
            if (assets.length === 0) {
                return [];
            }
            const ids = await this.db.insertInto('asset').values(assets).returning('id').execute();
            return ids.map(({ id }) => id);
        }
        getByDayOfYear(ownerIds, { year, day, month }) {
            return this.db
                .with('res', (qb) => qb
                .with('today', (qb) => qb
                .selectFrom((eb) => eb
                .fn('generate_series', [
                (0, kysely_1.sql) `(select date_part('year', min(("localDateTime" at time zone 'UTC')::date))::int from asset)`,
                (0, kysely_1.sql) `${year - 1}`,
            ])
                .as('year'))
                .select((eb) => eb.fn('make_date', [(0, kysely_1.sql) `year::int`, (0, kysely_1.sql) `${month}::int`, (0, kysely_1.sql) `${day}::int`]).as('date')))
                .selectFrom('today')
                .innerJoinLateral((qb) => qb
                .selectFrom('asset')
                .select(['asset.id', 'asset.localDateTime'])
                .innerJoin('asset_job_status', 'asset.id', 'asset_job_status.assetId')
                .where((0, kysely_1.sql) `(asset."localDateTime" at time zone 'UTC')::date`, '=', (0, kysely_1.sql) `today.date`)
                .where('asset.ownerId', '=', (0, database_1.anyUuid)(ownerIds))
                .where('asset.visibility', '=', enum_1.AssetVisibility.Timeline)
                .where((eb) => eb.exists((qb) => qb
                .selectFrom('asset_file')
                .whereRef('assetId', '=', 'asset.id')
                .where('asset_file.type', '=', enum_1.AssetFileType.Preview)))
                .where('asset.deletedAt', 'is', null)
                .orderBy((0, kysely_1.sql) `(asset."localDateTime" at time zone 'UTC')::date`, 'desc')
                .limit(20)
                .as('a'), (join) => join.onTrue())
                .selectAll('a'))
                .selectFrom('res')
                .select((0, kysely_1.sql) `date_part('year', ("localDateTime" at time zone 'UTC')::date)::int`.as('year'))
                .select((eb) => eb.fn.jsonAgg(eb.table('res')).as('assets'))
                .groupBy((0, kysely_1.sql) `("localDateTime" at time zone 'UTC')::date`)
                .orderBy((0, kysely_1.sql) `("localDateTime" at time zone 'UTC')::date`, 'desc')
                .execute();
        }
        getByIds(ids) {
            return this.db.selectFrom('asset').selectAll('asset').where('asset.id', '=', (0, database_1.anyUuid)(ids)).execute();
        }
        getByIdsWithAllRelationsButStacks(ids, viewingUserId) {
            return this.db
                .selectFrom('asset')
                .selectAll('asset')
                .select((0, database_1.withFacesAndPeople)({ viewingUserId }))
                .select(database_1.withTags)
                .$call(database_1.withExif)
                .where('asset.id', '=', (0, database_1.anyUuid)(ids))
                .execute();
        }
        async deleteAll(ownerId) {
            await this.db.deleteFrom('asset').where('ownerId', '=', ownerId).execute();
        }
        getByLibraryIdAndOriginalPath(libraryId, originalPath) {
            return this.db
                .selectFrom('asset')
                .selectAll('asset')
                .where('libraryId', '=', (0, database_1.asUuid)(libraryId))
                .where('originalPath', '=', originalPath)
                .limit(1)
                .executeTakeFirst();
        }
        async getLivePhotoCount(motionId) {
            const [{ count }] = await this.db
                .selectFrom('asset')
                .select((eb) => eb.fn.countAll().as('count'))
                .where('livePhotoVideoId', '=', (0, database_1.asUuid)(motionId))
                .execute();
            return count;
        }
        getFileSamples() {
            return this.db.selectFrom('asset_file').select(['assetId', 'path']).limit(kysely_1.sql.lit(3)).execute();
        }
        getForCopy(id) {
            return this.db
                .selectFrom('asset')
                .select(['id', 'stackId', 'originalPath', 'isFavorite'])
                .select(database_1.withFiles)
                .where('id', '=', (0, database_1.asUuid)(id))
                .limit(1)
                .executeTakeFirst();
        }
        getById(id, { exifInfo, faces, files, library, owner, smartSearch, stack, tags, edits } = {}) {
            return this.db
                .selectFrom('asset')
                .selectAll('asset')
                .where('asset.id', '=', (0, database_1.asUuid)(id))
                .$if(!!exifInfo, database_1.withExif)
                .$if(!!faces, (qb) => qb
                .select(faces?.person ? (0, database_1.withFacesAndPeople)({ viewingUserId: faces.viewingUserId }) : database_1.withFaces)
                .$narrowType())
                .$if(!!library, (qb) => qb.select(database_1.withLibrary))
                .$if(!!owner, (qb) => qb.select(database_1.withOwner))
                .$if(!!smartSearch, database_1.withSmartSearch)
                .$if(!!stack, (qb) => qb
                .leftJoin('stack', 'stack.id', 'asset.stackId')
                .$if(!stack.assets, (qb) => qb.select((eb) => eb.fn.toJson(eb.table('stack')).$castTo().as('stack')))
                .$if(!!stack.assets, (qb) => qb
                .leftJoinLateral((eb) => eb
                .selectFrom('asset as stacked')
                .selectAll('stack')
                .select((0, kysely_1.sql) `array_agg(to_json(stacked) ORDER BY stacked."fileCreatedAt" ASC)`.as('assets'))
                .whereRef('stacked.stackId', '=', 'stack.id')
                .whereRef('stacked.id', '!=', 'stack.primaryAssetId')
                .where('stacked.deletedAt', 'is', null)
                .where('stacked.visibility', '=', enum_1.AssetVisibility.Timeline)
                .groupBy('stack.id')
                .as('stacked_assets'), (join) => join.on('stack.id', 'is not', null))
                .select((eb) => eb.fn.toJson(eb.table('stacked_assets')).as('stack'))))
                .$if(!!files, (qb) => qb.select(database_1.withFiles))
                .$if(!!tags, (qb) => qb.select(database_1.withTags))
                .$if(!!edits, (qb) => qb.select(database_1.withEdits))
                .limit(1)
                .executeTakeFirst();
        }
        async updateAll(ids, options) {
            if (ids.length === 0) {
                return;
            }
            await this.db.updateTable('asset').set(options).where('id', '=', (0, database_1.anyUuid)(ids)).execute();
        }
        async updateByLibraryId(libraryId, options) {
            await this.db.updateTable('asset').set(options).where('libraryId', '=', (0, database_1.asUuid)(libraryId)).execute();
        }
        async update(asset) {
            const value = (0, lodash_1.omitBy)(asset, lodash_1.isUndefined);
            delete value.id;
            if (!(0, lodash_1.isEmpty)(value)) {
                return this.db
                    .with('asset', (qb) => qb.updateTable('asset').set(asset).where('id', '=', (0, database_1.asUuid)(asset.id)).returningAll())
                    .selectFrom('asset')
                    .selectAll('asset')
                    .$call(database_1.withExif)
                    .$call((qb) => qb.select(database_1.withFaces))
                    .$call((qb) => qb.select(database_1.withEdits))
                    .executeTakeFirst();
            }
            return this.getById(asset.id, { exifInfo: true, faces: {}, edits: true });
        }
        async remove(asset) {
            await this.db.deleteFrom('asset').where('id', '=', (0, database_1.asUuid)(asset.id)).execute();
        }
        getByChecksum({ ownerId, libraryId, checksum }) {
            return this.db
                .selectFrom('asset')
                .selectAll('asset')
                .where('ownerId', '=', (0, database_1.asUuid)(ownerId))
                .where('checksum', '=', checksum)
                .$call((qb) => (libraryId ? qb.where('libraryId', '=', (0, database_1.asUuid)(libraryId)) : qb.where('libraryId', 'is', null)))
                .limit(1)
                .executeTakeFirst();
        }
        getByChecksums(userId, checksums) {
            return this.db
                .selectFrom('asset')
                .select(['id', 'checksum', 'deletedAt'])
                .where('ownerId', '=', (0, database_1.asUuid)(userId))
                .where('checksum', 'in', checksums)
                .execute();
        }
        async getUploadAssetIdByChecksum(ownerId, checksum) {
            const asset = await this.db
                .selectFrom('asset')
                .select('id')
                .where('ownerId', '=', (0, database_1.asUuid)(ownerId))
                .where('checksum', '=', checksum)
                .where('libraryId', 'is', null)
                .limit(1)
                .executeTakeFirst();
            return asset?.id;
        }
        findLivePhotoMatch(options) {
            const { ownerId, otherAssetId, livePhotoCID, type } = options;
            return this.db
                .selectFrom('asset')
                .select(['asset.id', 'asset.ownerId'])
                .innerJoin('asset_exif', 'asset.id', 'asset_exif.assetId')
                .where('id', '!=', (0, database_1.asUuid)(otherAssetId))
                .where('ownerId', '=', (0, database_1.asUuid)(ownerId))
                .where('type', '=', type)
                .where('asset_exif.livePhotoCID', '=', livePhotoCID)
                .limit(1)
                .executeTakeFirst();
        }
        getStatistics(ownerId, { visibility, isFavorite, isTrashed }) {
            return this.db
                .selectFrom('asset')
                .select((eb) => eb.fn.countAll().filterWhere('type', '=', enum_1.AssetType.Audio).as(enum_1.AssetType.Audio))
                .select((eb) => eb.fn.countAll().filterWhere('type', '=', enum_1.AssetType.Image).as(enum_1.AssetType.Image))
                .select((eb) => eb.fn.countAll().filterWhere('type', '=', enum_1.AssetType.Video).as(enum_1.AssetType.Video))
                .select((eb) => eb.fn.countAll().filterWhere('type', '=', enum_1.AssetType.Other).as(enum_1.AssetType.Other))
                .where('ownerId', '=', (0, database_1.asUuid)(ownerId))
                .$if(visibility === undefined, database_1.withDefaultVisibility)
                .$if(!!visibility, (qb) => qb.where('asset.visibility', '=', visibility))
                .$if(isFavorite !== undefined, (qb) => qb.where('isFavorite', '=', isFavorite))
                .$if(!!isTrashed, (qb) => qb.where('asset.status', '!=', enum_1.AssetStatus.Deleted))
                .where('deletedAt', isTrashed ? 'is not' : 'is', null)
                .executeTakeFirstOrThrow();
        }
        getCalendarHeatmap(ownerId, dto) {
            const dateColumns = {
                [enum_1.CalendarHeatmapType.Upload]: { order: enum_1.AssetOrderBy.CreatedAt, column: 'createdAt' },
                [enum_1.CalendarHeatmapType.Taken]: { order: enum_1.AssetOrderBy.TakenAt, column: 'localDateTime' },
            };
            const { order, column } = dateColumns[dto.type];
            const date = (0, database_1.truncatedDate)(order, 'DAY');
            return this.db
                .selectFrom('asset')
                .select(date.as('date'))
                .select((eb) => eb.fn.countAll().as('count'))
                .where('ownerId', '=', (0, database_1.asUuid)(ownerId))
                .where(column, '>=', dto.from)
                .where(column, '<', dto.to)
                .where('deletedAt', 'is', null)
                .groupBy(date)
                .orderBy('date', 'asc')
                .execute();
        }
        async getTimeBuckets(options, auth) {
            return this.db
                .with('asset', (qb) => qb
                .selectFrom('asset')
                .select((0, database_1.truncatedDate)(options.orderBy).as('timeBucket'))
                .$if(!!options.isTrashed, (qb) => qb.where('asset.status', '!=', enum_1.AssetStatus.Deleted))
                .where('asset.deletedAt', options.isTrashed ? 'is not' : 'is', null)
                .$if(!!options.bbox, (qb) => {
                const bbox = options.bbox;
                const circle = getBoundingCircle(bbox);
                const withBoundingCircle = qb
                    .innerJoin('asset_exif', 'asset.id', 'asset_exif.assetId')
                    .where((0, kysely_1.sql) `earth_box(ll_to_earth_public(${circle.centerLatitude}, ${circle.centerLongitude}), ${circle.radius})`, '@>', (0, kysely_1.sql) `ll_to_earth_public(asset_exif.latitude, asset_exif.longitude)`);
                return withBoundingBox(withBoundingCircle, bbox);
            })
                .$if(options.visibility === undefined, database_1.withDefaultVisibility)
                .$if(!!options.visibility, (qb) => qb.where('asset.visibility', '=', options.visibility))
                .$if(!!options.albumId, (qb) => qb
                .innerJoin('album_asset', 'asset.id', 'album_asset.assetId')
                .where('album_asset.albumId', '=', (0, database_1.asUuid)(options.albumId)))
                .$if(!!options.personId, (qb) => (0, database_1.hasPeople)(qb, [options.personId]))
                .$if(!!options.withStacked, (qb) => qb
                .leftJoin('stack', (join) => join.onRef('stack.id', '=', 'asset.stackId').onRef('stack.primaryAssetId', '=', 'asset.id'))
                .where((eb) => eb.or([eb('asset.stackId', 'is', null), eb(eb.table('stack'), 'is not', null)])))
                .$if(!!options.userIds, (qb) => qb.where((eb) => {
                const isOwner = eb('asset.ownerId', '=', (0, database_1.anyUuid)(options.userIds));
                return options.personId ? eb.or([isOwner, (0, database_1.inSharedAlbum)(eb, auth.user.id)]) : isOwner;
            }))
                .$if(options.isFavorite !== undefined, (qb) => qb.where('asset.isFavorite', '=', options.isFavorite))
                .$if(!!options.assetType, (qb) => qb.where('asset.type', '=', options.assetType))
                .$if(options.isDuplicate !== undefined, (qb) => qb.where('asset.duplicateId', options.isDuplicate ? 'is not' : 'is', null))
                .$if(!!options.tagId, (qb) => (0, database_1.withTagId)(qb, options.tagId)))
                .selectFrom('asset')
                .select((0, kysely_1.sql) `("timeBucket" AT TIME ZONE 'UTC')::date::text`.as('timeBucket'))
                .select((eb) => eb.fn.countAll().as('count'))
                .groupBy('timeBucket')
                .orderBy('timeBucket', options.order ?? 'desc')
                .execute();
        }
        getTimeBucket(timeBucket, options, auth) {
            const order = options.order ?? 'desc';
            const query = this.db
                .with('cte', (qb) => qb
                .selectFrom('asset')
                .innerJoin('asset_exif', 'asset.id', 'asset_exif.assetId')
                .select((eb) => [
                'asset.duration',
                'asset.id',
                'asset.visibility',
                (0, kysely_1.sql) `asset."isFavorite" and asset."ownerId" = ${auth.user.id}`.as('isFavorite'),
                (0, kysely_1.sql) `asset.type = 'IMAGE'`.as('isImage'),
                (0, kysely_1.sql) `asset."deletedAt" is not null`.as('isTrashed'),
                'asset.livePhotoVideoId',
                (0, kysely_1.sql) `extract(epoch from (asset."localDateTime" AT TIME ZONE 'UTC' - asset."fileCreatedAt" at time zone 'UTC'))::real / 3600`.as('localOffsetHours'),
                'asset.ownerId',
                'asset.status',
                (0, kysely_1.sql) `asset."fileCreatedAt" at time zone 'utc'`.as('fileCreatedAt'),
                (0, kysely_1.sql) `asset."createdAt" at time zone 'utc'`.as('createdAt'),
                eb.fn('encode', ['asset.thumbhash', kysely_1.sql.lit('base64')]).as('thumbhash'),
                'asset_exif.projectionType',
                eb.fn
                    .coalesce(eb
                    .case()
                    .when((0, kysely_1.sql) `asset."height" = 0 or asset."width" = 0`)
                    .then(eb.lit(1))
                    .else((0, kysely_1.sql) `round(asset."width"::numeric / asset."height"::numeric, 3)`)
                    .end(), eb.lit(1))
                    .as('ratio'),
            ])
                .$if(!auth.sharedLink || auth.sharedLink.showExif, (qb) => qb.select(['asset_exif.city', 'asset_exif.country']))
                .$if(!!options.withCoordinates, (qb) => qb.select(['asset_exif.latitude', 'asset_exif.longitude']))
                .where('asset.deletedAt', options.isTrashed ? 'is not' : 'is', null)
                .$if(options.visibility === undefined, database_1.withDefaultVisibility)
                .$if(!!options.visibility, (qb) => qb.where('asset.visibility', '=', options.visibility))
                .$if(!!options.bbox, (qb) => {
                const bbox = options.bbox;
                const circle = getBoundingCircle(bbox);
                const withBoundingCircle = qb.where((0, kysely_1.sql) `earth_box(ll_to_earth_public(${circle.centerLatitude}, ${circle.centerLongitude}), ${circle.radius})`, '@>', (0, kysely_1.sql) `ll_to_earth_public(asset_exif.latitude, asset_exif.longitude)`);
                return withBoundingBox(withBoundingCircle, bbox);
            })
                .where((0, database_1.truncatedDate)(options.orderBy), '=', timeBucket.replace(/^[+-]/, ''))
                .$if(!!options.albumId, (qb) => qb.where((eb) => eb.exists(eb
                .selectFrom('album_asset')
                .whereRef('album_asset.assetId', '=', 'asset.id')
                .where('album_asset.albumId', '=', (0, database_1.asUuid)(options.albumId)))))
                .$if(!!options.personId, (qb) => (0, database_1.hasPeople)(qb, [options.personId]))
                .$if(!!options.userIds, (qb) => qb.where((eb) => {
                const isOwner = eb('asset.ownerId', '=', (0, database_1.anyUuid)(options.userIds));
                return options.personId ? eb.or([isOwner, (0, database_1.inSharedAlbum)(eb, auth.user.id)]) : isOwner;
            }))
                .$if(options.isFavorite !== undefined, (qb) => qb.where('asset.isFavorite', '=', options.isFavorite))
                .$if(!!options.withStacked, (qb) => qb
                .where((eb) => eb.not(eb.exists(eb
                .selectFrom('stack')
                .whereRef('stack.id', '=', 'asset.stackId')
                .whereRef('stack.primaryAssetId', '!=', 'asset.id'))))
                .leftJoinLateral((eb) => eb
                .selectFrom('asset as stacked')
                .select((0, kysely_1.sql) `array[stacked."stackId"::text, count('stacked')::text]`.as('stack'))
                .whereRef('stacked.stackId', '=', 'asset.stackId')
                .where('stacked.deletedAt', 'is', null)
                .where('stacked.visibility', '=', enum_1.AssetVisibility.Timeline)
                .groupBy('stacked.stackId')
                .as('stacked_assets'), (join) => join.onTrue())
                .select('stack'))
                .$if(!!options.assetType, (qb) => qb.where('asset.type', '=', options.assetType))
                .$if(options.isDuplicate !== undefined, (qb) => qb.where('asset.duplicateId', options.isDuplicate ? 'is not' : 'is', null))
                .$if(!!options.isTrashed, (qb) => qb.where('asset.status', '!=', enum_1.AssetStatus.Deleted))
                .$if(!!options.tagId, (qb) => (0, database_1.withTagId)(qb, options.tagId))
                .orderBy(options.orderBy === enum_1.AssetOrderBy.CreatedAt
                ? (0, kysely_1.sql) `"createdAt"`
                : (0, kysely_1.sql) `(asset."localDateTime" AT TIME ZONE 'UTC')::date`, order)
                .orderBy('asset.fileCreatedAt', order)
                .orderBy('asset.originalFileName', order))
                .with('agg', (qb) => qb
                .selectFrom('cte')
                .select((eb) => [
                eb.fn.coalesce(eb.fn('array_agg', ['duration']), kysely_1.sql.lit('{}')).as('duration'),
                eb.fn.coalesce(eb.fn('array_agg', ['id']), kysely_1.sql.lit('{}')).as('id'),
                eb.fn.coalesce(eb.fn('array_agg', ['visibility']), kysely_1.sql.lit('{}')).as('visibility'),
                eb.fn.coalesce(eb.fn('array_agg', ['isFavorite']), kysely_1.sql.lit('{}')).as('isFavorite'),
                eb.fn.coalesce(eb.fn('array_agg', ['isImage']), kysely_1.sql.lit('{}')).as('isImage'),
                eb.fn.coalesce(eb.fn('array_agg', ['isTrashed']), kysely_1.sql.lit('{}')).as('isTrashed'),
                eb.fn.coalesce(eb.fn('array_agg', ['livePhotoVideoId']), kysely_1.sql.lit('{}')).as('livePhotoVideoId'),
                eb.fn.coalesce(eb.fn('array_agg', ['fileCreatedAt']), kysely_1.sql.lit('{}')).as('fileCreatedAt'),
                eb.fn.coalesce(eb.fn('array_agg', ['localOffsetHours']), kysely_1.sql.lit('{}')).as('localOffsetHours'),
                eb.fn.coalesce(eb.fn('array_agg', ['createdAt']), kysely_1.sql.lit('{}')).as('createdAt'),
                eb.fn.coalesce(eb.fn('array_agg', ['ownerId']), kysely_1.sql.lit('{}')).as('ownerId'),
                eb.fn.coalesce(eb.fn('array_agg', ['projectionType']), kysely_1.sql.lit('{}')).as('projectionType'),
                eb.fn.coalesce(eb.fn('array_agg', ['ratio']), kysely_1.sql.lit('{}')).as('ratio'),
                eb.fn.coalesce(eb.fn('array_agg', ['status']), kysely_1.sql.lit('{}')).as('status'),
                eb.fn.coalesce(eb.fn('array_agg', ['thumbhash']), kysely_1.sql.lit('{}')).as('thumbhash'),
            ])
                .$if(!auth.sharedLink || auth.sharedLink.showExif, (qb) => qb.select((eb) => [
                eb.fn.coalesce(eb.fn('array_agg', ['city']), kysely_1.sql.lit('{}')).as('city'),
                eb.fn.coalesce(eb.fn('array_agg', ['country']), kysely_1.sql.lit('{}')).as('country'),
            ]))
                .$if(!!options.withCoordinates, (qb) => qb.select((eb) => [
                eb.fn.coalesce(eb.fn('array_agg', ['latitude']), kysely_1.sql.lit('{}')).as('latitude'),
                eb.fn.coalesce(eb.fn('array_agg', ['longitude']), kysely_1.sql.lit('{}')).as('longitude'),
            ]))
                .$if(!!options.withStacked, (qb) => qb.select((eb) => eb.fn.coalesce(eb.fn('json_agg', ['stack']), kysely_1.sql.lit('[]')).as('stack'))))
                .selectFrom('agg')
                .select((0, kysely_1.sql) `to_json(agg)::text`.as('assets'));
            return query.executeTakeFirstOrThrow();
        }
        async getAssetIdByCity(ownerId, { minAssetsPerField, maxFields }) {
            const items = await this.db
                .with('cities', (qb) => qb
                .selectFrom('asset_exif')
                .select('city')
                .where('city', 'is not', null)
                .groupBy('city')
                .having((eb) => eb.fn('count', [eb.ref('assetId')]), '>=', minAssetsPerField))
                .selectFrom('asset')
                .innerJoin('asset_exif', 'asset.id', 'asset_exif.assetId')
                .innerJoin('cities', 'asset_exif.city', 'cities.city')
                .distinctOn('asset_exif.city')
                .select(['assetId as data', 'asset_exif.city as value'])
                .$narrowType()
                .where('ownerId', '=', (0, database_1.asUuid)(ownerId))
                .where('visibility', '=', enum_1.AssetVisibility.Timeline)
                .where('type', '=', enum_1.AssetType.Image)
                .where('deletedAt', 'is', null)
                .limit(maxFields)
                .execute();
            return { fieldName: 'exifInfo.city', items };
        }
        async getRecentlyCreatedAssetIds(ownerId, maxAssets) {
            const items = await this.db
                .selectFrom('asset')
                .select(['id as data', 'createdAt as value'])
                .where('ownerId', '=', (0, database_1.asUuid)(ownerId))
                .where('asset.visibility', '=', enum_1.AssetVisibility.Timeline)
                .where('type', '=', enum_1.AssetType.Image)
                .where('deletedAt', 'is', null)
                .orderBy('value', 'desc')
                .limit(maxAssets)
                .execute();
            return { fieldName: 'createdAt', items };
        }
        async upsertFile(file) {
            await this.db
                .insertInto('asset_file')
                .values(file)
                .onConflict((oc) => oc.columns(['assetId', 'type', 'isEdited']).doUpdateSet((eb) => ({
                path: eb.ref('excluded.path'),
            })))
                .execute();
        }
        async upsertFiles(files) {
            if (files.length === 0) {
                return;
            }
            await this.db
                .insertInto('asset_file')
                .values(files)
                .onConflict((oc) => oc.columns(['assetId', 'type', 'isEdited']).doUpdateSet((eb) => ({
                path: eb.ref('excluded.path'),
                isProgressive: eb.ref('excluded.isProgressive'),
                isTransparent: eb.ref('excluded.isTransparent'),
            })))
                .execute();
        }
        async deleteFile({ assetId, type, edited, }) {
            await this.db
                .deleteFrom('asset_file')
                .where('assetId', '=', (0, database_1.asUuid)(assetId))
                .where('type', '=', type)
                .$if(edited !== undefined, (qb) => qb.where('isEdited', '=', edited))
                .execute();
        }
        async deleteFiles(files) {
            if (files.length === 0) {
                return;
            }
            await this.db
                .deleteFrom('asset_file')
                .where('id', '=', (0, database_1.anyUuid)(files.map((file) => file.id)))
                .execute();
        }
        async detectOfflineExternalAssets(libraryId, importPaths, exclusionPatterns) {
            const paths = importPaths.map((importPath) => `${importPath}%`);
            const exclusions = exclusionPatterns.map((pattern) => (0, misc_1.globToPostgresRegex)(pattern));
            return this.db
                .updateTable('asset')
                .set({
                isOffline: true,
                deletedAt: new Date(),
            })
                .where('isOffline', '=', false)
                .where('isExternal', '=', true)
                .where('libraryId', '=', (0, database_1.asUuid)(libraryId))
                .where((eb) => eb.or([
                eb.not(eb.or(paths.map((path) => eb('originalPath', 'like', path)))),
                eb.or(exclusions.map((pattern) => eb('originalPath', '~', pattern))),
            ]))
                .executeTakeFirstOrThrow();
        }
        async filterNewExternalAssetPaths(libraryId, paths) {
            const result = await this.db
                .selectFrom((0, database_1.unnest)(paths).as('path'))
                .select('path')
                .where((eb) => eb.not(eb.exists(this.db
                .selectFrom('asset')
                .select('originalPath')
                .whereRef('asset.originalPath', '=', eb.ref('path'))
                .where('libraryId', '=', (0, database_1.asUuid)(libraryId))
                .where('isExternal', '=', true))))
                .execute();
            return result.map((row) => row.path);
        }
        async getLibraryAssetCount(libraryId) {
            const { count } = await this.db
                .selectFrom('asset')
                .select((eb) => eb.fn.countAll().as('count'))
                .where('libraryId', '=', (0, database_1.asUuid)(libraryId))
                .executeTakeFirstOrThrow();
            return count;
        }
        buildGetForOriginal(ids, isEdited) {
            return this.db
                .selectFrom('asset')
                .select('asset.id')
                .select('originalFileName')
                .where('asset.id', 'in', ids)
                .$if(isEdited, (qb) => qb
                .leftJoin('asset_file', (join) => join
                .onRef('asset.id', '=', 'asset_file.assetId')
                .on('asset_file.isEdited', '=', true)
                .on('asset_file.type', '=', enum_1.AssetFileType.FullSize))
                .select('asset_file.path as editedPath'))
                .select('originalPath');
        }
        getForOriginal(id, isEdited) {
            return this.buildGetForOriginal([id], isEdited).executeTakeFirstOrThrow();
        }
        getForOriginals(ids, isEdited) {
            return this.buildGetForOriginal(ids, isEdited).execute();
        }
        async getForThumbnail(id, type, isEdited) {
            return this.db
                .selectFrom('asset')
                .where('asset.id', '=', id)
                .leftJoin('asset_file', (join) => join.onRef('asset.id', '=', 'asset_file.assetId').on('asset_file.type', '=', type))
                .select(['asset.originalPath', 'asset.originalFileName', 'asset_file.path as path'])
                .orderBy('asset_file.isEdited', isEdited ? 'desc' : 'asc')
                .executeTakeFirstOrThrow();
        }
        async getForVideo(id) {
            return this.db
                .selectFrom('asset')
                .select(['asset.originalPath'])
                .select((eb) => (0, database_1.withFilePath)(eb, enum_1.AssetFileType.EncodedVideo).as('encodedVideoPath'))
                .where('asset.id', '=', id)
                .where('asset.type', '=', enum_1.AssetType.Video)
                .executeTakeFirst();
        }
        async getForOcr(id) {
            return this.db
                .selectFrom('asset')
                .where('asset.id', '=', id)
                .select(database_1.withEdits)
                .innerJoin('asset_exif', (join) => join.onRef('asset_exif.assetId', '=', 'asset.id'))
                .select(['asset_exif.exifImageWidth', 'asset_exif.exifImageHeight', 'asset_exif.orientation'])
                .executeTakeFirst();
        }
        async getForEdit(id) {
            return this.db
                .selectFrom('asset')
                .select(['asset.type', 'asset.livePhotoVideoId', 'asset.originalPath', 'asset.originalFileName'])
                .where('asset.id', '=', id)
                .innerJoin('asset_exif', (join) => join.onRef('asset_exif.assetId', '=', 'asset.id'))
                .select([
                'asset_exif.exifImageWidth',
                'asset_exif.exifImageHeight',
                'asset_exif.orientation',
                'asset_exif.projectionType',
            ])
                .executeTakeFirst();
        }
        async getForMetadataExtractionTags(id) {
            return this.db
                .selectFrom('asset_exif')
                .select('asset_exif.tags')
                .where('asset_exif.assetId', '=', id)
                .executeTakeFirst();
        }
        async getForFaces(id) {
            return this.db
                .selectFrom('asset')
                .innerJoin('asset_exif', (join) => join.onRef('asset_exif.assetId', '=', 'asset.id'))
                .select(['asset_exif.exifImageHeight', 'asset_exif.exifImageWidth', 'asset_exif.orientation'])
                .select(database_1.withEdits)
                .where('asset.id', '=', id)
                .executeTakeFirstOrThrow();
        }
        async getForUpdateTags(id) {
            return this.db
                .selectFrom('asset')
                .select((eb) => (0, postgres_1.jsonArrayFrom)(eb
                .selectFrom('tag')
                .select('tag.value')
                .innerJoin('tag_asset', 'tag.id', 'tag_asset.tagId')
                .whereRef('asset.id', '=', 'tag_asset.assetId')).as('tags'))
                .where('asset.id', '=', id)
                .executeTakeFirstOrThrow();
        }
    };
    exports.AssetRepository = AssetRepository;
    __decorate([
        (0, decorators_1.GenerateSql)({
            params: [
                {
                    exif: { dateTimeOriginal: decorators_1.DummyValue.DATE, lockedProperties: ['dateTimeOriginal'] },
                    lockedPropertiesBehavior: 'append',
                },
            ],
        }),
        __metadata("design:type", Function),
        __metadata("design:paramtypes", [Object]),
        __metadata("design:returntype", Promise)
    ], AssetRepository.prototype, "upsertExif", null);
    __decorate([
        (0, decorators_1.GenerateSql)({ params: [[decorators_1.DummyValue.UUID], { model: decorators_1.DummyValue.STRING }] }),
        (0, decorators_1.Chunked)(),
        __metadata("design:type", Function),
        __metadata("design:paramtypes", [Array, Object]),
        __metadata("design:returntype", Promise)
    ], AssetRepository.prototype, "updateAllExif", null);
    __decorate([
        (0, decorators_1.GenerateSql)({ params: [[decorators_1.DummyValue.UUID], decorators_1.DummyValue.NUMBER, decorators_1.DummyValue.STRING] }),
        (0, decorators_1.Chunked)(),
        __metadata("design:type", Function),
        __metadata("design:paramtypes", [Array, Number, String]),
        __metadata("design:returntype", void 0)
    ], AssetRepository.prototype, "updateDateTimeOriginal", null);
    __decorate([
        (0, decorators_1.GenerateSql)({ params: [decorators_1.DummyValue.UUID, ['description']] }),
        __metadata("design:type", Function),
        __metadata("design:paramtypes", [String, Array]),
        __metadata("design:returntype", void 0)
    ], AssetRepository.prototype, "unlockProperties", null);
    __decorate([
        (0, decorators_1.GenerateSql)({ params: [decorators_1.DummyValue.UUID] }),
        __metadata("design:type", Function),
        __metadata("design:paramtypes", [String]),
        __metadata("design:returntype", void 0)
    ], AssetRepository.prototype, "getMetadata", null);
    __decorate([
        (0, decorators_1.GenerateSql)({ params: [decorators_1.DummyValue.UUID, decorators_1.DummyValue.STRING] }),
        __metadata("design:type", Function),
        __metadata("design:paramtypes", [String, String]),
        __metadata("design:returntype", void 0)
    ], AssetRepository.prototype, "getMetadataByKey", null);
    __decorate([
        (0, decorators_1.GenerateSql)({ params: [decorators_1.DummyValue.UUID, decorators_1.DummyValue.STRING] }),
        __metadata("design:type", Function),
        __metadata("design:paramtypes", [String, String]),
        __metadata("design:returntype", Promise)
    ], AssetRepository.prototype, "deleteMetadataByKey", null);
    __decorate([
        (0, decorators_1.GenerateSql)({ params: [[{ assetId: decorators_1.DummyValue.UUID, key: decorators_1.DummyValue.STRING }]] }),
        __metadata("design:type", Function),
        __metadata("design:paramtypes", [Array]),
        __metadata("design:returntype", Promise)
    ], AssetRepository.prototype, "deleteBulkMetadata", null);
    __decorate([
        (0, decorators_1.ChunkedArray)({ chunkSize: 4000 }),
        __metadata("design:type", Function),
        __metadata("design:paramtypes", [Array]),
        __metadata("design:returntype", Promise)
    ], AssetRepository.prototype, "createAll", null);
    __decorate([
        (0, decorators_1.GenerateSql)({ params: [decorators_1.DummyValue.UUID, { year: 2000, day: 1, month: 1 }] }),
        __metadata("design:type", Function),
        __metadata("design:paramtypes", [Array, Object]),
        __metadata("design:returntype", void 0)
    ], AssetRepository.prototype, "getByDayOfYear", null);
    __decorate([
        (0, decorators_1.GenerateSql)({ params: [[decorators_1.DummyValue.UUID]] }),
        (0, decorators_1.ChunkedArray)(),
        __metadata("design:type", Function),
        __metadata("design:paramtypes", [Array]),
        __metadata("design:returntype", void 0)
    ], AssetRepository.prototype, "getByIds", null);
    __decorate([
        (0, decorators_1.GenerateSql)({ params: [[decorators_1.DummyValue.UUID]] }),
        (0, decorators_1.ChunkedArray)({ paramIndex: 0 }),
        __metadata("design:type", Function),
        __metadata("design:paramtypes", [Array, String]),
        __metadata("design:returntype", void 0)
    ], AssetRepository.prototype, "getByIdsWithAllRelationsButStacks", null);
    __decorate([
        (0, decorators_1.GenerateSql)({ params: [decorators_1.DummyValue.UUID] }),
        __metadata("design:type", Function),
        __metadata("design:paramtypes", [String]),
        __metadata("design:returntype", Promise)
    ], AssetRepository.prototype, "deleteAll", null);
    __decorate([
        (0, decorators_1.GenerateSql)({ params: [decorators_1.DummyValue.UUID, decorators_1.DummyValue.STRING] }),
        __metadata("design:type", Function),
        __metadata("design:paramtypes", [String, String]),
        __metadata("design:returntype", void 0)
    ], AssetRepository.prototype, "getByLibraryIdAndOriginalPath", null);
    __decorate([
        (0, decorators_1.GenerateSql)({ params: [decorators_1.DummyValue.UUID] }),
        __metadata("design:type", Function),
        __metadata("design:paramtypes", [String]),
        __metadata("design:returntype", Promise)
    ], AssetRepository.prototype, "getLivePhotoCount", null);
    __decorate([
        (0, decorators_1.GenerateSql)(),
        __metadata("design:type", Function),
        __metadata("design:paramtypes", []),
        __metadata("design:returntype", void 0)
    ], AssetRepository.prototype, "getFileSamples", null);
    __decorate([
        (0, decorators_1.GenerateSql)({ params: [decorators_1.DummyValue.UUID] }),
        __metadata("design:type", Function),
        __metadata("design:paramtypes", [String]),
        __metadata("design:returntype", void 0)
    ], AssetRepository.prototype, "getForCopy", null);
    __decorate([
        (0, decorators_1.GenerateSql)({ params: [decorators_1.DummyValue.UUID] }),
        __metadata("design:type", Function),
        __metadata("design:paramtypes", [String, Object]),
        __metadata("design:returntype", void 0)
    ], AssetRepository.prototype, "getById", null);
    __decorate([
        (0, decorators_1.GenerateSql)({ params: [[decorators_1.DummyValue.UUID], {}] }),
        (0, decorators_1.Chunked)(),
        __metadata("design:type", Function),
        __metadata("design:paramtypes", [Array, Object]),
        __metadata("design:returntype", Promise)
    ], AssetRepository.prototype, "updateAll", null);
    __decorate([
        (0, decorators_1.GenerateSql)({ params: [{ ownerId: decorators_1.DummyValue.UUID, libraryId: decorators_1.DummyValue.UUID, checksum: decorators_1.DummyValue.BUFFER }] }),
        __metadata("design:type", Function),
        __metadata("design:paramtypes", [Object]),
        __metadata("design:returntype", void 0)
    ], AssetRepository.prototype, "getByChecksum", null);
    __decorate([
        (0, decorators_1.GenerateSql)({ params: [decorators_1.DummyValue.UUID, [decorators_1.DummyValue.BUFFER]] }),
        __metadata("design:type", Function),
        __metadata("design:paramtypes", [String, Array]),
        __metadata("design:returntype", void 0)
    ], AssetRepository.prototype, "getByChecksums", null);
    __decorate([
        (0, decorators_1.GenerateSql)({ params: [decorators_1.DummyValue.UUID, decorators_1.DummyValue.BUFFER] }),
        __metadata("design:type", Function),
        __metadata("design:paramtypes", [String, Buffer]),
        __metadata("design:returntype", Promise)
    ], AssetRepository.prototype, "getUploadAssetIdByChecksum", null);
    __decorate([
        (0, decorators_1.GenerateSql)({
            params: [decorators_1.DummyValue.UUID, { from: decorators_1.DummyValue.DATE, to: decorators_1.DummyValue.DATE, type: enum_1.CalendarHeatmapType.Upload }],
        }),
        __metadata("design:type", Function),
        __metadata("design:paramtypes", [String, Object]),
        __metadata("design:returntype", void 0)
    ], AssetRepository.prototype, "getCalendarHeatmap", null);
    __decorate([
        (0, decorators_1.GenerateSql)({ params: [{}, { user: { id: decorators_1.DummyValue.UUID } }] }),
        __metadata("design:type", Function),
        __metadata("design:paramtypes", [Object, Object]),
        __metadata("design:returntype", Promise)
    ], AssetRepository.prototype, "getTimeBuckets", null);
    __decorate([
        (0, decorators_1.GenerateSql)({
            params: [decorators_1.DummyValue.TIME_BUCKET, { withStacked: true }, { user: { id: decorators_1.DummyValue.UUID } }],
        }),
        __metadata("design:type", Function),
        __metadata("design:paramtypes", [String, Object, Object]),
        __metadata("design:returntype", void 0)
    ], AssetRepository.prototype, "getTimeBucket", null);
    __decorate([
        (0, decorators_1.GenerateSql)({ params: [decorators_1.DummyValue.UUID, { minAssetsPerField: 5, maxFields: 12 }] }),
        __metadata("design:type", Function),
        __metadata("design:paramtypes", [String, Object]),
        __metadata("design:returntype", Promise)
    ], AssetRepository.prototype, "getAssetIdByCity", null);
    __decorate([
        (0, decorators_1.GenerateSql)({ params: [decorators_1.DummyValue.UUID, 12] }),
        __metadata("design:type", Function),
        __metadata("design:paramtypes", [String, Number]),
        __metadata("design:returntype", Promise)
    ], AssetRepository.prototype, "getRecentlyCreatedAssetIds", null);
    __decorate([
        (0, decorators_1.GenerateSql)({ params: [decorators_1.DummyValue.UUID, [decorators_1.DummyValue.STRING], [decorators_1.DummyValue.STRING]] }),
        __metadata("design:type", Function),
        __metadata("design:paramtypes", [String, Array, Array]),
        __metadata("design:returntype", Promise)
    ], AssetRepository.prototype, "detectOfflineExternalAssets", null);
    __decorate([
        (0, decorators_1.GenerateSql)({ params: [decorators_1.DummyValue.UUID, [decorators_1.DummyValue.STRING]] }),
        __metadata("design:type", Function),
        __metadata("design:paramtypes", [String, Array]),
        __metadata("design:returntype", Promise)
    ], AssetRepository.prototype, "filterNewExternalAssetPaths", null);
    __decorate([
        (0, decorators_1.GenerateSql)({ params: [decorators_1.DummyValue.UUID, true] }),
        __metadata("design:type", Function),
        __metadata("design:paramtypes", [String, Boolean]),
        __metadata("design:returntype", void 0)
    ], AssetRepository.prototype, "getForOriginal", null);
    __decorate([
        (0, decorators_1.GenerateSql)({ params: [[decorators_1.DummyValue.UUID], true] }),
        __metadata("design:type", Function),
        __metadata("design:paramtypes", [Array, Boolean]),
        __metadata("design:returntype", void 0)
    ], AssetRepository.prototype, "getForOriginals", null);
    __decorate([
        (0, decorators_1.GenerateSql)({ params: [decorators_1.DummyValue.UUID, enum_1.AssetFileType.Preview, true] }),
        __metadata("design:type", Function),
        __metadata("design:paramtypes", [String, String, Boolean]),
        __metadata("design:returntype", Promise)
    ], AssetRepository.prototype, "getForThumbnail", null);
    __decorate([
        (0, decorators_1.GenerateSql)({ params: [decorators_1.DummyValue.UUID] }),
        __metadata("design:type", Function),
        __metadata("design:paramtypes", [String]),
        __metadata("design:returntype", Promise)
    ], AssetRepository.prototype, "getForVideo", null);
    __decorate([
        (0, decorators_1.GenerateSql)({ params: [decorators_1.DummyValue.UUID] }),
        __metadata("design:type", Function),
        __metadata("design:paramtypes", [String]),
        __metadata("design:returntype", Promise)
    ], AssetRepository.prototype, "getForOcr", null);
    __decorate([
        (0, decorators_1.GenerateSql)({ params: [decorators_1.DummyValue.UUID] }),
        __metadata("design:type", Function),
        __metadata("design:paramtypes", [String]),
        __metadata("design:returntype", Promise)
    ], AssetRepository.prototype, "getForEdit", null);
    __decorate([
        (0, decorators_1.GenerateSql)({ params: [decorators_1.DummyValue.UUID] }),
        __metadata("design:type", Function),
        __metadata("design:paramtypes", [String]),
        __metadata("design:returntype", Promise)
    ], AssetRepository.prototype, "getForMetadataExtractionTags", null);
    __decorate([
        (0, decorators_1.GenerateSql)({ params: [decorators_1.DummyValue.UUID] }),
        __metadata("design:type", Function),
        __metadata("design:paramtypes", [String]),
        __metadata("design:returntype", Promise)
    ], AssetRepository.prototype, "getForFaces", null);
    __decorate([
        (0, decorators_1.GenerateSql)({ params: [decorators_1.DummyValue.UUID] }),
        __metadata("design:type", Function),
        __metadata("design:paramtypes", [String]),
        __metadata("design:returntype", Promise)
    ], AssetRepository.prototype, "getForUpdateTags", null);
    exports.AssetRepository = AssetRepository = __decorate([
        (0, common_1.Injectable)(),
        __param(0, (0, nestjs_kysely_1.InjectKysely)()),
        __metadata("design:paramtypes", [kysely_1.Kysely])
    ], AssetRepository);
    //# sourceMappingURL=asset.repository.js.map
  metadata.service.js: |
    "use strict";
    var __decorate = (this && this.__decorate) || function (decorators, target, key, desc) {
        var c = arguments.length, r = c < 3 ? target : desc === null ? desc = Object.getOwnPropertyDescriptor(target, key) : desc, d;
        if (typeof Reflect === "object" && typeof Reflect.decorate === "function") r = Reflect.decorate(decorators, target, key, desc);
        else for (var i = decorators.length - 1; i >= 0; i--) if (d = decorators[i]) r = (c < 3 ? d(r) : c > 3 ? d(target, key, r) : d(target, key)) || r;
        return c > 3 && r && Object.defineProperty(target, key, r), r;
    };
    var __metadata = (this && this.__metadata) || function (k, v) {
        if (typeof Reflect === "object" && typeof Reflect.metadata === "function") return Reflect.metadata(k, v);
    };
    var __importDefault = (this && this.__importDefault) || function (mod) {
        return (mod && mod.__esModule) ? mod : { "default": mod };
    };
    Object.defineProperty(exports, "__esModule", { value: true });
    exports.MetadataService = void 0;
    exports.firstDateTime = firstDateTime;
    const common_1 = require("@nestjs/common");
    const exiftool_vendored_1 = require("exiftool-vendored");
    const lodash_1 = __importDefault(require("lodash"));
    const luxon_1 = require("luxon");
    const promises_1 = require("node:fs/promises");
    const node_path_1 = require("node:path");
    const storage_core_1 = require("../cores/storage.core");
    const decorators_1 = require("../decorators");
    const enum_1 = require("../enum");
    const base_service_1 = require("./base.service");
    const asset_util_1 = require("../utils/asset.util");
    const database_1 = require("../utils/database");
    const date_1 = require("../utils/date");
    const mime_types_1 = require("../utils/mime-types");
    const misc_1 = require("../utils/misc");
    const tag_1 = require("../utils/tag");
    const tasks_1 = require("../utils/tasks");
    const POSTGRES_INT_MAX = 2_147_483_647;
    const POSTGRES_INT_MIN = -2_147_483_648;
    const EXIF_DATE_TAGS = [
        'SubSecDateTimeOriginal',
        'SubSecCreateDate',
        'DateTimeOriginal',
        'CreationDate',
        'CreateDate',
        'MediaCreateDate',
        'DateTimeCreated',
        'GPSDateTime',
        'DateTimeUTC',
        'SonyDateTime2',
        'SourceImageCreateTime',
    ];
    function firstDateTime(tags) {
        for (const tag of EXIF_DATE_TAGS) {
            const tagValue = tags?.[tag];
            if (tagValue instanceof exiftool_vendored_1.ExifDateTime) {
                return {
                    tag,
                    dateTime: tagValue,
                };
            }
            if (typeof tagValue !== 'string') {
                continue;
            }
            const exifDateTime = exiftool_vendored_1.ExifDateTime.fromEXIF(tagValue);
            if (exifDateTime) {
                return {
                    tag,
                    dateTime: exifDateTime,
                };
            }
        }
    }
    const validate = (value) => {
        if (Array.isArray(value)) {
            value = value[0];
        }
        if (typeof value === 'string') {
            return null;
        }
        if (typeof value === 'number' &&
            (Number.isNaN(value) || !Number.isFinite(value) || value < POSTGRES_INT_MIN || value > POSTGRES_INT_MAX)) {
            return null;
        }
        return value ?? null;
    };
    const validateRange = (value, min, max) => {
        const val = validate(value);
        if (val === null || val < min || val > max) {
            return null;
        }
        return Math.round(val);
    };
    const getLensModel = (exifTags) => {
        const lensModel = String(exifTags.LensID ?? exifTags.LensType ?? exifTags.LensSpec ?? exifTags.LensModel ?? '').trim();
        if (lensModel === '----') {
            return null;
        }
        if (lensModel.startsWith('Unknown')) {
            return null;
        }
        return lensModel || null;
    };
    let MetadataService = class MetadataService extends base_service_1.BaseService {
        async onBootstrap() {
            this.logger.log('Bootstrapping metadata service');
            await this.init();
        }
        async onShutdown() {
            await this.metadataRepository.teardown();
        }
        onConfigInit({ newConfig }) {
            this.metadataRepository.setMaxConcurrency(newConfig.job.metadataExtraction.concurrency);
        }
        onConfigUpdate({ newConfig }) {
            this.metadataRepository.setMaxConcurrency(newConfig.job.metadataExtraction.concurrency);
        }
        async init() {
            this.logger.log('Initializing metadata service');
            try {
                await this.jobRepository.pause(enum_1.QueueName.MetadataExtraction);
                await this.databaseRepository.withLock(enum_1.DatabaseLock.GeodataImport, () => this.mapRepository.init());
                await this.jobRepository.resume(enum_1.QueueName.MetadataExtraction);
                this.logger.log(`Initialized local reverse geocoder`);
            }
            catch (error) {
                this.logger.error(`Unable to initialize reverse geocoding: ${error}`, error?.stack);
                throw new Error('Metadata service init failed', { cause: error });
            }
        }
        async linkLivePhotos(asset, exifInfo) {
            if (!exifInfo.livePhotoCID) {
                return;
            }
            const otherType = asset.type === enum_1.AssetType.Video ? enum_1.AssetType.Image : enum_1.AssetType.Video;
            const match = await this.assetRepository.findLivePhotoMatch({
                livePhotoCID: exifInfo.livePhotoCID,
                ownerId: asset.ownerId,
                libraryId: asset.libraryId,
                otherAssetId: asset.id,
                type: otherType,
            });
            if (!match) {
                return;
            }
            const [photoAsset, motionAsset] = asset.type === enum_1.AssetType.Image ? [asset, match] : [match, asset];
            await Promise.all([
                this.assetRepository.update({ id: photoAsset.id, livePhotoVideoId: motionAsset.id }),
                this.assetRepository.update({ id: motionAsset.id, visibility: enum_1.AssetVisibility.Hidden }),
                this.albumRepository.removeAssetsFromAll([motionAsset.id]),
            ]);
            await this.eventRepository.emit('AssetHide', { assetId: motionAsset.id, userId: motionAsset.ownerId });
        }
        isOrientationSidewards(orientation) {
            return [
                enum_1.ExifOrientation.MirrorHorizontalRotate270CW,
                enum_1.ExifOrientation.Rotate90CW,
                enum_1.ExifOrientation.MirrorHorizontalRotate90CW,
                enum_1.ExifOrientation.Rotate270CW,
            ].includes(orientation);
        }
        async handleQueueMetadataExtraction(job) {
            const { force } = job;
            for await (const assets of (0, misc_1.batched)(this.assetJobRepository.streamForMetadataExtraction(force))) {
                await this.jobRepository.queueAll(assets.map((asset) => ({ name: enum_1.JobName.AssetExtractMetadata, data: { id: asset.id } })));
            }
            return enum_1.JobStatus.Success;
        }
        async handleMetadataExtraction(data) {
            const [{ metadata, reverseGeocoding }, asset] = await Promise.all([
                this.getConfig({ withCache: true }),
                this.assetJobRepository.getForMetadataExtraction(data.id),
            ]);
            if (!asset) {
                return;
            }
            const [exifResult, stats] = await Promise.all([
                this.getExifTags(asset),
                this.storageRepository.stat(asset.originalPath),
            ]);
            const { tags: exifTags, audio, video, packets, format } = exifResult;
            this.logger.verbose('Exif Tags', exifTags);
            const dates = this.getDates(asset, exifTags, stats);
            const { width, height } = this.getImageDimensions(exifTags);
            let geo = { country: null, state: null, city: null }, latitude = null, longitude = null;
            if (this.hasGeo(exifTags)) {
                latitude = Number(exifTags.GPSLatitude);
                longitude = Number(exifTags.GPSLongitude);
                if (reverseGeocoding.enabled) {
                    geo = await this.mapRepository.reverseGeocode({ latitude, longitude });
                }
            }
            const tags = this.getTagList(exifTags);
            const exifData = {
                assetId: asset.id,
                dateTimeOriginal: dates.dateTimeOriginal,
                modifyDate: stats.mtime,
                timeZone: dates.timeZone,
                latitude,
                longitude,
                country: geo.country,
                state: geo.state,
                city: geo.city,
                fileSizeInByte: stats.size,
                exifImageHeight: validate(height),
                exifImageWidth: validate(width),
                orientation: validate(exifTags.Orientation)?.toString() ?? null,
                projectionType: exifTags.ProjectionType ? exifTags.ProjectionType.toUpperCase() : null,
                bitsPerSample: this.getBitsPerSample(exifTags),
                colorspace: exifTags.ColorSpace === undefined ? null : String(exifTags.ColorSpace),
                make: exifTags.Make ?? exifTags.Device?.Manufacturer ?? exifTags.AndroidMake ?? (exifTags.DeviceManufacturer || null),
                model: exifTags.Model ?? exifTags.Device?.ModelName ?? exifTags.AndroidModel ?? (exifTags.DeviceModelName || null),
                fps: video?.frameRate ?? validate(Number(exifTags.VideoFrameRate)),
                iso: validate(exifTags.ISO),
                exposureTime: exifTags.ExposureTime ?? null,
                lensModel: getLensModel(exifTags),
                fNumber: validate(exifTags.FNumber),
                focalLength: validate(exifTags.FocalLength),
                description: String(exifTags.ImageDescription || exifTags.Description || '').trim(),
                profileDescription: exifTags.ProfileDescription || null,
                rating: exifTags.Rating === 0 ? null : validateRange(exifTags.Rating, 1, 5),
                livePhotoCID: (exifTags.ContentIdentifier || exifTags.MediaGroupUUID) ?? null,
                autoStackId: this.getAutoStackId(exifTags),
                tags: tags.length > 0 ? tags : null,
            };
            const audioData = format && audio?.codecName
                ? {
                    assetId: asset.id,
                    bitrate: audio.bitrate,
                    index: audio.index,
                    profile: audio.profile,
                    codecName: audio.codecName,
                }
                : undefined;
            const videoData = format?.formatName && format.formatLongName && video?.codecName && video?.timeBase
                ? {
                    assetId: asset.id,
                    bitrate: video.bitrate,
                    frameCount: video.frameCount,
                    timeBase: video.timeBase,
                    index: video.index,
                    profile: video.profile,
                    level: video.level,
                    colorPrimaries: video.colorPrimaries,
                    colorTransfer: video.colorTransfer,
                    colorMatrix: video.colorMatrix,
                    dvProfile: video.dvProfile,
                    dvLevel: video.dvLevel,
                    dvBlSignalCompatibilityId: video.dvBlSignalCompatibilityId,
                    codecName: video.codecName,
                    formatName: format.formatName,
                    formatLongName: format.formatLongName,
                    pixelFormat: video.pixelFormat,
                        cropTop: video.cropTop,
                        cropBottom: video.cropBottom,
                        cropLeft: video.cropLeft,
                        cropRight: video.cropRight,
                    }
                : undefined;
            const keyframeData = packets && packets.keyframePts.length > 0
                ? {
                    assetId: asset.id,
                    totalDuration: packets.totalDuration,
                    packetCount: packets.packetCount,
                    outputFrames: packets.outputFrames,
                    pts: packets.keyframePts,
                    accDuration: packets.keyframeAccDuration,
                    ownDuration: packets.keyframeOwnDuration,
                }
                : undefined;
            const isSidewards = exifTags.Orientation && this.isOrientationSidewards(exifTags.Orientation);
            const assetWidth = validate(isSidewards ? height : width);
            const assetHeight = validate(isSidewards ? width : height);
            const tasks = new tasks_1.Tasks();
            tasks.push(() => this.assetRepository.update({
                id: asset.id,
                duration: this.getDuration(exifTags),
                localDateTime: dates.localDateTime,
                fileCreatedAt: dates.dateTimeOriginal ?? undefined,
                fileModifiedAt: stats.mtime,
                width: !asset.isEdited || asset.width === null ? assetWidth : undefined,
                height: !asset.isEdited || asset.height === null ? assetHeight : undefined,
            }), async () => {
                await this.assetRepository.upsertExif({
                    exif: exifData,
                    audio: audioData,
                    video: videoData,
                    keyframes: keyframeData,
                    lockedPropertiesBehavior: 'skip',
                });
                await this.applyTagList(asset);
            });
            if (this.isMotionPhoto(asset, exifTags)) {
                tasks.push(() => this.applyMotionPhotos(asset, exifTags, dates, stats));
            }
            if ((0, misc_1.isFaceImportEnabled)(metadata) && this.hasTaggedFaces(exifTags)) {
                tasks.push(() => this.applyTaggedFaces(asset, exifTags));
            }
            await tasks.all();
            if (exifData.livePhotoCID) {
                await this.linkLivePhotos(asset, exifData);
            }
            await this.assetRepository.upsertJobStatus({ assetId: asset.id, metadataExtractedAt: new Date() });
            await this.eventRepository.emit('AssetMetadataExtracted', {
                assetId: asset.id,
                userId: asset.ownerId,
                source: data.source,
            });
        }
        async handleQueueSidecar({ force }) {
            for await (const assets of (0, misc_1.batched)(this.assetJobRepository.streamForSidecar(force))) {
                await this.jobRepository.queueAll(assets.map((asset) => ({ name: enum_1.JobName.SidecarCheck, data: { id: asset.id } })));
            }
            return enum_1.JobStatus.Success;
        }
        async handleSidecarCheck({ id }) {
            const asset = await this.assetJobRepository.getForSidecarCheckJob(id);
            if (!asset) {
                return;
            }
            let sidecarPath = null;
            for (const candidate of this.getSidecarCandidates(asset)) {
                const isExists = await this.storageRepository.checkFileExists(candidate, promises_1.constants.R_OK);
                if (!isExists) {
                    continue;
                }
                sidecarPath = candidate;
                break;
            }
            const { sidecarFile } = (0, asset_util_1.getAssetFiles)(asset.files);
            const isChanged = sidecarPath !== sidecarFile?.path;
            if (sidecarFile?.path || sidecarPath) {
                this.logger.debug(`Sidecar check found old=${sidecarFile?.path}, new=${sidecarPath} will ${isChanged ? 'update' : 'do nothing for'} asset ${asset.id}: ${asset.originalPath}`);
            }
            else {
                this.logger.verbose(`No sidecars found for asset ${asset.id}: ${asset.originalPath}`);
            }
            if (!isChanged) {
                return enum_1.JobStatus.Skipped;
            }
            await (sidecarPath === null
                ? this.assetRepository.deleteFile({ assetId: asset.id, type: enum_1.AssetFileType.Sidecar })
                : this.assetRepository.upsertFile({ assetId: asset.id, type: enum_1.AssetFileType.Sidecar, path: sidecarPath }));
            return enum_1.JobStatus.Success;
        }
        async handleTagAsset({ assetId }) {
            await this.jobRepository.queue({ name: enum_1.JobName.SidecarWrite, data: { id: assetId } });
        }
        async handleUntagAsset({ assetId }) {
            await this.jobRepository.queue({ name: enum_1.JobName.SidecarWrite, data: { id: assetId } });
        }
        async handleSidecarWrite(job) {
            const { id } = job;
            const asset = await this.assetJobRepository.getForSidecarWriteJob(id);
            if (!asset) {
                return enum_1.JobStatus.Failed;
            }
            const lockedProperties = await this.assetJobRepository.getLockedPropertiesForMetadataExtraction(id);
            const { sidecarFile } = (0, asset_util_1.getAssetFiles)(asset.files);
            const sidecarPath = sidecarFile?.path || `${asset.originalPath}.xmp`;
            const { description, dateTimeOriginal, latitude, longitude, rating, tags, timeZone } = lodash_1.default.pick({
                description: asset.exifInfo.description,
                dateTimeOriginal: asset.exifInfo.dateTimeOriginal,
                latitude: asset.exifInfo.latitude,
                longitude: asset.exifInfo.longitude,
                rating: asset.exifInfo.rating ?? 0,
                tags: asset.exifInfo.tags,
                timeZone: asset.exifInfo.timeZone,
            }, lockedProperties);
            const exif = lodash_1.default.omitBy({
                Description: description,
                ImageDescription: description,
                DateTimeOriginal: (0, date_1.mergeTimeZone)(dateTimeOriginal, timeZone)?.toISO(),
                GPSLatitude: latitude,
                GPSLongitude: longitude,
                Rating: rating,
                TagsList: tags,
            }, lodash_1.default.isUndefined);
            if (Object.keys(exif).length === 0) {
                return enum_1.JobStatus.Skipped;
            }
            await this.metadataRepository.writeTags(sidecarPath, exif);
            if (asset.files.length === 0) {
                await this.assetRepository.upsertFile({ assetId: id, type: enum_1.AssetFileType.Sidecar, path: sidecarPath });
            }
            await this.assetRepository.unlockProperties(asset.id, lockedProperties);
            return enum_1.JobStatus.Success;
        }
        getSidecarCandidates({ files, originalPath }) {
            const candidates = [];
            const { sidecarFile } = (0, asset_util_1.getAssetFiles)(files);
            if (sidecarFile?.path) {
                candidates.push(sidecarFile.path);
            }
            const assetPath = (0, node_path_1.parse)(originalPath);
            candidates.push(`${originalPath}.xmp`, `${(0, node_path_1.join)(assetPath.dir, assetPath.name)}.xmp`);
            return candidates;
        }
        getImageDimensions(exifTags) {
            let [width, height] = exifTags.ImageSize?.toString()
                ?.split('x')
                ?.map((dim) => Number.parseInt(dim) || undefined) ?? [];
            if (!width || !height) {
                [width, height] = [exifTags.ImageWidth, exifTags.ImageHeight];
            }
            return { width, height };
        }
        async getExifTags(asset) {
            const { sidecarFile } = (0, asset_util_1.getAssetFiles)(asset.files);
            const shouldProbe = asset.type === enum_1.AssetType.Video || asset.originalPath.toLowerCase().endsWith('.gif');
            const [mediaTags, sidecarTags, videoResult] = await Promise.all([
                this.metadataRepository.readTags(asset.originalPath),
                sidecarFile ? this.metadataRepository.readTags(sidecarFile.path) : null,
                shouldProbe ? this.getVideoTags(asset.originalPath) : null,
            ]);
            if (sidecarTags) {
                const result = firstDateTime(sidecarTags);
                const sidecarDate = result?.dateTime;
                if (sidecarDate) {
                    for (const tag of EXIF_DATE_TAGS) {
                        delete mediaTags[tag];
                    }
                    for (const tag of ['zone', 'tz', 'tzSource']) {
                        delete mediaTags[tag];
                    }
                }
            }
            if (videoResult || !mime_types_1.mimeTypes.isPossiblyAnimatedImage(asset.originalPath)) {
                delete mediaTags.Duration;
            }
            delete sidecarTags?.Duration;
            if (mime_types_1.mimeTypes.isHeifImage(asset.originalPath)) {
                const orientation = this.getHeifOrientation(mediaTags);
                if (orientation === null) {
                    delete mediaTags.Orientation;
                }
                else {
                    mediaTags.Orientation = orientation;
                }
            }
            return {
                tags: { ...mediaTags, ...videoResult?.tags, ...sidecarTags },
                audio: videoResult?.audio,
                video: videoResult?.video,
                packets: videoResult?.packets,
                format: videoResult?.format ?? null,
            };
        }
        getTagList(exifTags) {
            let tags;
            if (exifTags.TagsList) {
                tags = exifTags.TagsList.map(String);
            }
            else if (exifTags.HierarchicalSubject) {
                tags = exifTags.HierarchicalSubject.map((tag) => typeof tag === 'number'
                    ? String(tag)
                    : tag
                        .split('|')
                        .map((tag) => tag.replaceAll('/', '|'))
                        .join('/'));
            }
            else if (exifTags.Keywords) {
                let keywords = exifTags.Keywords;
                if (!Array.isArray(keywords)) {
                    keywords = [keywords];
                }
                tags = keywords.map(String);
            }
            else {
                tags = [];
            }
            return tags;
        }
        async applyTagList({ id, ownerId }) {
            const asset = await this.assetRepository.getForMetadataExtractionTags(id);
            const results = await (0, tag_1.upsertTags)(this.tagRepository, {
                userId: ownerId,
                tags: asset?.tags ?? [],
            });
            await this.tagRepository.replaceAssetTags(id, results.map((tag) => tag.id));
        }
        isMotionPhoto(asset, tags) {
            return asset.type === enum_1.AssetType.Image && !!(tags.MotionPhoto || tags.MicroVideo);
        }
        async applyMotionPhotos(asset, tags, dates, stats) {
            const isMotionPhoto = tags.MotionPhoto;
            const isMicroVideo = tags.MicroVideo;
            const videoOffset = tags.MicroVideoOffset;
            const hasMotionPhotoVideo = tags.MotionPhotoVideo;
            const hasEmbeddedVideoFile = tags.EmbeddedVideoType === 'MotionPhoto_Data' && tags.EmbeddedVideoFile;
            const directory = Array.isArray(tags.ContainerDirectory)
                ? tags.ContainerDirectory
                : null;
            let length = 0;
            let padding = 0;
            if (isMotionPhoto && directory) {
                for (const entry of directory) {
                    if (entry?.Item?.Semantic === 'MotionPhoto') {
                        length = entry.Item.Length ?? 0;
                        padding = entry.Item.Padding ?? 0;
                        break;
                    }
                }
            }
            if (isMicroVideo && typeof videoOffset === 'number') {
                length = videoOffset;
            }
            if (!length && !hasEmbeddedVideoFile && !hasMotionPhotoVideo) {
                return;
            }
            this.logger.debug(`Starting motion photo video extraction for asset ${asset.id}: ${asset.originalPath}`);
            try {
                const position = stats.size - length - padding;
                let video;
                if (hasMotionPhotoVideo) {
                    video = await this.metadataRepository.extractBinaryTag(asset.originalPath, 'MotionPhotoVideo');
                }
                else if (hasEmbeddedVideoFile) {
                    video = await this.metadataRepository.extractBinaryTag(asset.originalPath, 'EmbeddedVideoFile');
                }
                else {
                    video = await this.storageRepository.readFile(asset.originalPath, {
                        buffer: Buffer.alloc(length),
                        position,
                        length,
                    });
                }
                const checksum = this.cryptoRepository.hashSha1(video);
                const checksumQuery = { ownerId: asset.ownerId, libraryId: asset.libraryId ?? undefined, checksum };
                let motionAsset = await this.assetRepository.getByChecksum(checksumQuery);
                let isNewMotionAsset = false;
                if (!motionAsset) {
                    try {
                        const motionAssetId = this.cryptoRepository.randomUUID();
                        motionAsset = await this.assetRepository.create({
                            id: motionAssetId,
                            libraryId: asset.libraryId,
                            type: enum_1.AssetType.Video,
                            fileCreatedAt: dates.dateTimeOriginal,
                            fileModifiedAt: stats.mtime,
                            localDateTime: dates.localDateTime,
                            checksum,
                            checksumAlgorithm: enum_1.ChecksumAlgorithm.sha1File,
                            ownerId: asset.ownerId,
                            originalPath: storage_core_1.StorageCore.getAndroidMotionPath(asset, motionAssetId),
                            originalFileName: `${(0, node_path_1.parse)(asset.originalFileName).name}.mp4`,
                            visibility: enum_1.AssetVisibility.Hidden,
                        });
                        isNewMotionAsset = true;
                        if (!asset.isExternal) {
                            await this.userRepository.updateUsage(asset.ownerId, video.byteLength);
                        }
                    }
                    catch (error) {
                        if (!(0, database_1.isAssetChecksumConstraint)(error)) {
                            throw error;
                        }
                        motionAsset = await this.assetRepository.getByChecksum(checksumQuery);
                        if (!motionAsset) {
                            this.logger.warn(`Unable to find existing motion video asset for ${asset.id}: ${asset.originalPath}`);
                            return;
                        }
                    }
                }
                if (!isNewMotionAsset) {
                    this.logger.debugFn(() => {
                        const base64Checksum = checksum.toString('base64');
                        return `Motion asset with checksum ${base64Checksum} already exists for asset ${asset.id}: ${asset.originalPath}`;
                    });
                }
                if (motionAsset.visibility === enum_1.AssetVisibility.Timeline) {
                    await this.assetRepository.update({
                        id: motionAsset.id,
                        visibility: enum_1.AssetVisibility.Hidden,
                    });
                    this.logger.log(`Hid unlinked motion photo video asset (${motionAsset.id})`);
                }
                if (asset.livePhotoVideoId !== motionAsset.id) {
                    await this.assetRepository.update({ id: asset.id, livePhotoVideoId: motionAsset.id });
                    if (asset.livePhotoVideoId) {
                        await this.jobRepository.queue({
                            name: enum_1.JobName.AssetDelete,
                            data: { id: asset.livePhotoVideoId, deleteOnDisk: true },
                        });
                        this.logger.log(`Removed old motion photo video asset (${asset.livePhotoVideoId})`);
                    }
                }
                const isExistsOnDisk = await this.storageRepository.checkFileExists(motionAsset.originalPath);
                if (!isExistsOnDisk) {
                    this.storageCore.ensureFolders(motionAsset.originalPath);
                    await this.storageRepository.createFile(motionAsset.originalPath, video);
                    this.logger.log(`Wrote motion photo video to ${motionAsset.originalPath}`);
                    await this.handleMetadataExtraction({ id: motionAsset.id });
                    await this.jobRepository.queue({ name: enum_1.JobName.AssetEncodeVideo, data: { id: motionAsset.id } });
                }
                this.logger.debug(`Finished motion photo video extraction for asset ${asset.id}: ${asset.originalPath}`);
            }
            catch (error) {
                this.logger.error(`Failed to extract motion video for ${asset.id}: ${asset.originalPath}: ${error}`, error?.stack);
            }
        }
        hasTaggedFaces(tags) {
            return (tags.RegionInfo !== undefined && tags.RegionInfo.AppliedToDimensions && tags.RegionInfo.RegionList.length > 0);
        }
        orientRegionInfo(regionInfo, orientation) {
            if (orientation === undefined || orientation === enum_1.ExifOrientation.Horizontal) {
                return regionInfo;
            }
            const isSidewards = this.isOrientationSidewards(orientation);
            const adjustedAppliedToDimensions = isSidewards
                ? {
                    ...regionInfo.AppliedToDimensions,
                    W: regionInfo.AppliedToDimensions.H,
                    H: regionInfo.AppliedToDimensions.W,
                }
                : regionInfo.AppliedToDimensions;
            const adjustedRegionList = regionInfo.RegionList.map((region) => {
                let { X, Y, W, H } = region.Area;
                X = Number(X);
                Y = Number(Y);
                W = Number(W);
                H = Number(H);
                switch (orientation) {
                    case enum_1.ExifOrientation.MirrorHorizontal: {
                        X = 1 - X;
                        break;
                    }
                    case enum_1.ExifOrientation.Rotate180: {
                        [X, Y] = [1 - X, 1 - Y];
                        break;
                    }
                    case enum_1.ExifOrientation.MirrorVertical: {
                        Y = 1 - Y;
                        break;
                    }
                    case enum_1.ExifOrientation.MirrorHorizontalRotate270CW: {
                        [X, Y] = [Y, X];
                        break;
                    }
                    case enum_1.ExifOrientation.Rotate90CW: {
                        [X, Y] = [1 - Y, X];
                        break;
                    }
                    case enum_1.ExifOrientation.MirrorHorizontalRotate90CW: {
                        [X, Y] = [1 - Y, 1 - X];
                        break;
                    }
                    case enum_1.ExifOrientation.Rotate270CW: {
                        [X, Y] = [Y, 1 - X];
                        break;
                    }
                }
                if (isSidewards) {
                    [W, H] = [H, W];
                }
                return {
                    ...region,
                    Area: { ...region.Area, X, Y, W, H },
                };
            });
            return {
                ...regionInfo,
                AppliedToDimensions: adjustedAppliedToDimensions,
                RegionList: adjustedRegionList,
            };
        }
        async applyTaggedFaces(asset, tags) {
            if (!tags.RegionInfo?.AppliedToDimensions || tags.RegionInfo.RegionList.length === 0) {
                return;
            }
            const facesToAdd = [];
            const existingNames = await this.personRepository.getDistinctNames(asset.ownerId, { withHidden: true });
            const existingNameMap = new Map(existingNames.map(({ personGroupId, name }) => [name.toLowerCase(), personGroupId]));
            const missing = [];
            const missingWithFaceAsset = [];
            const adjustedRegionInfo = this.orientRegionInfo(tags.RegionInfo, tags.Orientation);
            const imageWidth = adjustedRegionInfo.AppliedToDimensions.W;
            const imageHeight = adjustedRegionInfo.AppliedToDimensions.H;
            for (const region of adjustedRegionInfo.RegionList) {
                if (!region.Name) {
                    continue;
                }
                const loweredName = region.Name.toLowerCase();
                const personGroupId = existingNameMap.get(loweredName) || this.cryptoRepository.randomUUID();
                const X = Number(region.Area.X);
                const Y = Number(region.Area.Y);
                const W = Number(region.Area.W);
                const H = Number(region.Area.H);
                const face = {
                    id: this.cryptoRepository.randomUUID(),
                    personGroupId,
                    assetId: asset.id,
                    imageWidth,
                    imageHeight,
                    boundingBoxX1: Math.floor((X - W / 2) * imageWidth),
                    boundingBoxY1: Math.floor((Y - H / 2) * imageHeight),
                    boundingBoxX2: Math.floor((X + W / 2) * imageWidth),
                    boundingBoxY2: Math.floor((Y + H / 2) * imageHeight),
                    sourceType: enum_1.SourceType.Exif,
                };
                facesToAdd.push(face);
                if (!existingNameMap.has(loweredName)) {
                    missing.push({
                        personGroupId,
                        ownerId: asset.ownerId,
                        clusterGroupId: asset.clusterGroupId,
                        name: region.Name,
                    });
                    missingWithFaceAsset.push({ personGroupId, ownerId: asset.ownerId, faceAssetId: face.id });
                }
            }
            if (missing.length > 0) {
                this.logger.debugFn(() => `Creating missing persons: ${missing.map((p) => `${p.name}/${p.personGroupId}`)}`);
                await this.personRepository.createGroups(missing.map((item) => ({ id: item.personGroupId, clusterGroupId: asset.clusterGroupId })));
                await this.personRepository.createAll(missing.map(({ name, ownerId, personGroupId }) => ({ name, ownerId, personGroupId })));
                const jobs = missing.map(({ personGroupId, ownerId }) => ({ name: enum_1.JobName.PersonGenerateThumbnail, data: { personGroupId, ownerId } }));
                await this.jobRepository.queueAll(jobs);
            }
            const facesToRemove = asset.faces.filter((face) => face.sourceType === enum_1.SourceType.Exif).map((face) => face.id);
            if (facesToRemove.length > 0) {
                this.logger.debug(`Removing ${facesToRemove.length} faces for asset ${asset.id}: ${asset.originalPath}`);
            }
            if (facesToAdd.length > 0) {
                this.logger.debug(`Creating ${facesToAdd.length} faces from metadata for asset ${asset.id}: ${asset.originalPath}`);
            }
            if (facesToRemove.length > 0 || facesToAdd.length > 0) {
                await this.personRepository.refreshFaces(facesToAdd, facesToRemove);
            }
            if (missingWithFaceAsset.length > 0) {
                await this.personRepository.updateAll(missingWithFaceAsset);
            }
        }
        getDates(asset, exifTags, stats) {
            const result = firstDateTime(exifTags);
            const tag = result?.tag;
            const dateTime = result?.dateTime;
            if (dateTime) {
                this.logger.verbose(`Date and time is ${dateTime} using exifTag ${tag} for asset ${asset.id}: ${asset.originalPath}`);
            }
            else {
                this.logger.verbose(`No exif date time information found for asset ${asset.id}: ${asset.originalPath}`);
            }
            let timeZone = exifTags.zone ?? null;
            if (timeZone === null && (dateTime?.rawValue?.endsWith('Z') || dateTime?.rawValue?.endsWith('+00:00'))) {
                timeZone = 'UTC+0';
            }
            if (timeZone) {
                this.logger.verbose(`Found timezone ${timeZone} via ${exifTags.zoneSource} for asset ${asset.id}: ${asset.originalPath}`);
            }
            else {
                this.logger.debug(`No timezone information found for asset ${asset.id}: ${asset.originalPath}`);
            }
            let dateTimeOriginal = dateTime?.toDateTime();
            if (dateTimeOriginal && !dateTime?.hasZone) {
                dateTimeOriginal = dateTimeOriginal.setZone('UTC', { keepLocalTime: true });
            }
            dateTimeOriginal = dateTimeOriginal?.setZone(timeZone ?? 'UTC');
            let localDateTime = dateTimeOriginal?.setZone('UTC', { keepLocalTime: true });
            if (!localDateTime || !dateTimeOriginal) {
                const earliestDate = luxon_1.DateTime.fromMillis(Math.min(asset.fileCreatedAt.getTime(), stats.birthtimeMs ? Math.min(stats.mtimeMs, stats.birthtimeMs) : stats.mtime.getTime()));
                this.logger.debug(`No exif date time found, falling back on ${earliestDate.toISO()}, earliest of file creation and modification for asset ${asset.id}: ${asset.originalPath}`);
                dateTimeOriginal = localDateTime = earliestDate;
            }
            this.logger.verbose(`Found local date time ${localDateTime.toISO()} for asset ${asset.id}: ${asset.originalPath}`);
            return {
                timeZone,
                localDateTime: localDateTime.toJSDate(),
                dateTimeOriginal: dateTimeOriginal.toJSDate(),
            };
        }
        hasGeo(tags) {
            const lat = Number(tags.GPSLatitude);
            const lng = Number(tags.GPSLongitude);
            return !Number.isNaN(lat) && !Number.isNaN(lng) && (lat !== 0 || lng !== 0);
        }
        getAutoStackId(tags) {
            if (!tags) {
                return null;
            }
            return tags.BurstID ?? tags.BurstUUID ?? tags.CameraBurstID ?? tags.MediaUniqueID ?? null;
        }
        getBitsPerSample(tags) {
            const bitDepthTags = [
                tags.BitsPerSample,
                tags.ComponentBitDepth,
                tags.ImagePixelDepth,
                tags.BitDepth,
                tags.ColorBitDepth,
            ].map((tag) => (typeof tag === 'string' ? Number.parseInt(tag) : tag));
            let bitsPerSample = bitDepthTags.find((tag) => typeof tag === 'number' && !Number.isNaN(tag)) ?? null;
            if (bitsPerSample && bitsPerSample >= 24 && bitsPerSample % 3 === 0) {
                bitsPerSample /= 3;
            }
            return bitsPerSample;
        }
        getDuration(tags) {
            const duration = tags.Duration;
            const seconds = typeof duration === 'number' ? duration : Number.parseFloat(duration);
            return Number.isFinite(seconds) ? Math.round(luxon_1.Duration.fromObject({ seconds }).toMillis()) : null;
        }
        async getVideoTags(originalPath) {
            const { videoStreams, audioStreams, format } = await this.mediaRepository.probe(originalPath);
            const video = videoStreams[0];
            const audio = audioStreams[0];
            const packets = video?.timeBase ? await this.mediaRepository.probePackets(originalPath, video.index) : null;
            const tags = {};
            if (video) {
                if (video.width) {
                    tags.ImageWidth = video.width;
                }
                if (video.height) {
                    tags.ImageHeight = video.height;
                }
                switch (video.rotation) {
                    case -90: {
                        tags.Orientation = enum_1.ExifOrientation.Rotate90CW;
                        break;
                    }
                    case 0: {
                        tags.Orientation = enum_1.ExifOrientation.Horizontal;
                        break;
                    }
                    case 90: {
                        tags.Orientation = enum_1.ExifOrientation.Rotate270CW;
                        break;
                    }
                    case 180: {
                        tags.Orientation = enum_1.ExifOrientation.Rotate180;
                        break;
                    }
                }
            }
            if (format.duration) {
                tags.Duration = format.duration;
            }
            return { tags, audio, video, packets, format };
        }
        getHeifOrientation(exifTags) {
            const rotation = typeof exifTags.Rotation === 'number' ? exifTags.Rotation : undefined;
            switch (rotation) {
                case 0: {
                    return enum_1.ExifOrientation.Horizontal;
                }
                case 1: {
                    return enum_1.ExifOrientation.Rotate270CW;
                }
                case 2: {
                    return enum_1.ExifOrientation.Rotate180;
                }
                case 3: {
                    return enum_1.ExifOrientation.Rotate90CW;
                }
                default: {
                    return null;
                }
            }
        }
    };
    exports.MetadataService = MetadataService;
    __decorate([
        (0, decorators_1.OnEvent)({ name: 'AppBootstrap', workers: [enum_1.ImmichWorker.Microservices] }),
        __metadata("design:type", Function),
        __metadata("design:paramtypes", []),
        __metadata("design:returntype", Promise)
    ], MetadataService.prototype, "onBootstrap", null);
    __decorate([
        (0, decorators_1.OnEvent)({ name: 'AppShutdown' }),
        __metadata("design:type", Function),
        __metadata("design:paramtypes", []),
        __metadata("design:returntype", Promise)
    ], MetadataService.prototype, "onShutdown", null);
    __decorate([
        (0, decorators_1.OnEvent)({ name: 'ConfigInit', workers: [enum_1.ImmichWorker.Microservices] }),
        __metadata("design:type", Function),
        __metadata("design:paramtypes", [Object]),
        __metadata("design:returntype", void 0)
    ], MetadataService.prototype, "onConfigInit", null);
    __decorate([
        (0, decorators_1.OnEvent)({ name: 'ConfigUpdate', workers: [enum_1.ImmichWorker.Microservices], server: true }),
        __metadata("design:type", Function),
        __metadata("design:paramtypes", [Object]),
        __metadata("design:returntype", void 0)
    ], MetadataService.prototype, "onConfigUpdate", null);
    __decorate([
        (0, decorators_1.OnJob)({ name: enum_1.JobName.AssetExtractMetadataQueueAll, queue: enum_1.QueueName.MetadataExtraction }),
        __metadata("design:type", Function),
        __metadata("design:paramtypes", [Object]),
        __metadata("design:returntype", Promise)
    ], MetadataService.prototype, "handleQueueMetadataExtraction", null);
    __decorate([
        (0, decorators_1.OnJob)({ name: enum_1.JobName.AssetExtractMetadata, queue: enum_1.QueueName.MetadataExtraction }),
        __metadata("design:type", Function),
        __metadata("design:paramtypes", [Object]),
        __metadata("design:returntype", Promise)
    ], MetadataService.prototype, "handleMetadataExtraction", null);
    __decorate([
        (0, decorators_1.OnJob)({ name: enum_1.JobName.SidecarQueueAll, queue: enum_1.QueueName.Sidecar }),
        __metadata("design:type", Function),
        __metadata("design:paramtypes", [Object]),
        __metadata("design:returntype", Promise)
    ], MetadataService.prototype, "handleQueueSidecar", null);
    __decorate([
        (0, decorators_1.OnJob)({ name: enum_1.JobName.SidecarCheck, queue: enum_1.QueueName.Sidecar }),
        __metadata("design:type", Function),
        __metadata("design:paramtypes", [Object]),
        __metadata("design:returntype", Promise)
    ], MetadataService.prototype, "handleSidecarCheck", null);
    __decorate([
        (0, decorators_1.OnEvent)({ name: 'AssetTag' }),
        __metadata("design:type", Function),
        __metadata("design:paramtypes", [Object]),
        __metadata("design:returntype", Promise)
    ], MetadataService.prototype, "handleTagAsset", null);
    __decorate([
        (0, decorators_1.OnEvent)({ name: 'AssetUntag' }),
        __metadata("design:type", Function),
        __metadata("design:paramtypes", [Object]),
        __metadata("design:returntype", Promise)
    ], MetadataService.prototype, "handleUntagAsset", null);
    __decorate([
        (0, decorators_1.OnJob)({ name: enum_1.JobName.SidecarWrite, queue: enum_1.QueueName.Sidecar }),
        __metadata("design:type", Function),
        __metadata("design:paramtypes", [Object]),
        __metadata("design:returntype", Promise)
    ], MetadataService.prototype, "handleSidecarWrite", null);
    exports.MetadataService = MetadataService = __decorate([
        (0, common_1.Injectable)()
    ], MetadataService);
    //# sourceMappingURL=metadata.service.js.map
  media.service.js: |
    "use strict";
    var __decorate = (this && this.__decorate) || function (decorators, target, key, desc) {
        var c = arguments.length, r = c < 3 ? target : desc === null ? desc = Object.getOwnPropertyDescriptor(target, key) : desc, d;
        if (typeof Reflect === "object" && typeof Reflect.decorate === "function") r = Reflect.decorate(decorators, target, key, desc);
        else for (var i = decorators.length - 1; i >= 0; i--) if (d = decorators[i]) r = (c < 3 ? d(r) : c > 3 ? d(target, key, r) : d(target, key)) || r;
        return c > 3 && r && Object.defineProperty(target, key, r), r;
    };
    var __metadata = (this && this.__metadata) || function (k, v) {
        if (typeof Reflect === "object" && typeof Reflect.metadata === "function") return Reflect.metadata(k, v);
    };
    Object.defineProperty(exports, "__esModule", { value: true });
    exports.MediaService = void 0;
    const common_1 = require("@nestjs/common");
    const constants_1 = require("../constants");
    const storage_core_1 = require("../cores/storage.core");
    const decorators_1 = require("../decorators");
    const editing_dto_1 = require("../dtos/editing.dto");
    const enum_1 = require("../enum");
    const base_service_1 = require("./base.service");
    const asset_util_1 = require("../utils/asset.util");
    const editor_1 = require("../utils/editor");
    const media_1 = require("../utils/media");
    const mime_types_1 = require("../utils/mime-types");
    const misc_1 = require("../utils/misc");
    const transform_1 = require("../utils/transform");
    let MediaService = class MediaService extends base_service_1.BaseService {
        videoInterfaces = { dri: [], mali: false };
        async onBootstrap() {
            this.videoInterfaces = await this.storageCore.getVideoInterfaces();
        }
        async handleQueueGenerateThumbnails({ force }) {
            const config = await this.getConfig({ withCache: true });
            const isFullsizeEnabled = config.image.fullsize.enabled;
            for await (const assets of (0, misc_1.batched)(this.assetJobRepository.streamForThumbnailJob({ force, fullsizeEnabled: isFullsizeEnabled }))) {
                const jobs = [];
                for (const asset of assets) {
                    if (force || !asset.isEdited) {
                        jobs.push({ name: enum_1.JobName.AssetGenerateThumbnails, data: { id: asset.id } });
                    }
                    if (asset.isEdited) {
                        jobs.push({ name: enum_1.JobName.AssetEditThumbnailGeneration, data: { id: asset.id } });
                    }
                }
                await this.jobRepository.queueAll(jobs);
            }
            for await (const people of (0, misc_1.batched)(this.personRepository.getAll(force ? undefined : { thumbnailPath: '' }))) {
                const jobs = [];
                for (const person of people) {
                    const { ownerId, personGroupId } = person;
                    if (!person.faceAssetId) {
                        const face = await this.personRepository.getRandomFace(personGroupId);
                        if (!face) {
                            continue;
                        }
                        await this.personRepository.update({ ownerId, personGroupId, faceAssetId: face.id });
                    }
                    jobs.push({ name: enum_1.JobName.PersonGenerateThumbnail, data: { ownerId, personGroupId } });
                }
                await this.jobRepository.queueAll(jobs);
            }
            return enum_1.JobStatus.Success;
        }
        async handleQueueMigration() {
            const { active, waiting } = await this.jobRepository.getJobCounts(enum_1.QueueName.Migration);
            if (active === 1 && waiting === 0) {
                await this.storageCore.removeEmptyDirs(enum_1.StorageFolder.Thumbnails);
                await this.storageCore.removeEmptyDirs(enum_1.StorageFolder.EncodedVideo);
            }
            for await (const assets of (0, misc_1.batched)(this.assetJobRepository.streamForMigrationJob())) {
                await this.jobRepository.queueAll(assets.map((asset) => ({ name: enum_1.JobName.AssetFileMigration, data: { id: asset.id } })));
            }
            for await (const people of (0, misc_1.batched)(this.personRepository.getAll())) {
                await this.jobRepository.queueAll(people.map(({ ownerId, personGroupId }) => ({
                    name: enum_1.JobName.PersonFileMigration,
                    data: { ownerId, personGroupId },
                })));
            }
            return enum_1.JobStatus.Success;
        }
        async handleAssetMigration({ id }) {
            const { image } = await this.getConfig({ withCache: true });
            const asset = await this.assetJobRepository.getForMigrationJob(id);
            if (!asset) {
                return enum_1.JobStatus.Failed;
            }
            await this.storageCore.moveAssetImage(asset, enum_1.AssetFileType.FullSize, image.fullsize.format);
            await this.storageCore.moveAssetImage(asset, enum_1.AssetFileType.Preview, image.preview.format);
            await this.storageCore.moveAssetImage(asset, enum_1.AssetFileType.Thumbnail, image.thumbnail.format);
            await this.storageCore.moveAssetVideo(asset);
            return enum_1.JobStatus.Success;
        }
        async handleAssetEditThumbnailGeneration({ id }) {
            const asset = await this.assetJobRepository.getForGenerateThumbnailJob(id);
            const config = await this.getConfig({ withCache: true });
            if (!asset) {
                this.logger.warn(`Thumbnail generation failed for asset ${id}: not found in database or missing metadata`);
                return enum_1.JobStatus.Failed;
            }
            const generated = await this.generateEditedThumbnails(asset, config);
            await this.syncFiles(asset.files.filter((file) => file.isEdited), generated?.files ?? []);
            let thumbhash = generated?.thumbhash;
            if (!thumbhash) {
                const extractedImage = await this.extractOriginalImage(asset, config.image);
                const { info, data, colorspace } = extractedImage;
                thumbhash = await this.mediaRepository.generateThumbhash(data, {
                    colorspace,
                    processInvalidImages: false,
                    raw: info,
                    edits: [],
                });
            }
            if (!asset.thumbhash || Buffer.compare(asset.thumbhash, thumbhash) !== 0) {
                await this.assetRepository.update({ id: asset.id, thumbhash });
            }
            const fullsizeDimensions = generated?.fullsizeDimensions ?? (0, asset_util_1.getDimensions)(asset.exifInfo);
            await this.assetRepository.update({ id: asset.id, ...fullsizeDimensions });
            return enum_1.JobStatus.Success;
        }
        async handleGenerateThumbnails({ id }) {
            const asset = await this.assetJobRepository.getForGenerateThumbnailJob(id);
            const config = await this.getConfig({ withCache: true });
            if (!asset) {
                this.logger.warn(`Thumbnail generation failed for asset ${id}: not found in database or missing metadata`);
                return enum_1.JobStatus.Failed;
            }
            if (asset.visibility === enum_1.AssetVisibility.Hidden) {
                this.logger.verbose(`Thumbnail generation skipped for asset ${id}: not visible`);
                return enum_1.JobStatus.Skipped;
            }
            let generated;
            if (asset.type === enum_1.AssetType.Video || asset.originalFileName.toLowerCase().endsWith('.gif')) {
                this.logger.verbose(`Thumbnail generation for video ${id} ${asset.originalPath}`);
                generated = await this.generateVideoThumbnails(asset, config);
            }
            else if (asset.type === enum_1.AssetType.Image) {
                this.logger.verbose(`Thumbnail generation for image ${id} ${asset.originalPath}`);
                generated = await this.generateImageThumbnails(asset, config);
            }
            else {
                this.logger.warn(`Skipping thumbnail generation for asset ${id}: ${asset.type} is not an image or video`);
                return enum_1.JobStatus.Skipped;
            }
            const editedGenerated = await this.generateEditedThumbnails(asset, config);
            if (editedGenerated) {
                generated.files.push(...editedGenerated.files);
            }
            await this.syncFiles(asset.files, generated.files);
            const thumbhash = editedGenerated?.thumbhash || generated.thumbhash;
            if (!asset.thumbhash || Buffer.compare(asset.thumbhash, thumbhash) !== 0) {
                await this.assetRepository.update({ id: asset.id, thumbhash });
            }
            return enum_1.JobStatus.Success;
        }
        async extractImage(originalPath, minSize) {
            let extracted = await this.mediaRepository.extract(originalPath);
            if (extracted && !(await this.shouldUseExtractedImage(extracted.buffer, minSize))) {
                extracted = null;
            }
            return extracted;
        }
        async decodeImage(thumbSource, exifInfo, targetSize) {
            const { image } = await this.getConfig({ withCache: true });
            const colorspace = this.isSRGB(exifInfo) ? enum_1.Colorspace.Srgb : image.colorspace;
            const decodeOptions = {
                colorspace,
                processInvalidImages: process.env.IMMICH_PROCESS_INVALID_IMAGES === 'true',
                size: targetSize,
                orientation: exifInfo.orientation ? Number(exifInfo.orientation) : undefined,
            };
            const { info, data } = await this.mediaRepository.decodeImage(thumbSource, decodeOptions);
            return { info, data, colorspace };
        }
        async extractOriginalImage(asset, image, useEdits = false) {
            const isExtractEmbedded = image.extractEmbedded && mime_types_1.mimeTypes.isRaw(asset.originalFileName);
            const extracted = isExtractEmbedded ? await this.extractImage(asset.originalPath, image.preview.size) : null;
            const isGenerateFullsize = ((image.fullsize.enabled || asset.exifInfo.projectionType === 'EQUIRECTANGULAR') &&
                !mime_types_1.mimeTypes.isWebSupportedImage(asset.originalPath)) ||
                useEdits;
            const isConvertFullsize = isGenerateFullsize && (!extracted || !mime_types_1.mimeTypes.isWebSupportedImage(` .${extracted.format}`));
            const thumbSource = extracted ? extracted.buffer : asset.originalPath;
            const { data, info, colorspace } = await this.decodeImage(thumbSource, extracted ? asset.exifInfo : { ...asset.exifInfo, orientation: null }, isConvertFullsize ? undefined : image.preview.size);
            let isTransparent = false;
            if (!extracted && mime_types_1.mimeTypes.canBeTransparent(asset.originalPath)) {
                ({ isTransparent } = await this.mediaRepository.getImageMetadata(asset.originalPath));
            }
            return {
                extracted,
                data,
                info,
                colorspace,
                convertFullsize: isConvertFullsize,
                generateFullsize: isGenerateFullsize,
                isTransparent,
            };
        }
        async generateImageThumbnails(asset, { image }, useEdits = false) {
            const extractedImage = await this.extractOriginalImage(asset, image, useEdits);
            const { info, data, colorspace, generateFullsize, convertFullsize, extracted, isTransparent } = extractedImage;
            const previewFormat = image.preview.format;
            this.warnOnTransparencyLoss(isTransparent, previewFormat, asset.id);
            const thumbnailFormat = image.thumbnail.format;
            this.warnOnTransparencyLoss(isTransparent, thumbnailFormat, asset.id);
            const previewFile = this.getImageFile(asset, {
                fileType: enum_1.AssetFileType.Preview,
                format: previewFormat,
                isEdited: useEdits,
                isProgressive: !!image.preview.progressive && previewFormat !== enum_1.ImageFormat.Webp,
                isTransparent,
            });
            const thumbnailFile = this.getImageFile(asset, {
                fileType: enum_1.AssetFileType.Thumbnail,
                format: thumbnailFormat,
                isEdited: useEdits,
                isProgressive: !!image.thumbnail.progressive && thumbnailFormat !== enum_1.ImageFormat.Webp,
                isTransparent,
            });
            this.storageCore.ensureFolders(previewFile.path);
            const baseOptions = { colorspace, processInvalidImages: false, raw: info, edits: useEdits ? asset.edits : [] };
            const thumbnailOptions = { ...image.thumbnail, ...baseOptions, format: thumbnailFormat };
            const previewOptions = { ...image.preview, ...baseOptions, format: previewFormat };
            const promises = [
                this.mediaRepository.generateThumbhash(data, baseOptions),
                this.mediaRepository.generateThumbnail(data, thumbnailOptions, thumbnailFile.path),
                this.mediaRepository.generateThumbnail(data, previewOptions, previewFile.path),
            ];
            let fullsizeFile;
            if (convertFullsize) {
                const fullsizeFormat = image.fullsize.format;
                this.warnOnTransparencyLoss(isTransparent, fullsizeFormat, asset.id);
                fullsizeFile = this.getImageFile(asset, {
                    fileType: enum_1.AssetFileType.FullSize,
                    format: fullsizeFormat,
                    isEdited: useEdits,
                    isProgressive: !!image.fullsize.progressive && fullsizeFormat !== enum_1.ImageFormat.Webp,
                    isTransparent,
                });
                const fullsizeOptions = {
                    ...baseOptions,
                    format: fullsizeFormat,
                    quality: image.fullsize.quality,
                    progressive: image.fullsize.progressive,
                };
                promises.push(this.mediaRepository.generateThumbnail(data, fullsizeOptions, fullsizeFile.path));
            }
            else if (generateFullsize && extracted && extracted.format === enum_1.RawExtractedFormat.Jpeg) {
                fullsizeFile = this.getImageFile(asset, {
                    fileType: enum_1.AssetFileType.FullSize,
                    format: extracted.format,
                    isEdited: false,
                    isProgressive: !!image.fullsize.progressive && image.fullsize.format !== enum_1.ImageFormat.Webp,
                    isTransparent,
                });
                this.storageCore.ensureFolders(fullsizeFile.path);
                await this.storageRepository.createOrOverwriteFile(fullsizeFile.path, extracted.buffer);
                await this.mediaRepository.writeExif({
                    orientation: asset.exifInfo.orientation,
                    colorspace: asset.exifInfo.colorspace,
                }, fullsizeFile.path);
            }
            const outputs = await Promise.all(promises);
            if (asset.exifInfo.projectionType === 'EQUIRECTANGULAR') {
                const promises = [
                    this.mediaRepository.copyTagGroup('XMP-GPano', asset.originalPath, previewFile.path),
                    fullsizeFile
                        ? this.mediaRepository.copyTagGroup('XMP-GPano', asset.originalPath, fullsizeFile.path)
                        : Promise.resolve(),
                ];
                await Promise.all(promises);
            }
            const decodedDimensions = { width: info.width, height: info.height };
            const fullsizeDimensions = useEdits ? (0, transform_1.getOutputDimensions)(asset.edits, decodedDimensions) : decodedDimensions;
            return {
                files: fullsizeFile ? [previewFile, thumbnailFile, fullsizeFile] : [previewFile, thumbnailFile],
                thumbhash: outputs[0],
                fullsizeDimensions,
            };
        }
        async handleGeneratePersonThumbnail({ ownerId, personGroupId, }) {
            const { image } = await this.getConfig({ withCache: true });
            const data = await this.personRepository.getDataForThumbnailGenerationJob({ ownerId, personGroupId });
            if (!data) {
                this.logger.error(`Could not generate person thumbnail for ${personGroupId}: missing data`);
                return enum_1.JobStatus.Failed;
            }
            const { x1, y1, x2, y2, oldWidth, oldHeight, exifOrientation, previewPath, originalPath } = data;
            let inputImage;
            if (data.type === enum_1.AssetType.Video) {
                if (!previewPath) {
                    this.logger.error(`Could not generate person thumbnail for video ${personGroupId}: missing preview path`);
                    return enum_1.JobStatus.Failed;
                }
                inputImage = previewPath;
            }
            else if (image.extractEmbedded && mime_types_1.mimeTypes.isRaw(originalPath)) {
                const extracted = await this.extractImage(originalPath, image.preview.size);
                inputImage = extracted ? extracted.buffer : originalPath;
            }
            else {
                inputImage = originalPath;
            }
            const { data: decodedImage, info } = await this.mediaRepository.decodeImage(inputImage, {
                colorspace: image.colorspace,
                processInvalidImages: process.env.IMMICH_PROCESS_INVALID_IMAGES === 'true',
                orientation: Buffer.isBuffer(inputImage) && exifOrientation ? Number(exifOrientation) : undefined,
            });
            const thumbnailPath = storage_core_1.StorageCore.getPersonThumbnailPath({ ownerId, personGroupId });
            this.storageCore.ensureFolders(thumbnailPath);
            const thumbnailOptions = {
                colorspace: image.colorspace,
                format: enum_1.ImageFormat.Jpeg,
                raw: info,
                quality: image.thumbnail.quality,
                progressive: false,
                processInvalidImages: false,
                size: constants_1.FACE_THUMBNAIL_SIZE,
                edits: [
                    {
                        action: editing_dto_1.AssetEditAction.Crop,
                        parameters: this.getCrop({ old: { width: oldWidth, height: oldHeight }, new: { width: info.width, height: info.height } }, { x1, y1, x2, y2 }),
                    },
                ],
            };
            await this.mediaRepository.generateThumbnail(decodedImage, thumbnailOptions, thumbnailPath);
            await this.personRepository.update({ ownerId, personGroupId, thumbnailPath });
            return enum_1.JobStatus.Success;
        }
        getCrop(dims, { x1, y1, x2, y2 }) {
            const clampedX1 = (0, misc_1.clamp)(x1, 0, dims.old.width);
            const clampedY1 = (0, misc_1.clamp)(y1, 0, dims.old.height);
            const clampedX2 = (0, misc_1.clamp)(x2, 0, dims.old.width);
            const clampedY2 = (0, misc_1.clamp)(y2, 0, dims.old.height);
            const widthScale = dims.new.width / dims.old.width;
            const heightScale = dims.new.height / dims.old.height;
            const halfWidth = (widthScale * (clampedX2 - clampedX1)) / 2;
            const halfHeight = (heightScale * (clampedY2 - clampedY1)) / 2;
            const middleX = Math.round(widthScale * clampedX1 + halfWidth);
            const middleY = Math.round(heightScale * clampedY1 + halfHeight);
            const targetHalfSize = Math.floor(Math.max(halfWidth, halfHeight) * 1.1);
            const newHalfSize = Math.min(middleX - Math.max(0, middleX - targetHalfSize), middleY - Math.max(0, middleY - targetHalfSize), Math.min(dims.new.width - 1, middleX + targetHalfSize) - middleX, Math.min(dims.new.height - 1, middleY + targetHalfSize) - middleY);
            return {
                x: middleX - newHalfSize,
                y: middleY - newHalfSize,
                width: newHalfSize * 2,
                height: newHalfSize * 2,
            };
        }
        async generateVideoThumbnails(asset, { ffmpeg, image }) {
            const previewFile = this.getImageFile(asset, {
                fileType: enum_1.AssetFileType.Preview,
                format: image.preview.format,
                isEdited: false,
                isProgressive: false,
                isTransparent: false,
            });
            const thumbnailFile = this.getImageFile(asset, {
                fileType: enum_1.AssetFileType.Thumbnail,
                format: image.thumbnail.format,
                isEdited: false,
                isProgressive: false,
                isTransparent: false,
            });
            this.storageCore.ensureFolders(previewFile.path);
            const { videoStream, format } = asset;
            if (!videoStream || !format) {
                throw new Error(`Missing video metadata for asset ${asset.id}`);
            }
            const previewConfig = media_1.ThumbnailConfig.create({ ...ffmpeg, targetResolution: image.preview.size.toString() });
            const thumbConfig = media_1.ThumbnailConfig.create({ ...ffmpeg, targetResolution: image.thumbnail.size.toString() });
            const previewOptions = previewConfig.getCommand(enum_1.TranscodeTarget.Video, videoStream, undefined, format ?? undefined);
            const thumbnailOptions = thumbConfig.getCommand(enum_1.TranscodeTarget.Video, videoStream, undefined, format ?? undefined);
            await this.mediaRepository.transcode(asset.originalPath, previewFile.path, previewOptions);
            await this.mediaRepository.transcode(asset.originalPath, thumbnailFile.path, thumbnailOptions);
            const thumbhash = await this.mediaRepository.generateThumbhash(previewFile.path, {
                colorspace: image.colorspace,
                processInvalidImages: process.env.IMMICH_PROCESS_INVALID_IMAGES === 'true',
            });
            return {
                files: [previewFile, thumbnailFile],
                thumbhash,
                fullsizeDimensions: { width: videoStream.width, height: videoStream.height },
            };
        }
        async handleQueueVideoConversion(job) {
            const { force } = job;
            for await (const assets of (0, misc_1.batched)(this.assetJobRepository.streamForVideoConversion(force))) {
                await this.jobRepository.queueAll(assets.map((asset) => ({ name: enum_1.JobName.AssetEncodeVideo, data: { id: asset.id } })));
            }
            return enum_1.JobStatus.Success;
        }
        async handleVideoConversion({ id }) {
            const asset = await this.assetJobRepository.getForVideoConversion(id);
            if (!asset) {
                return enum_1.JobStatus.Failed;
            }
            const input = asset.originalPath;
            const output = storage_core_1.StorageCore.getEncodedVideoPath(asset);
            this.storageCore.ensureFolders(output);
            const { videoStream, format } = asset;
            const audioStream = asset.audioStream ?? undefined;
            if (!videoStream || !format) {
                this.logger.warn(`Skipped transcoding for asset ${asset.id}: missing metadata; re-run extraction first`);
                return enum_1.JobStatus.Failed;
            }
            if (!videoStream.height || !videoStream.width) {
                this.logger.warn(`Skipped transcoding for asset ${asset.id}: no video dimensions`);
                return enum_1.JobStatus.Failed;
            }
            if (videoStream.cropTop === undefined || videoStream.cropTop === null) {
                const probe = await this.mediaRepository.probe(input);
                if (probe.videoStreams?.[0]) {
                    const s = probe.videoStreams[0];
                    videoStream.cropTop = s.cropTop ?? null;
                    videoStream.cropBottom = s.cropBottom ?? null;
                    videoStream.cropLeft = s.cropLeft ?? null;
                    videoStream.cropRight = s.cropRight ?? null;
                }
            }
            let { ffmpeg } = await this.getConfig({ withCache: true });
            const target = this.getTranscodeTarget(ffmpeg, videoStream, audioStream);
            if (target === enum_1.TranscodeTarget.None && !this.isRemuxRequired(ffmpeg, format)) {
                const encodedVideo = (0, asset_util_1.getAssetFile)(asset.files, enum_1.AssetFileType.EncodedVideo, { isEdited: false });
                if (encodedVideo) {
                    this.logger.log(`Transcoded video exists for asset ${asset.id}, but is no longer required. Deleting...`);
                    await this.jobRepository.queue({ name: enum_1.JobName.FileDelete, data: { files: [encodedVideo.path] } });
                    await this.assetRepository.deleteFiles([encodedVideo]);
                }
                else {
                    this.logger.verbose(`Asset ${asset.id} does not require transcoding based on current policy, skipping`);
                }
                return enum_1.JobStatus.Skipped;
            }
            const command = media_1.BaseConfig.create(ffmpeg, this.videoInterfaces).getCommand(target, videoStream, audioStream);
            if (ffmpeg.accel === enum_1.TranscodeHardwareAcceleration.Disabled) {
                this.logger.log(`Transcoding video ${asset.id} without hardware acceleration`);
            }
            else {
                this.logger.log(`Transcoding video ${asset.id} with ${ffmpeg.accel.toUpperCase()}-accelerated encoding and${ffmpeg.accelDecode ? '' : ' software'} decoding`);
            }
            try {
                await this.mediaRepository.transcode(input, output, command);
            }
            catch (error) {
                this.logger.error(`Error occurred during transcoding: ${error.message}`);
                if (ffmpeg.accel === enum_1.TranscodeHardwareAcceleration.Disabled) {
                    return enum_1.JobStatus.Failed;
                }
                let isPartialFallbackSuccess = false;
                if (ffmpeg.accelDecode) {
                    try {
                        this.logger.error(`Retrying with ${ffmpeg.accel.toUpperCase()}-accelerated encoding and software decoding`);
                        ffmpeg = { ...ffmpeg, accelDecode: false };
                        const command = media_1.BaseConfig.create(ffmpeg, this.videoInterfaces).getCommand(target, videoStream, audioStream);
                        await this.mediaRepository.transcode(input, output, command);
                        isPartialFallbackSuccess = true;
                    }
                    catch (error) {
                        this.logger.error(`Error occurred during transcoding: ${error.message}`);
                    }
                }
                if (!isPartialFallbackSuccess) {
                    this.logger.error(`Retrying with ${ffmpeg.accel.toUpperCase()} acceleration disabled`);
                    ffmpeg = { ...ffmpeg, accel: enum_1.TranscodeHardwareAcceleration.Disabled };
                    const command = media_1.BaseConfig.create(ffmpeg, this.videoInterfaces).getCommand(target, videoStream, audioStream);
                    await this.mediaRepository.transcode(input, output, command);
                }
            }
            this.logger.log(`Successfully encoded ${asset.id}`);
            await this.assetRepository.upsertFile({
                assetId: asset.id,
                type: enum_1.AssetFileType.EncodedVideo,
                path: output,
                isEdited: false,
            });
            return enum_1.JobStatus.Success;
        }
        getTranscodeTarget(config, videoStream, audioStream) {
            const isAudioTranscodeRequired = this.isAudioTranscodeRequired(config, audioStream);
            const isVideoTranscodeRequired = this.isVideoTranscodeRequired(config, videoStream);
            if (isAudioTranscodeRequired && isVideoTranscodeRequired) {
                return enum_1.TranscodeTarget.All;
            }
            if (isAudioTranscodeRequired) {
                return enum_1.TranscodeTarget.Audio;
            }
            if (isVideoTranscodeRequired) {
                return enum_1.TranscodeTarget.Video;
            }
            return enum_1.TranscodeTarget.None;
        }
        isAudioTranscodeRequired(ffmpegConfig, stream) {
            if (!stream) {
                return false;
            }
            switch (ffmpegConfig.transcode) {
                case enum_1.TranscodePolicy.Disabled: {
                    return false;
                }
                case enum_1.TranscodePolicy.All: {
                    return true;
                }
                case enum_1.TranscodePolicy.Required:
                case enum_1.TranscodePolicy.Optimal:
                case enum_1.TranscodePolicy.Bitrate: {
                    return !ffmpegConfig.acceptedAudioCodecs.includes(stream.codecName);
                }
                default: {
                    throw new Error(`Unsupported transcode policy: ${ffmpegConfig.transcode}`);
                }
            }
        }
        isVideoTranscodeRequired(ffmpegConfig, stream) {
            const isScalingEnabled = ffmpegConfig.targetResolution !== 'original';
            const targetRes = Number.parseInt(ffmpegConfig.targetResolution);
            const isLargerThanTargetRes = isScalingEnabled && Math.min(stream.height, stream.width) > targetRes;
            const maxBitrate = this.parseBitrateToBps(ffmpegConfig.maxBitrate);
            const isLargerThanTargetBitrate = maxBitrate > 0 && stream.bitrate > maxBitrate;
            const isTargetVideoCodec = ffmpegConfig.acceptedVideoCodecs.includes(stream.codecName);
            const isRequired = !isTargetVideoCodec || !stream.pixelFormat.endsWith('420p');
            switch (ffmpegConfig.transcode) {
                case enum_1.TranscodePolicy.Disabled: {
                    return false;
                }
                case enum_1.TranscodePolicy.All: {
                    return true;
                }
                case enum_1.TranscodePolicy.Required: {
                    return isRequired;
                }
                case enum_1.TranscodePolicy.Optimal: {
                    return isRequired || isLargerThanTargetRes;
                }
                case enum_1.TranscodePolicy.Bitrate: {
                    return isRequired || isLargerThanTargetBitrate;
                }
                default: {
                    throw new Error(`Unsupported transcode policy: ${ffmpegConfig.transcode}`);
                }
            }
        }
        isRemuxRequired(ffmpegConfig, { formatName, formatLongName }) {
            if (ffmpegConfig.transcode === enum_1.TranscodePolicy.Disabled) {
                return false;
            }
            const formatLongNameMapping = {
                'QuickTime / MOV': enum_1.VideoContainer.Mov,
                'Matroska / WebM': enum_1.VideoContainer.Webm,
            };
            const name = (formatLongName ? formatLongNameMapping[formatLongName] : undefined) ?? formatName;
            return name !== enum_1.VideoContainer.Mp4 && !ffmpegConfig.acceptedContainers.includes(name);
        }
        isSRGB({ colorspace, profileDescription, bitsPerSample, }) {
            if (colorspace || profileDescription) {
                return [colorspace, profileDescription].some((s) => s?.toLowerCase().includes('srgb'));
            }
            if (bitsPerSample) {
                return bitsPerSample === 8;
            }
            return true;
        }
        parseBitrateToBps(bitrateString) {
            const bitrateValue = Number.parseInt(bitrateString);
            if (Number.isNaN(bitrateValue)) {
                this.logger.log(`Maximum bitrate '${bitrateString} is not a number and will be ignored.`);
                return 0;
            }
            if (bitrateString.toLowerCase().endsWith('k')) {
                return bitrateValue * 1000;
            }
            if (bitrateString.toLowerCase().endsWith('m')) {
                return bitrateValue * 1_000_000;
            }
            return bitrateValue;
        }
        async shouldUseExtractedImage(extractedPathOrBuffer, targetSize) {
            const { width, height } = await this.mediaRepository.getImageMetadata(extractedPathOrBuffer);
            const extractedSize = Math.min(width, height);
            return extractedSize >= targetSize;
        }
        async syncFiles(oldFiles, newFiles) {
            const toUpsert = [];
            const pathsToDelete = [];
            const toDelete = new Set(oldFiles);
            for (const newFile of newFiles) {
                const existingFile = oldFiles.find((file) => file.type === newFile.type && file.isEdited === newFile.isEdited);
                if (existingFile) {
                    toDelete.delete(existingFile);
                }
                if (existingFile?.path !== newFile.path ||
                    existingFile.isProgressive !== newFile.isProgressive ||
                    existingFile.isTransparent !== newFile.isTransparent) {
                    toUpsert.push(newFile);
                    if (existingFile && existingFile.path !== newFile.path) {
                        this.logger.debug(`Deleting old ${newFile.type} image for asset ${newFile.assetId} in favor of a replacement`);
                        pathsToDelete.push(existingFile.path);
                    }
                }
            }
            if (toUpsert.length > 0) {
                await this.assetRepository.upsertFiles(toUpsert);
            }
            if (toDelete.size > 0) {
                const toDeleteArray = [...toDelete];
                for (const file of toDeleteArray) {
                    pathsToDelete.push(file.path);
                }
                await this.assetRepository.deleteFiles(toDeleteArray);
            }
            if (pathsToDelete.length > 0) {
                await this.jobRepository.queue({ name: enum_1.JobName.FileDelete, data: { files: pathsToDelete } });
            }
        }
        async generateEditedThumbnails(asset, config) {
            if (asset.type !== enum_1.AssetType.Image || (asset.files.length === 0 && asset.edits.length === 0)) {
                return;
            }
            const generated = asset.edits.length > 0 ? await this.generateImageThumbnails(asset, config, true) : undefined;
            const crop = asset.edits.find((e) => e.action === editing_dto_1.AssetEditAction.Crop);
            const cropBox = crop
                ? {
                    x1: crop.parameters.x,
                    y1: crop.parameters.y,
                    x2: crop.parameters.x + crop.parameters.width,
                    y2: crop.parameters.y + crop.parameters.height,
                }
                : undefined;
            const originalDimensions = (0, asset_util_1.getDimensions)(asset.exifInfo);
            const assetFaces = await this.personRepository.getFaces(asset.id, { viewingUserId: asset.ownerId });
            const ocrData = await this.ocrRepository.getByAssetId(asset.id, {});
            const faceStatuses = (0, editor_1.checkFaceVisibility)(assetFaces, originalDimensions, cropBox);
            await this.personRepository.updateVisibility(faceStatuses.visible, faceStatuses.hidden);
            const ocrStatuses = (0, editor_1.checkOcrVisibility)(ocrData, originalDimensions, cropBox);
            await this.ocrRepository.updateOcrVisibilities(asset.id, ocrStatuses.visible, ocrStatuses.hidden);
            return generated;
        }
        warnOnTransparencyLoss(isTransparent, format, assetId) {
            if (isTransparent && format === enum_1.ImageFormat.Jpeg) {
                this.logger.warn(`Asset ${assetId} has transparency but the configured format is ${format} which does not support it, consider using a format that does, such as ${enum_1.ImageFormat.Webp}`);
            }
        }
        getImageFile(asset, options) {
            const path = storage_core_1.StorageCore.getImagePath(asset, options);
            return {
                assetId: asset.id,
                type: options.fileType,
                path,
                isEdited: options.isEdited,
                isProgressive: options.isProgressive,
                isTransparent: options.isTransparent,
            };
        }
    };
    exports.MediaService = MediaService;
    __decorate([
        (0, decorators_1.OnEvent)({ name: 'AppBootstrap', workers: [enum_1.ImmichWorker.Microservices] }),
        __metadata("design:type", Function),
        __metadata("design:paramtypes", []),
        __metadata("design:returntype", Promise)
    ], MediaService.prototype, "onBootstrap", null);
    __decorate([
        (0, decorators_1.OnJob)({ name: enum_1.JobName.AssetGenerateThumbnailsQueueAll, queue: enum_1.QueueName.ThumbnailGeneration }),
        __metadata("design:type", Function),
        __metadata("design:paramtypes", [Object]),
        __metadata("design:returntype", Promise)
    ], MediaService.prototype, "handleQueueGenerateThumbnails", null);
    __decorate([
        (0, decorators_1.OnJob)({ name: enum_1.JobName.FileMigrationQueueAll, queue: enum_1.QueueName.Migration }),
        __metadata("design:type", Function),
        __metadata("design:paramtypes", []),
        __metadata("design:returntype", Promise)
    ], MediaService.prototype, "handleQueueMigration", null);
    __decorate([
        (0, decorators_1.OnJob)({ name: enum_1.JobName.AssetFileMigration, queue: enum_1.QueueName.Migration }),
        __metadata("design:type", Function),
        __metadata("design:paramtypes", [Object]),
        __metadata("design:returntype", Promise)
    ], MediaService.prototype, "handleAssetMigration", null);
    __decorate([
        (0, decorators_1.OnJob)({ name: enum_1.JobName.AssetEditThumbnailGeneration, queue: enum_1.QueueName.Editor }),
        __metadata("design:type", Function),
        __metadata("design:paramtypes", [Object]),
        __metadata("design:returntype", Promise)
    ], MediaService.prototype, "handleAssetEditThumbnailGeneration", null);
    __decorate([
        (0, decorators_1.OnJob)({ name: enum_1.JobName.AssetGenerateThumbnails, queue: enum_1.QueueName.ThumbnailGeneration }),
        __metadata("design:type", Function),
        __metadata("design:paramtypes", [Object]),
        __metadata("design:returntype", Promise)
    ], MediaService.prototype, "handleGenerateThumbnails", null);
    __decorate([
        (0, decorators_1.OnJob)({ name: enum_1.JobName.PersonGenerateThumbnail, queue: enum_1.QueueName.ThumbnailGeneration }),
        __metadata("design:type", Function),
        __metadata("design:paramtypes", [Object]),
        __metadata("design:returntype", Promise)
    ], MediaService.prototype, "handleGeneratePersonThumbnail", null);
    __decorate([
        (0, decorators_1.OnJob)({ name: enum_1.JobName.AssetEncodeVideoQueueAll, queue: enum_1.QueueName.VideoConversion }),
        __metadata("design:type", Function),
        __metadata("design:paramtypes", [Object]),
        __metadata("design:returntype", Promise)
    ], MediaService.prototype, "handleQueueVideoConversion", null);
    __decorate([
        (0, decorators_1.OnJob)({ name: enum_1.JobName.AssetEncodeVideo, queue: enum_1.QueueName.VideoConversion }),
        __metadata("design:type", Function),
        __metadata("design:paramtypes", [Object]),
        __metadata("design:returntype", Promise)
    ], MediaService.prototype, "handleVideoConversion", null);
    exports.MediaService = MediaService = __decorate([
        (0, common_1.Injectable)()
    ], MediaService);
    //# sourceMappingURL=media.service.js.map
